# r13 诊断报告（2026-09-14）

**轮次性质**：HUD 动效 + UI 音效 + 相机特写俯角修复后的回归出图（非新美术轮次）。
上一轮 r12 之后本波改动：`f460b70`（HUD 动效/UI 音效）、`704bf28`（特写俯角真 bug 修复）、
`8dd88b0`（55 条测试断言清账，无视觉改动）、`62f25b0`（注释校准，无视觉改动）。

## 结论：无回归，可交用户实玩验收手感

- **洋红 8/8 = 0 像素**；全图无黑帧/无异常（8 张 0.8~3.0MB 真实内容）。
- **HUD 全机位**（`hud-fullscreen.jpg`）：顶栏三模式段、回合/时间、双方存活、名册 8 行、
  小地图、返回、右下提示条全部在位；武器面板未选中时隐藏（设计行为）。
  CanvasGroup 动效改造无布局回归。
- **选中反馈**（`unit-closeup.jpg`）：青色虚线描边正常，Lambert 明暗正常。
- **爆炸机位**（`explosion-moment.png`）：火光 + 蒸汽 + 弹道预览虚线正常；
  r7 已裁决的「无橙色火球壳」（D-1）维持不变，不在本轮范围。
- **`battle-45` 构图与 r12 不同 = 预期**：特写俯角修复后（45°→30° 首次真实生效），
  该机位记录的玩家跟随视角变成真正的 30° 俯角特写——这是修复的证据，不是退化。

## 待用户实玩验收（静帧无法判断）

HUD 动效手感：面板滑入/淡出速度、回合横幅 punch、受击行弹跳、按钮 punch、血条滚动节奏
（常量集中在 `Assets/Scripts/UI/UiMotionRules.cs`，全部标提案/待定）；UI 音效
（UiClick/UiPanelOpen/UiError）音量是否合适（`SfxCatalog` 配方）。
