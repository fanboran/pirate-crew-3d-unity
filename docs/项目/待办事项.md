# 待办事项

> 按 AGENTS 约定记录改进待办；按里程碑组织，做完的挪到底部归档区。
>
> 最近更新：2026-09-22（六支 3D 像素渲染视频调研入库
> [调研-3D像素渲染视频整合](../技术/渲染/调研-3D像素渲染视频整合.md)：步骤 2 遗留⑥ 的描边路线拿到两条候选依据，
> 步骤 3 增补「帧级调色板量化」与「抖动范式 (b)」两项；**同日 v3 源码级蓝图入库
> [蓝图-新渲染管线-v3蓝本](../技术/渲染/蓝图-新渲染管线-v3蓝本.md)**：给出 15 张缓冲 / 9 个 Feature 的真实执行次序、
> 「v3 里其实没在跑的东西」四条源码实证、P0→P5 路线与三张移植清单 + 6 个裁决点）

## 当前任务板（美术风格翻新：等距像素卡通 + M4 大海域世界化）

- [ ] **美术风格翻新流程**（创始人裁决 2026-09-21 转向等距像素卡通，开工序列与口径见
      [渲染管线-等距像素卡通.md](../技术/渲染/渲染管线-等距像素卡通.md) §8）：
  - [x] **步骤 1：正交相机 + 全屏像素化**（2026-09-21）——`PixelationRendererFeature`（两档渲染器挂载，
        blit 材质 = URP 自带 CoreBlit pass 0 Nearest）+ `BattleCameraController` 正交化（OrthoSize 档位
        特写 5/全场 17/全景 span×0.3/滚轮 [3,60] 整数档；旋转锁死；FOV 特效当量映射）+ URP 全局设置
        （HDR 关/MSAA 1x/附加光 0/主光阴影关）+ 相机测试三文件改正交口径（EditMode 1161/1162 全绿）；
        实拍验收 [docs/images/pixel-review/r1/](../images/pixel-review/r1/README.md)（块化判据 0.00/UI
        不块化/洋红 0/shader error 0）。**俯角已裁决真等距 35.264° + 方位角固定 45°（2026-09-21；ToonPilot 试点场景已按此建，Battle 主相机待 M1 重烘）；OrthoSize 档位与 RT 高 360 仍为 AI 提案，待实玩校准**
  - [x] **步骤 2：赛璐璐 shader + 反向壳描边试点**（2026-09-21）——落成 **ToonPilot 独立风格测试场景**
        （创始人指令：单建测试场景并逐层复杂化，像从头开发新游戏）：`PirateToon.shader`（赛璐璐本体
        + 常驻 INK 反壳描边）+ `ToonLightDriver` + `SmoothNormalsBaker`（顶点色 GBA 平滑法线）
        + `ToonPilotSetup`（岛台/双船员/海面/真等距相机）+ 播放器 `-toonPilotOut` 五机位 + 判据脚本
        `tools/pixel-review/judge_toon_pilot.py`；**门禁全绿**（EditMode 1167/1166/0/1、PlayMode 7/7、
        播放器 shader error 0、装配链五步全绿）。赛璐璐全链已验证工作；描边/几何/船员三处未决见下条
  - [ ] 步骤 2 遗留（下轮首修，按序）：① 岛台顶面生成（`IslandShellGeometry.BuildSolidShell` 列式
        模式生成不出完整顶面——参照 `ShowcaseLevels.BuildLogicGrid` 的平台模式构造）② 船员渲染
        （资产/引用齐备但不可见，编辑器查层级）③ 描边覆盖面（编辑器 Frame Debugger 会诊；
        线宽实测 1 RT px 不可见、2px 起可见）④ 复拍验收色阶对比（抖动已关/暗部与海面基调已压暗）
        ⑤ 顶点色 R 逐格明暗偏移 + 格缝（参照比对权重最大项的零风险先手）⑥ 描边路线裁决：
        反壳只给外轮廓 vs 内部断口线交给纹理/后处理 depth+normal
        ——**调研依据已就位**（[调研-3D像素渲染视频整合](../技术/渲染/调研-3D像素渲染视频整合.md) §4.6/§4.7：
        v2 给七步「单像素 + 明暗双线」屏幕空间描边，v3 给「法线剧变压暗一级」的零成本路线，
        两条候选成本差一个量级，M1 样张出对照图裁决）。
        **第三条候选更轻（2026-09-22 追到 t3ssel8r 作者自述）**：内线 = **深度缓冲取邻域比较、按比邻域近/远比
        对该像素提亮或压暗**（不阈值化、不做遮罩合成、不用 compute）——见
        [参照-t3ssel8r像素引擎](../技术/渲染/参照-t3ssel8r像素引擎.md) §1.4；同篇 §1.5 还给了他的迭代结论
        （**边缘检测器从深度通道重写为 normal pass**，"结果干净得多"）。建议样张对比时以这条为基准方案。
        **v3 那条候选已读通源码（2026-09-22）**：它的 Outline 特征在资产里是 `m_Active: 0`（**根本没在用**），
        画面上的"柱亮边 / 石阶暗线"实为 `RimLightShading()`（屏幕空间光向门控边缘光）＋ `DiffuseShading()` 里
        `connect < 0.75` 时 `ndotl -= singleLevel`（**连通域降一档**）两件合成、**方向随光而变**——
        详见 [蓝图-新渲染管线-v3蓝本](../技术/渲染/蓝图-新渲染管线-v3蓝本.md) §3.1/§3.3 与 §8 裁决点 3
  - [ ] **步骤 2/3 的架构级前置：观感锚裁决 + 管线重写蓝图**（2026-09-22 蓝图已入库，**代码零改动**）——
        蓝图：[蓝图-新渲染管线-v3蓝本](../技术/渲染/蓝图-新渲染管线-v3蓝本.md)。**v3 与本仓的真分野是"着色发生在哪一档
        分辨率"**：本仓是全分辨率前向着色 + 事后降采（降采后没有第二次着色机会）；v3 是物体 pass 只写 G-buffer、
        全部着色与后处理**都在 320×180 域上**跑。已读通的产出：15 张缓冲表（含分辨率/格式）、9 个 RendererFeature
        的注入时机与真实执行次序、3 段连通域 compute（平面预测深度残差 + 8 次 flood + 单元统计）、
        **P0→P5 分阶段路线**、**三张清单**（可直接翻译 10 条 / 必须按本仓裁决改 10 条 / 建议不要搬 8 条）。
        **建议第一步是 P0 的两件零架构改动对照**（RT 高 320×180 vs 360、抖动换 1-bit 密度图案）——
        它们直接回答"观感锚是不是 v3"。**6 个裁决点见蓝图 §8**，其中 #1（观感锚）是其余一切的前置。
        ⚠ 两条源码实证要记住：**v3 的优先级仲裁 Pass 是死 pass**（双重 `SetTargetBuffer` 覆盖 + 要写的 `_UVBuffer`
        从未绑定 + 改尺寸后读越界，三条独立证据），对它的画面**零贡献**；**它的 5× 超采样不是画质手段**（降采是点采样、
        无 box 平均），而是给连通域判据提供细粒度深度/法线
  - [x] 相机口径两条（2026-09-21）：**真等距俯角 35.264°**（渲染篇 §2.1 裁决落地：代码常量/基准方向/
        装配 offset 对角/场景重烘/两处断言）+ **方位角固定 45°**
        ——⚠ **口径已统一（2026-09-22 创始人确认）**：本行原写"方位可旋转（创始人修正）"，与
        [渲染篇 §2](../技术/渲染/渲染管线-等距像素卡通.md)（"旋转**锁死，只平移**"）及
        [立项任务书](../技术/美术翻新-等距像素卡通立项任务书.md)（"方位角固定 45°〔2026-09-21 裁决〕"）
        **三处冲突**，现按**固定**为准；右键环绕退役、观察模式保留为调试出口。屏幕拖拽→世界映射建议实玩复核
  - [ ] 步骤 3：调色板定档（32~64 色）+ Blender 量化脚本，翻新第一个资产
        ——**同期增补两项**（2026-09-22 六视频调研带出，依据见
        [调研-3D像素渲染视频整合](../技术/渲染/调研-3D像素渲染视频整合.md) §4.4/§4.5 与 §5 的 P0）：
        ① **帧级调色板量化**（低分辨率域后处理一趟：线性 → OKLab → 取最近调色板色；骨架可照 v4 源码的
        「双最近色 + 棋盘二选一」）——这是渲染篇 §7「颜色漂出板外」的收敛手段，原列「演进项」；
        **同引擎版本的完整先例已入库**：`SL0ANE/SloanePixelartURP`（Unity 2022.3.43f1 + URP 14，MIT，
        `PaletteGenerationCIEDE.compute` + `ColorCorrection.shader` + 6 个 TestPalette 资产；工作台
        `external/sloane-urp/`）
        ② **抖动范式 (b)**（2×2 抖动贴图 + soft-light，t3ssel8r 系）与现行范式 (a)（4×4 Bayer 加法偏移）
        做成开关做 A/B，两范式**不可叠加**；注意抖动贴图必须**中点归一化** `(m+0.5)/4`——
        v4 源码用的是 `m/4`，会系统性偏暗，**不要照抄**
  - [x] 步骤 4a：**UI 九宫格生成器（Beveled Pixel）已落地**（2026-09-22）——
        `Assets/Editor/BeveledPixelSpriteBuilder.cs` 出 26 张（7 tone × {凸起块, 按压块, 凹槽} + 5 填充，
        色值全部从调色板槽位派生）、判据 `Assets/Art/Tests/BeveledPixelSkinTests.cs`（7 条，直接读盘上
        PNG 量色带/切角/外环闭合/明暗方向）、接入 `ArtGate` ⑦.5 步；实现口径见
        [Beveled Pixel 九宫格规范](../技术/资产管线/BeveledPixel九宫格规范.md)。
        走查修掉两处：① 三层带改**同心环**（原"上下横贯"把两侧外环截断、四角不闭合）；
        ② 切角改**按整格切**（原按像素切出离格锯齿）。两条都已固化成判据
        （外环 8 连通 1 段无端点 / 切角形状独立复算）。
        **装配侧一行未动**——一个界面都还没换皮，玻璃族仍在役
  - [ ] 步骤 4b：一屏换装试点（提案 Battle HUD）→ 全界面扫荡。开工前需裁三项：
        UI 字体（像素字体 vs StickHand 混搭）、`UI_*` 调色板槽定稿（5 个里 3 个仍是提案态）、
        新增"分组标题/分区色"缺一支；另有 6 条复核项（切角深度 / 顶边是否拆两档 / `S1` 派生值 /
        凹槽底次生亮带 / 填充亮沿是否偏黄 / `Danger` 暗档冷紫是否合语义）
  - [ ] 步骤 5：存量资产批量翻新（任务书 M3：49 件 FBX 按「视线焦点 > 单位 > 远景」分批）
  - [x] 阴影关闭后的落地感补偿：**放置类弹体已接**（2026-09-21，`ProjectileContactShadow` 按
        `IsPersistent` 判定挂脚下径向面片；7 个船员 prefab 此前已有）。摸底结论：空中飞行弹体不做
        （需射线贴地组件，像素风下接受）；静态装饰的接触遮蔽待海面/地形方案一并定
  - [x] SSAO 退役（2026-09-21）：Balanced 档反装（`UninstallBalancedSsao`），URP 变体预过滤标志随
        Unity 自动重写；色带表面上的屏幕空间遮蔽是脏渐变来源，像素管线下必关

- [ ] **关卡制作管线 + 样板三关重构**（2026-09-19 用户裁决：两艘程序化船「完全不能看」删除；
      按真关卡策划流程重做——每关一份设计文档，六阶段管线，逐阶段留文本与资产）：
  - [x] 管线章程 + 旧样板三关归档 + L01/L02/L03 设计文档（AI 提案/待终审）+ 布局图/难度曲线
        渲染工具（文档 ASCII 块为唯一真源，`82a0ed5`，[docs/设计/关卡/](../设计/关卡/README.md)）
  - [x] 双船退役（配方/烘焙器/资产/接线全清，-2697 行）+ 三关编成/武器/luck 重写
        （L1 教学 4v3·L2 碎岛雨 4v4·L3 考核 4v5，luck 阶梯 1→2→5）+ 碎岛岛壳烘焙件
        （IslandShellGeometry.BuildSolidShell 与逻辑场按构造对齐）+ 确定性/摆位契约/设计契约
        测试（EditMode 1164 用例 1163 绿 1 跳过，重烘逐字节零漂移，`9b25c6d`）
  - [x] 三关实拍走查（AI 自查通过：L1 云场 7 人贴云 / L2 七岛 8 人贴岛 / L3 空岛 9 人贴草皮，
        无船无洋红；走查记录与实拍图回写各设计文档 §8）
  - [ ] **设计文档用户终审**（三关体验目标/难度曲线/资产方案，docs/设计/关卡/）
  - [ ] Battle.unity 回填提交（当前场景文件交织本线接线与隔壁 HUD 线的场景改动，
        待 HUD 线收口后一并入库——运行时已按新接线工作，提交纯为版本库卫生）
  - [ ] L2 观感升级候选：FloatingIslandComposer 多岛小尺寸版替换岛壳碎岛【提案/待定】

- [ ] **M4 大海域世界化**（2026-09-17 用户裁决，第一批已落地，契约见 [`M4-大海域世界化.md`](../技术/世界化/M4-大海域世界化.md)）：
  - [x] 设计契约 + 统一风格参数 `tools/blender/scene/style_tokens.py`（`ed8ce73`）
  - [x] WorldKit 47 件资产（4 组 subagent 并行：地形/道具/船坞/远景，`f5208d6`）
  - [x] 8 张语义地图数据层 + 连通性/出生点/栅格测试（harness 全绿，`2a55e5c`）
  - [x] BattleController 世界地图分支 + OceanRig/全景档/氛围档接线（`a150abf`）
  - [x] 大海域海面（OceanRig + PirateOcean.shader + HPWater 技法吸收，`d9b7f9f`）
  - [x] 落地翻滚（RollRules 按 Flash 逆向转写）+ 战舰世界式瞄准（`8db0734`）
  - [x] batchmode 门禁全绿（EditMode 1114/0/1、编译 0 错、shader error 0 条、.meta 已入库）
  - [x] 视觉审计修复批次 A–F（2026-09-17，`e1e7b65`..`88f645f`：救活水模拟/长涌包络与雾距
        按"可见海域预算"重定标/阴影 224+SSAO/站面 PirateTerrain 化/漂浮道具起伏；
        EditMode 1146/0 + PlayMode 7/7；数值【提案/待定】）
  - [ ] **视觉批次实拍验收**（新对话按交接 §21 清单看图：涟漪/长涌/阴影/雾距/站面材质/浮标起伏，
        提案值实拍后转正；调参入口见 §21.2）
  - [ ] **五审统筹修复**（架构/代码/UI/地图设计/视觉五份审计的交叉去重与修复排期，
        轨道 Wave 0 + Track 2–8 + 总排序见 [`审计/汇总-五审交叉与修复统筹.md`](../审计/汇总-五审交叉与修复统筹.md)；
        四阀门已裁决 2026-09-17：尺度=方案 D、实拍主出图用户终审、删 TeamTurnTracker、Horizon 接线）
    - [x] Wave 0 卫生 + Track 4 死件清仓 + Track 5 资源生命周期 + Track 7 性能 + Horizon 接线
          + 装配链重跑（波1，`baa4886`..`0449655` 七连，EditMode 1196/0/1 全绿；详见交接 §21）
    - [x] Track 2 玩法 P1（`3c07e9c`/`f11af61`，主协调线 §25 收口）
    - [x] 波 2：Track 6 契约 + 2P 乐句 / 海床板 / 分层军火配置（方案 D）/ 出包贴图 / 障碍掩码 / 俯视海图
          （`542d71c`/`1125e3b`/`26a0f5d`/`11b3920`，主协调线 §25 收口；乐句契约漏改由 `5ef73e2` 补齐）
    - [x] Track 8①②（主模型亲自）：反向依赖修正（`fcda283`，Core/BattleLaunchContext 注入）+
          asmdef 五程序集拆分（`a3973ab`，EditMode 1308/0/1 + PlayMode 7/7 全绿）；架构审计 P0-1 销号（§7）。
          **余项**：P1-4 目录归位 + P1-5 命名空间折叠（163 文件扫荡，须安静窗口）
    - [x] **隔壁交接-1（八图布局重摆与出生构图）**：已收口（`89eeb2d`..`21f9165` 六连，专项文档已删）——
          站面覆盖 3.9-13.8% → **16.6-17.8%**、内容跨度 x 51-76% → **76-94%** / z 29-75% → **69-85%**、
          出生质心间距 40-174u → **44-117u**、八图**全部单连通**、零落水道具（原 33 件）、
          wreck_hymn 破掉严格镜像；出生机位遮挡比 53-80% → **0-44%**；实拍 40 张入库
          `docs/images/worldmap-captures-r2/`（判据脚本 `tools/worldmap/judge_worldmap_captures.py`）。
          顺带修掉两个**计划外**问题：① 散布落点被父级缩放甩飞（50-147 件全部偏 3000-12000u，
          "散布几乎不可见"的根因）；② 波 1 装配链重跑冲掉了 Battle 场景的 WorldKit 资产表与
          海面材质引用（实拍第一版只剩灰盒）——装配链须在链尾补跑
          `WorldMapAssetSetBuilder.BuildAll` 收口。详见 [审计/地图设计审计报告.md](../审计/专项/地图设计审计报告.md) §五
    - [ ] 交接-1 的四条阈值待用户终审（站面覆盖 ≥15% / 内容跨度 ≥55% / 出生质心间距 ≤120u /
          礁石场密度 66-223 件每图，均【提案/待定】）
    - [x] **隔壁交接-2（场景 Prefab 化 + Find 清退）**：**已完成**——四个装配产物场景折叠成
          "一个 Prefab 实例"（Battle 18,772 → 186 行，内容进 `BattleRig.prefab`；三个 UI 场景各配
          一个屏幕 Prefab）；八步装配子链收进 `Editor/BattleScenePipeline.cs`（顺序即代码）；
          折叠态与链顺序由 `Tests/Battle/SceneAssemblyContractTests` 冻结。契约在
          `pirate-crew/Assets/Scenes/README.md`，逐条处置见
          [`审计/场景接线审计报告.md`](../审计/专项/场景接线审计报告.md) 开头的「执行状态」表
    - [ ] **场景 Prefab 拆分的组织性收益（审计报告阶段 2b，有意未做）**：把单个 `BattleRig.prefab`
          按模块拆成 相机 rig / HUD / 世界 rig / 导演 四个嵌套 Prefab。收益是模块归位；代价是
          嵌套 Prefab 之间的交叉引用（Unity 里只能靠"外层 Prefab 引用自己的嵌套实例"表达，
          跨资产引用不成立）。当前 Prefab 由装配链每次重新生成，拆分的组织收益不足以抵消这份风险
    - [ ] 实拍图组（主出图用户终审）：8 图 A–F 验收 + chop 三档 + Horizon 签名核对
    - [x] **隔壁交接-3（环境光天空盒驱动，视觉遗留 #6）**：**已翻转并出图 2026-09-18**——
          用户拍板后三步全落：开关=Skybox + 场景烘焙跟上（`a27add2`）、播放器重建
          （shader error 0，途中抓出 `_MainLightPosition` 重定义——只在真实变体编译暴露，`9cf1bac`）、
          三档 × 五机位实拍入库 `docs/images/ambient-skybox-debug/`（判据全过：三档亮度严格单调
          noon 0.648 > dusk 0.571 > overcast 0.398、渐变方向正确、阴云铅灰无日盘）；
          任务书误读已纠正（自研 shader 早已 `SampleSH` 采样环境光）。
          **待用户终审**三档观感；两个待办：日盘判据需朝阳机位（BuildWorldShots 待加）、
          **海面不可见为跨域阻塞项**（海床板退役=542d71c 有意，但 OceanRig 网格也不绘制且
          Player.log 0 错误；候选 542d71c/11b3920/fcda283，r2 后无人重拍；归因=开关临时改回
          Trilight 重建对照），A–F 实拍验收轮须先修（见 `docs/images/ambient-skybox-debug/README.md`）
    - [ ] **海面不可见归因修复**（2026-09-18 三档出图发现，阻塞 A–F 实拍验收）：世界图模式
          OceanRig 网格不绘制（天空盒下半球直接露出），Player.log 0 错误、`-oceanDebug` 无变化；
          海床板退役（`542d71c`）是有意的，海面不是。候选引入提交 `542d71c`/`11b3920`/`fcda283`；
          与环境光翻转的关系未排除（未找到机制）。水域/世界图域认领
  - [x] Unity 实拍看图（`-worldMap` 捕图参数早已接入 `PlayerArtCapture.BuildWorldShots`；
        kit FBX 换灰盒由 `WorldMapAssetSetBuilder` 生成器完成，波 1 装配链重跑冲掉的接线已补回）
        —— 八图 40 张实拍入库 `docs/images/worldmap-captures-r2/`（交接-1 收口，`9ab372e`）
  - [ ] **UI 脚本不满足 Unity 的「类名 = 文件名、不嵌套」规则（10 处存量，已登记棘轮）**：
        类型 `StickLayoutElement` / `StickLayoutGroup` / `StickContextAnchor` / `WobbledDotGraphic`
        与宿主同文件不同名；`SketchSwitchGraphic` / `SketchToggleGraphic` / `StickHoverScale` /
        `WindowDragHandle` / `ToastFader`（×2，`SketchWidgets` 与 `StickKit` **同名重复**）是嵌套类。
        后果：这些组件**存不进 Prefab**、场景重载后还原不回来（`WavyLineGraphic` 就是同一类，
        做场景 Prefab 化时才暴露、已修）。当前它们都不在任何场景里，故不阻塞；
        清理由 `Tests/UI/ScriptAssetSerializabilityTests` 的 `KnownOffenders` 白名单逐条推进
        （清一条删一行，白名单留僵尸项会被另一条测试抓）。
        两个同名 `ToastFader` 谁留谁删属设计决定，与 Beveled Pixel UI 换装批次一并裁
  - [ ] **接线小缺陷**（场景接线审计 F-9 发现，未出图验证）：`Scene_SandWet.mat` 不在
        `Assets/Art/Materials/Scene/` 里 → 运行时 `RuntimeSceneArt` 的 SandWet 组（簇级水线湿沙）
        被 `MaterialFor` 静默跳过（烘焙版有、运行时版没有）；修法二选一：让 `SceneArtBuilder`
        真的产出该 .mat，或把该槽改指 `Surface_WetSand.mat`
  - [ ] M4 §7 未决项：~~满力射程是否随大图上调~~（**已裁 2026-09-17：方案 D——图跨度维持、
        8 图按原版语义配分层军火（twang20/30 + 全图级五件）+ 俯视海图瞄准 UI**，见
        `审计/汇总-五审交叉与修复统筹.md` §七）/ 战役进度映射 8 图 / 风暴降水系统
  - [ ] FLIP（MIT，`external/fluid-ref/FLIP`）交互液体（落水飞溅/沉没演出）后续候选

### v1.0.0 已收口项（2026-09-16/17）

- [x] **发布收口**（2026-09-16，本轮多个提交）：设置面板真接线（音量×4/画质档/全屏，设置槽 9 读改写）、
  战斗暂停菜单（Esc/按钮 + BattlePause 门控回合/AI/相机/瞄准）、再来一局（场景栈顶替换 replaceTop）、
  战斗结算画面（战役局含星级/经验/新招募）、返回确认弹窗、应用图标（程序化）+ 版本 1.0.0 + MIT LICENSE、
  Debug.Log 收口进 `Core/Log`（发布版删 Info/Warn 调用）、SceneNames 归位 + 删 BattlePlaceholder 遗留。
- [x] **背景音乐出厂音量下调**（2026-09-16 用户反馈）：Ambient 0.8→0.5、Music 0.8→0.7
  （`AudioSettingsStore` 按类别默认值；玩家可在设置面板微调，落盘设置槽 9）。
- [x] **Blender 图标管线**（2026-09-16 立项、17 验收）：`tools/blender/icon/` 无头渲染图标已入库
  （本机 Blender 5.2.1 LTS：`F:/SteamLibrary/steamapps/common/Blender/blender.exe`）。
- [x] **Blender 建模试点撤销**（2026-09-17 用户指示）：**角色模型不许改，低多边形就是定案风格**——
  船员试点产物已全部删除，游戏内角色从未被改动。规则已写入 AGENTS.md。
- [ ] **Blender 场景建模（主线，2026-09-17 起新对话接手）**：样板资产**已入库**——大帆船（含白帆，
  r8+ 清单"悬空白帆补船体"缺口）+ 木栈桥（入水立柱，"木栈桥入水立柱"缺口），
  `tools/blender/scene/` + `Assets/Art/Models/SceneKit/` + 装配/出图脚本，编译 0 错误。
  **剩余步骤照 docs/项目/交接与恢复指南.md §19 的作业序列执行**：BuildAll 生成 showcase →
  手工登记 Build Settings YAML → 重构播放器 → `-sceneKitOut` 出图 → 用户过审 → 接线战斗场景。
- [ ] **用户实玩验收**：设置面板/暂停菜单/结算画面/返回确认的观感与手感；
  HUD 动效手感（r13 遗留）；下调后的背景音量是否合适。
- [x] **图标终选**：Blender 渲染版入库（`tools/blender/icon/render_icon.py` 幂等可重跑；
  程序化版保留在 `ReleaseGate.DrawIcon` 作为回退）。

- [x] **55 条过期测试断言清账**（2026-09-14，`8dd88b0`）：根因=「格子 ×2」扫荡后世界口径全面翻倍
  （`LevelGeometry.TileWorldSize` 1→2 → `PixelsPerUnit` 32→16、重力 −39.0625、水面 −0.4、相机全场 45°/30），
  断言对齐源码真值，测试名随新语义改名；另修 UiStrings 按键名白名单（r12 模式提示有意保留 AD/WS 等）、
  BedPad 音源迁移登记+SHA256 核对。EditMode **55 失败→0**；harness All 86→31（余 31 条 = §4.2 登记的
  无头环境固有失败，非回归）。
- [x] **HUD 动效 + UI 音效接线**（2026-09-14，`f460b70`）：`UiMotionRules`（纯 C# 可无头测）+
  `UiMotion`（unscaled 时间驱动，hit-stop 时 UI 反馈不停）+ `BattleHud` 接线（武器面板滑入/淡出、
  回合横幅 punch、受击行弹跳、按钮 punch）。UI 音效落点：UiClick / UiPanelOpen / UiError——
  J1 音效搬运时「UI 层未接线」的欠账清账。**juice 六层计划至此全部落地**（其余五层此前已由
  `d46c387` 运镜手感、`CrewAnimationRules` 投掷三拍/受击、`WaterSplashFx` 波纹、hit-stop、
  `AudioVariation` 变奏完成）。
- [x] **注释漂移校准**（2026-09-14）：8 处 docstring 仍写 ×2 前旧值；场景文档草丛区间改 200-1500
  （level_1 受陆地格与船面禁草限制实发 ~222 簇）。
- [ ] **r13 出图验收**：ArtGate → 播放器 → 8 机位截图 → 人工观感验收（HUD 动效为运行时表现，
  静帧只能验证面板/横幅状态正确，手感待实玩）。
- [ ] **用户实玩验收 HUD 动效手感**：面板滑入速度、血条滚动节奏、按钮 punch 幅度均为提案/待定
  （见 `UiMotionRules` 常量注释），按实玩反馈调参。
- [ ] **用户拍板项**：仓库 LICENSE；关卡数 33 vs 18 核对。

## 远期（明确不承诺）

- **33 关全量重制**（README 已声明入远期）：关卡数据转写 + 节奏按原版（武器前 5 关发完+地形变奏）。
- **海战/大世界系统**：按 AGENTS 节奏约束，M2 前口径即不碰，当前亦非优先。
- ~~游戏内建模 Blender 全量化~~（表述有误已修正：**角色**不许改；**场景**资产走 Blender 是用户
  明确要求的主线，见当前任务板"Blender 场景建模"条）。
- **BedPad 音源残本**（登记为观察项）：game-2 规范树已无 `ambient_pad.wav`，字节相同残本只在
  `.temp/_restore_morning` 临时快照；残本消失时 `Port_SourcesExistInGame2Checkout` 会报真实失败。
  届时改指向本工程 Resources 副本（已 SHA256 验证一致）或把 BedPad 标记为「源已消费」。

## 工程基建（随时可做）

- [ ] **行尾策略（`.gitattributes`）**：`core.autocrlf=true` 且无 `.gitattributes`，Unity YAML 行尾噪音仍在；
  属仓库级策略变更，留给用户决定。
- [ ] **PlayMode 测试副作用**：跑 PlayMode 会把 `EditorSettings.m_EnterPlayModeOptionsEnabled` 置 1，
  提交前需 `git checkout` 还原（本会话门禁脚本已内置）。
- [ ] **CI**：GitHub Actions + batchmode EditMode/PlayMode（参考 stick-world 的 CI-DI 文档）。
- [ ] **`FontAssetBuilder.BuildAll` 重跑会换字体资产 GUID 并重排字形集**：2026-09-22 实测，
      三个 `Resources/Fonts/*.asset.meta` 的 guid 全部改变、`StickHand-Regular SDF.asset`
      内容大改（586 增 / 1160 删）。当前 HEAD 里没有别的资产引用旧 guid，所以没炸；
      但"重跑一次就是新 GUID"意味着任何**引用方一旦存在就会被静默打断**，且字形集重排会改变文本观感。
      先查清是"删了重建"还是"就地重生成"，再决定要不要把 GUID 固定下来。归属：UI/字体域
- [ ] **`SceneAssetManifestBuilder` 的清单会随目录漂移**：`Assets/Art/Models/scene-assets.json`
      本次由 77 件更新为 83 件（补上 ToonPilot 的 5 个烘焙网格 + 1 个 prefab）——
      清单是生成物，跑链才更新，平时会静默落后。要么纳入门禁（跑链后校验清单是否为最新），
      要么接受"它是快照"并在文档里说明。归属：资产管线

---

## 归档区

### 2026-09-14 两波并行 agent 清账（本次会话）

- Agent A（Battle/Fx/Water/Ambient 域 44 条）：全按源码常量对齐，9 处测试名随新语义改名；
  数值验证后更新 WaterSim CFL 阈值（0.15 ≪ 0.495，60Hz 稳定）。
- Agent B（SceneArt/文案/音频域 14 条）：格号→世界坐标换算（HullSpanX/侧墙抖动筛窗）、
  植被下限 150（实测 222 留 1/3 余量）、岛高上限按相机预设重推（36+1 块 × 0.5 = 18.5）、
  按键名白名单、BedPad 迁移登记。

### M3 船员管理循环（骨架闭环完成）

- [x] Roster / Progression + NUnit、CampaignManager + 选关 UI、招募→编成→出战→结算闭环、
  SaveManager 存档接通（星级与经验曲线为提案标注）。

### M2 战斗（✅ 完成并经真 3D 化重做 + 美术写实化 r2~r7 十轮视觉迭代）

- 坐标权威/运动学/场景/爆炸/AI/弹体/瓦片地形/小地图/描边/17 武器全量落地；门禁与回合规则详见
  [交接与恢复指南.md](交接与恢复指南.md) §2、§6。
- 描边调试截图（`docs/images/outline-debug/`）与美术评审档案（`export/art-review/`）入库。

### M1 主菜单 + 场景流转 + 存档骨架（已完成 2026-09-12）

- [x] EventBus（12 测试）、SceneLoader（7 测试）、SaveManager（20 测试）、Bootstrapper、
  MainMenu/Battle 场景 + 端到端 PlayMode 流转。
- 环境经验沉淀：batchmode 必带 `-projectPath` + `-nographics`；EditMode asmdef 要
  `includePlatforms:["Editor"]`；asmdef 引用只写 name 或只写 GUID。

### M0 骨架（已完成）

- [x] URP 落地、asmdef + UTF 接入、ProjectSettings 定名、`pirate-crew/` 子目录结构。
- [ ] **无头验证台测试发现器失效排查**（2026-09-20 发现）：`dotnet test M2Harness.csproj -p:HarnessScope=All|Combat` 报"没有可用测试"（编译本身通过——源码兼容性仍由它证明，但用例不执行）。NUnit adapter 注册/平台设置层面的问题，先于本轮 namespace 改动存在（csproj 零改动复现）。主验证链 batchmode EditMode 1161 条全绿不受影响，harness 修复后再恢复双轨
