# 交接与恢复指南

> **这份文档干什么用的**：长任务跨对话会话推进时，新会话**先读这份**恢复上下文，不重新摸底。
> 用户说「继续」时：读本文 → 读 [待办事项.md](待办事项.md) → 开工。
> **何时更新**：每次里程碑推进、重大决策变更、或上下文即将压缩之前。
>
> 最近更新：2026-09-19（§29：管线合并任务书五阶段执行完毕——几何生成退出运行时；
> 2026-09-17（§26：Track 8①② 落地——反向依赖修正 + asmdef 五程序集，全量门禁全绿，
> **交接-2 阶段 2a 解锁但等安静窗口**；§22：隔壁交接-2 阶段 1——场景接线**实测**审计 + 切分契约 + 转储/比对工具；
> **前置 Track 8①② 未满足，阶段 2–5 待其落地**；§20：M4 大海域世界化第一批落地；§19：Blender 场景套件样板入库待接线）

> ⚠ **本会话有一条正在进行中的大改动**：[M2-3D化-并行推进与交接.md](归档/M2-3D化-并行推进与交接.md)。
> 第一版把 Flash 的 2D 侧视坐标搬进了 Unity 的 XY 竖直面并冻结 Z，被用户指出"这是 2D 游戏"。
> 现按 **Godot 出空间结构 + Flash 出数值** 重做（规范：[M2-3D空间模型对齐.md](../设计/M2-3D空间模型对齐.md)）。
> **接手前先读那两份文档**，否则很容易按旧的 2D 假设改代码。

---

## 1. 项目一句话

**海盗军团夺宝 3D** —— 求职作品集项目（本人 2027 届计算机本科，求职游戏客户端实习）。Unity 2022.3 重制，
定位是简历上的 Unity 实战素材，证明「引擎概念相通，换引擎能干活」。

两个参照基准，**分工不同，不要混淆**：

| 基准 | 位置 | 作用 |
| --- | --- | --- |
| **2D Flash 原版**《海盗军团抢宝藏》(Nitrome Mutiny) | 逆向后落档在 `docs/技术/参考逆向/参考游戏逆向-海盗军团抢宝藏-静态.md`（868 行） | **玩法/数值/公式的唯一权威**（回合机、17 武器表、爆炸公式、AI 伪代码、33 关） |
| Godot 4 版 | `../game-3/pirate-crew-3d/`（26 个 `.gd` / 2380 行） | **翻译基准与设计参照**，但它是骨架且有多处漂移——**空 TODO 与未接线代码不能当实现来源** |

> ⚠ Godot 版的 `config/*.json` **不是**权威：`config_manager.gd:19` 读的是 `*.cfg` 而目录里只有 `*.json`，
> 从未被运行时加载，只是离线规格。数值一律以 Flash 逆向文档为准。

---

## 2. 当前状态

| 里程碑 | 状态 |
| --- | --- |
| M0 骨架 | ✅ 完成（URP 落地、asmdef + 测试接入、工程定名） |
| M1 流转 | ✅ 完成（主菜单 + 场景流转 + 存档骨架 + 端到端 PlayMode 测试） |
| M2 战斗 | ✅ 完成并经两波并行 agent 收尾：真 3D 化 + 17/17 武器 + 瓦片地形 + 小地图 + 描边出图（§2.1） |
| M3 管理循环 | ✅ 骨架闭环（招募/编成/选关/结算/存档；星级与经验曲线为提案标注） |

### 2.1 ✅ M2 真 3D 化（已完成并提交 `3c7397e`）

**为什么**：第一版把 Flash 原版的 2D 侧视坐标 1:1 搬进 Unity 的 XY 竖直平面、用 `FreezePositionZ` 锁死深度，
成了"披着 3D 引擎的 2D 游戏"；而 Godot 基准是真 3D（XZ 地面 + 45° 相机 + 单位沿 Z 分路）。用户确认按
**对齐 Godot 版真 3D** 重做。

**口径**：Godot 出空间结构、**Flash 逆向文档出数值**（Godot 版的弹道/伤害层是自相矛盾的占位实现，只取结构不取数值）；
竞技场 = XZ 水平面、重力沿 -Y、地面顶面 y=0、水面 y=-0.2、投掷含 `ThrowLift=0.7` 仰角抬升。
细节：[M2-3D空间模型对齐.md](../设计/M2-3D空间模型对齐.md)。

**门禁实测（2026-09-13，波次 2 后最终值）**：EditMode **491/491**、PlayMode **7/7 且 skipped=0**、
编译 0 错误；场景链 `M2BattleSceneSetup.BuildAll → HudMinimapSceneSetup.WireMinimap → M3SceneSetup.BuildAll`
全部 exit 0（顺序敏感：前两步会重建/增量接线 Battle，最后一步写构建列表）。

**两波并行收尾都做了什么**（细节见各提交）：
- 波次 1（`b155f87`/`f9f4b17`/`e867641`/`318fc6b`）：AI 落点评分轴定案（XZ 质心距离罚项）、
  `Ballistics.PredictTrajectory` 死代码删除、天空盒材质归位 Art、**6 把特殊武器补齐**（17/17 可玩）、
  **小地图**（§2.4 点阵/§8.1 配色）、**M3 骨架**（6 名可招募/编成上限 4/3 章 15 关/星级结算/存档槽位 1）。
- 波次 2（`a62004f`）：**瓦片地形**（XZ 格位 +Y 堆叠、爆炸逐格摧毁、AI 按格地表判落点、
  墙反弹走 PhysX；高度语义提案）、**关卡注入**（选关真正决定竞技场数据）、
  **编成注入出战**（战役入口按名册过滤红队）、锚/海鸥/潮汐/加农改**点击放置**（原拖拽预览与实际弹体不符）、
  AI 加农评分、小地图瓦片点阵。

**⚠ 尚未做的一件事：还没有人用眼睛看过它**。场景已重建入库，下一步只需打开编辑器按 Play，
核对"是不是真 3D"（XZ 拉开、透视、阴影、单位沿纵深散布），并让用户验收相机手感/抛物弧线。

**遗留的已知问题**（波次 1 并行收尾已处置，结论如下）：
- AI §6.2 落点打分轴已 3D 化：「更好落点」= 预测落点与敌方存活质心的 **XZ 像素距离**，
  线性罚项 0.003/px（标定依据见 `AiEvaluation.cs` 与 [M2-3D空间模型对齐.md](../设计/M2-3D空间模型对齐.md) §8）。
- `Ballistics.PredictTrajectory` 确认死代码，已删除（含 4 条测试）；`IntegrateGroundContact/WallContact`
  保留给瓦片地形墙反弹接线。
- 天空盒材质已挪至 `Assets/Art/Materials/BattleSky.mat`（GUID 不变，场景零改动）。

**门禁实测数字**（随时可复跑，命令见 §5）：

- EditMode **310/310**；PlayMode **7/7 且 `skipped=0`**（2026-09-13 经 MCP `run_tests` 复跑）
- 无头验证台分域基线：`Combat` 86、`Data` 42、`Battle` 138（`Rendering`/`Runtime`/`DataEditor` 为纯编译检查，0 错误）
- 代码规模：约 40 次提交；`Assets/Scripts` 下 Core 7 / PirateCrew（Data 9、Combat 5、Battle 22+、Rendering 1）/ UI 3 个 `.cs`

**M2 闭环的实际含义**（重要，别高估）：从"选中角色 → 选动作 → 拖拽投掷 → 弹体飞行 → 按条件引爆 →
伤害/击退/死亡 → 回合推进 → 胜负结算"这条链是接通的，且有程序化测试覆盖；**描边/选中反馈也已接通并有截图**。
**但"好不好玩/跟不跟手/好不好看"没有任何主观验收**——投摆力度、地雷引信节奏、连锁引爆观感、
相机跟随、HUD 可用性全部未经人眼确认（见 §8）。

---

## 3. 新会话恢复顺序

1. 本文（恢复全局认知与环境铁律）
2. [待办事项.md](待办事项.md)（当前任务板与已完成归档）
3. 按任务按需读，**不要预加载**：
   - **要动坐标/相机/投掷/爆炸 → 必读 [`M2-3D空间模型对齐.md`](../设计/M2-3D空间模型对齐.md)**
     （XZ 竞技场口径 + "Godot 出结构 / Flash 出数值"的分工；不读极可能按旧的 2D 假设改错）
   - 要接着做 3D 化收尾 → [`M2-3D化-并行推进与交接.md`](归档/M2-3D化-并行推进与交接.md) 的第 1 节「当前状态」
   - 要写战斗逻辑（数值/公式） → [`参考游戏逆向-...-静态.md`](../技术/参考逆向/参考游戏逆向-海盗军团抢宝藏-静态.md) 对应章节
   - 要翻译某个 Godot 文件 → [`M2-Godot基准摘要.md`](../技术/参考逆向/M2-Godot基准摘要.md)（`.gd → .cs` 对照 + 空 TODO 清单）
   - 要写 Unity 代码 → [`M2-Unity参照库调研.md`](../技术/架构/M2-Unity参照库调研.md) 的 API 风险清单 R1-R9（**必读**，内有本机核实的版本陷阱）
   - 要加跨模块事件 → [`EventBus事件契约.md`](../技术/架构/EventBus事件契约.md)
   - 要动 shader → [`描边Shader调试.md`](../技术/渲染/描边Shader调试.md)

---

## 4. 环境铁律（踩过坑，不要重犯）

### 4.1 Unity 进程

- **batchmode 必须显式带 `-projectPath` 且带 `-nographics`**。缺 `-projectPath` 会打开 EditorPrefs 里的"最近工程"，
  可能污染/锁住别的项目——本项目曾因此产生**杀不死的僵尸进程**卡住 `Temp/UnityLockfile`，只能重启机器清理。
- **一次只跑一个 Unity 进程**（`Library/` 锁是独占资源）。多 agent 并行时，涉及 Unity 的验证由协调者串行执行。
- **跑完 PlayMode 测试后检查 `ProjectSettings/EditorSettings.asset`**：实测（2026-09-13）PlayMode 测试运行过程会把
  `m_EnterPlayModeOptionsEnabled` 从 `0` 改成 `1`（配 `m_EnterPlayModeOptions: 3` = 关掉域重载 + 场景重载）。
  这会**静默改变静态状态语义**（例如 `EventBus` 这类静态事件中心在两次 play 之间不再被重置），
  不是本项目想要的默认值 → 提交前用 `git checkout -- pirate-crew/ProjectSettings/EditorSettings.asset` 还原。
- **`core.autocrlf = true` 且仓库无 `.gitattributes`**：Unity 写出的 YAML 是 LF，git 入库按 CRLF 归一，
  于是每次 Unity 改写文件都会出现"整文件行尾变化"的噪音警告。已登记为待办（见 [待办事项.md](待办事项.md) 工程基建），
  本次未擅自改仓库级行尾策略。
- 本环境 **batchmode 没有可用的渲染路径**（不带 `-nographics` 会卡在 GfxDevice 创建），
  所以 `Camera.Render` / `ScreenCapture` **拿不到真实像素**——需要出图的验收必须用图形界面编辑器会话。

### 4.2 无头验证台（这是本项目最重要的效率杠杆）

`external/m2-harness/`（**不入库**，`external/` 已 gitignore）用 `dotnet` 直接引用 Unity 已编译程序集 + NuGet NUnit，
**不启动 Unity 就能编译工程源码并跑纯 C# 测试**，从而绕开 `Library/` 独占锁。

```bash
cd /f/VSCode/pirate-crew-3d-unity
mkdir -p external/harness-<你的域> && cp external/m2-harness/M2Harness.csproj external/harness-<你的域>/
cd external/harness-<你的域>
dotnet test  M2Harness.csproj -p:HarnessScope=Battle    # 跑测试
dotnet build M2Harness.csproj -p:HarnessScope=Runtime   # 只编译（快）
```

| `HarnessScope` | 编译范围 | 用途 |
| --- | --- | --- |
| `All` | `Scripts/**` + `Tests/*.cs` + `Tests/PlayMode/*.cs` | 全量体检（含 31 条第 4.3 节所说的必然失败项）。⚠ MSBuild 通配符**不递归子目录**，`Tests/PlayMode` 是显式补的一行；PlayMode 用例在本域**只编译**、不可在无头环境运行（2026-09-13 发现并修补） |
| `Data` | Core + `PirateCrew/Data` + `Tests/Data` | 数值层 |
| `Combat` | Core + `PirateCrew/Combat` + `Tests/Combat` | 战斗纯逻辑 |
| `Battle` | Core + Data + Combat + Battle + UI + `Tests/Battle` | 战斗组装层与弹体（**刻意排除 `Rendering/`**，避免并行 agent 互相干扰） |
| `Rendering` | Core + `PirateCrew/Rendering` | 描边 RendererFeature（需 URP 程序集） |
| `Runtime` | `Scripts/**` | 纯运行时编译检查 |
| `DataEditor` | `Scripts/**` + `Assets/Editor/**` | **进 Unity 之前抓编辑器脚本的编译错误** |

**能力边界（务必理解，别误判）**：`GameObject` / `MonoBehaviour` / `ScriptableObject` 的实例化走原生 `ECall`，
脱离 Unity 运行时必抛 `SecurityException`。实测 44 条 M1 EditMode 测试在该台里 13 通过 / 31 失败，失败全部因为
`new GameObject()`。**这不是回归，是固有边界。**

→ 因此本项目的核心逻辑**刻意分成两层**：纯 C# 静态类（可无头测，秒级迭代）+ MonoBehaviour 薄壳
（只拿引用、驱动、调纯函数）。**写新逻辑时请沿用这个分层**，否则会失去无头测试能力。

### 4.3 图像与观感

- **本会话模型可以消费图像**（2026-09-13 实测：`Read` 一张 PNG 能准确描述内容）。
  历史上"模型无法看图"的结论是错的，见 §9 的勘误。
- 编辑器打开时优先走 **MCP 的 `unity-mcp`**：`manage_camera` 的 `screenshot`
  （`capture_source` 选 `game_view`/`scene_view`、`include_image=true` 返回内联 PNG、`batch=surround/orbit` 多角度）、
  `manage_scene` 的 `scene_view_frame`。这样**不另开 Unity 进程、不抢锁**。
  - ⚠ **bridge 不会在首次打开工程时自启**（这是"编辑器开着但 MCP 报 `No Unity Editor instances found`"的根因，
    2026-09-13 实测查明）：
    - MCP for Unity v10 默认走 **stdio** 传输（ZCode 侧 `mcp-for-unity` 的 `--transport` 默认也是 stdio），
      编辑器侧必须监听本地 socket 才会被发现。
    - 包里负责启动的是 `StdioBridgeHost.StartAutoConnect()`，但 `StdioBridgeHost` 的**静态构造只在类被首次触达时执行**
      ——只打开工程、不打开 MCP 窗口时无人触达它。
    - `StdioBridgeReloadHandler` 的职责只是"domain reload **之后恢复**"（resume），不负责首次启动。
    - `HttpAutoStartHandler` 只管 HTTP 路径（`if (!useHttp) return;`），且要 `EditorPrefs` 的
      `MCPForUnity.AutoStartOnLoad` 为 true（默认关闭）——所以它救不了 stdio。
  - **本项目的处理（已生效并验证）**：`Assets/Editor/McpBridgeKick.cs`（`[InitializeOnLoad]`，反射调用
    `StdioBridgeHost` 以便包缺失时优雅降级）在编辑器加载时自动启动 bridge。
    菜单 `PirateCrew/MCP/启动 stdio Bridge` 可手动重触发；不需要了就删掉该文件（连同 `.meta`）。
  - **生效验证方式**（2026-09-13 实测通过）：`Assets/Editor/McpBridgeKick.cs.meta` 已生成
    + `netstat -ano | grep ":6400"` 显示 `LISTENING` 且 PID 等于 Unity 编辑器进程
    + `manage_scene get_active` 能返回场景。三者齐备即为正常。
  - **若报 no instances**（说明该脚本还没被编辑器导入）：让用户**点一下 Unity 窗口**
    （Auto Refresh 会在重获焦点时导入新脚本）或按 `Ctrl+R`，或打开 **`Window > MCP for Unity`** 手动启动。
    ⚠ 从外部无法强制编辑器刷新——包里**没有 FileSystemWatcher**，没有"投递命令"的通道，
    而唯一能提供进程间通信的东西恰好就是那个没起来的 bridge。所以这一步只能由用户在编辑器里完成。
  - 已实测可用的 MCP 工具（编辑器开着时）：`manage_scene`（get_active/get_hierarchy/load/save）、
    `read_console`、`manage_graphics`（`feature_list` 可列 URP Renderer Feature）、
    `manage_camera` 的 `screenshot`、`execute_menu_item`、`execute_code`、`manage_editor`（play/stop）、
    `run_tests`/`get_test_job`（**编辑器开着时跑 EditMode/PlayMode 测试的正确方式**，不必抢锁跑 batchmode）、
    `manage_gameobject`/`manage_asset` 等。
    - `manage_camera` 的 `output_folder` **要显式指定**（如 `export/outline-debug/_work`），
      否则默认往 `Assets/Screenshots/` 落图，会把临时截图混进 Unity 资产里。
    - `execute_code` 走 CodeDom（C# 6）：能用 `var`/字符串拼接，但别用 C# 7+ 语法；类型名写全限定名最稳。
    - 用 `execute_code` 直接调公开 API（如 `BattleController.SelectCharacter`）比模拟鼠标可靠得多。
  - 排查用的实测判据：项目下 `Assets/UnityMCP/Log/` 是否存在、注册表
    `HKCU\Software\Unity Technologies\Unity Editor 5.x` 下有无 MCP 相关键
    （本项目实测该日志目录从未生成，属正常——McpLog 走 Editor.log）。
    注意 8080 端口常被其它程序（如 Steam 的 steamwebhelper）占用，别误判成 MCP；
    本项目的 bridge 端口是 **6400**（`HttpEndpointUtility.DefaultPort`）。
  - 判断依据：编辑器是否真的打开了本工程，看 `Temp/UnityLockfile` 是否存在、时间戳何时更新
    （Unity 打开工程还会生成 `ProjectSettings/PackageManagerSettings.asset`、`URPProjectSettings.asset`）。
  - **锁被占时不要跑 batchmode**（会失败或抢占）；此时要验证只能用 MCP，或等用户关掉编辑器。
  - **礼貌约束**：进 play mode 采集画面会占住编辑器（该工程期间用户不能编辑），
    且会加载场景、覆盖用户当前打开的场景。做这类操作前先跟用户确认时机。
- 判断动态手感/动画节奏时，静帧不够——`Read` 支持 MP4，**录一小段视频再看**。
- 程序化测量的退路仍然有效（`external/tools/` 下的 `ocr.ps1`、`color_blocks.py`、`imgdiff.py`），
  适合回答"画面是否全黑""某区域出现什么颜色""改前改后差多少"，但回答不了"好不好看"。

### 4.4 改动 shader 的验证标准（血的教训，别用静态核对代替）

**唯一算数的验证是：图形界面编辑器进 play mode 后 `read_console` 里 0 条 shader error。**
静态核对 `#include` 文件是否存在、`grep` 符号是否定义，**只能证明"符号存在"**，证明不了：

- include 链**自洽**：`GlobalIllumination.hlsl` 里逐个符号都能 grep 到，但它自己的函数体用了
  `BRDF.hlsl` 的 `BRDFData` 而它并不 include `BRDF.hlsl` → 真实编译报 `unrecognized identifier 'BRDFData'`；
- 额外 Pass **会被执行**：URP 的 `DrawObjectsPass` 用固定 ShaderTagId 列表取 pass，**每个 tag 槽位只取一个 pass**。
  描边 Pass 若和本体 Pass 同标 `"LightMode"="UniversalForward"`，会被**静默丢弃**——
  材质正常、本体正常、Console 一点报错都没有，就是没有描边。要加额外 pass 就给它一个独立且未被占用的
  `LightMode`（本项目用 `"SRPDefaultUnlit"`）。

两次事故的完整复盘（含定位手法）在 [描边Shader调试.md](../技术/渲染/描边Shader调试.md) §八，改 shader 前值得先读一遍。

---

## 5. 门禁命令（改完代码后必跑）

```bash
U="F:/Unity/2022.3.62f1c1/Editor/Unity.exe"
P="F:/VSCode/pirate-crew-3d-unity/pirate-crew"

# 编译 + 导入（同时给新脚本生成 .meta）
"$U" -batchmode -nographics -quit -projectPath "$P" -logFile -

# EditMode 全量
"$U" -batchmode -nographics -projectPath "$P" -runTests -testPlatform EditMode \
  -testResults "F:/VSCode/pirate-crew-3d-unity/external/results-editmode.xml" -logFile -

# PlayMode 全量（把 EditMode 换 PlayMode）
```

**注意**：新脚本必须经过 Unity 导入才会生成 `.meta`（`.meta` 要入库，否则引用会漂）。手写 `.meta` 也可行
（字段结构与 Unity 生成的逐字段一致、GUID 为 32 位 hex 即可，Unity 会保留），但优先让 Unity 生成。

**关键验收技巧**：`BattleSmokeTests` 里有一条场景级用例，**未接线时 `Assert.Ignore`、接线后自动生效**。
所以看 PlayMode 结果的 `skipped` 是否归零，就能判断"场景是否真接线"——比只看有没有失败可靠得多。

---

## 6. 关键架构决策与理由（新会话不要重新论证）

记录"为什么"，避免推翻已定的东西：

1. **数值三层**：纯 C# `WeaponCatalog`/`CrewCatalog`/`LevelCatalog`（**真值来源**，可无头测）
   → ScriptableObject 定义类（引擎侧引用与策划调参）→ Editor 生成器 `M2DataAssetGenerator`（幂等产出 `.asset`）。
   理由：SO 实例化在无头环境必崩，但数值又必须可测；生成器里**不允许**出现第二份数值副本（避免真值分叉）。
2. **纯逻辑 / MonoBehaviour 分层**：见 §4.2。这不是风格偏好，是为了保住无头测试能力。
3. **程序集划分**：`PirateCrew.Gameplay`（玩法）/ `PirateCrew.Tests`（EditMode）/ `PirateCrew.PlayModeTests` /
   `PirateCrew.Rendering`（**独立**，因需引用 URP 程序集）。
   - asmdef 引用**只写 name 或只写 GUID，双写会 duplicate**（M1 踩过）。
   - **Unity 只对名称以 `UnityEngine.` 开头的程序集自动引用**（uGUI 的 `UnityEngine.UI` 因此可用）；
     Cinemachine 这类普通包程序集必须显式写进 `references`——`PirateCrew.Gameplay.asmdef` 已加 `"Cinemachine"`，
     所以相机代码用**强类型** `CinemachineVirtualCamera`，**不要退回反射**。
4. **跨模块通信**：只走 `Core/EventBus` 的字符串事件（snake_case），事件名与载荷必须登记在
   [EventBus事件契约.md](../技术/架构/EventBus事件契约.md)。禁止 `GameObject.Find`、禁止跨模块裸 `GetComponent`。
   同模块内调用、生命周期通知、每帧高频数据**不走** EventBus。
5. **3D 化的四个决策**（写代码注释里已交代，改前先读）：
   - px→单位固定 **1 瓦片 = 1 单位（32px）**，常量集中在 `LevelGeometry`
   - Flash 的 y 轴向下 → Unity y 向上，**显式取负翻转**；注意 `ExplosionResolver.deltaVy` 是 Flash 约定算出的
   - 重力/力度按 `Time.fixedDeltaTime = 1/25` + `Physics.gravity = -19.53125` 配平，使 Flash 的
     `vy += w` 与 PhysX 半隐式欧拉**逐步严格相等**——这是"预览 = 实弹"的根基（Godot 版预览≠实弹是已知教训）
   - 落水即死取**全局规则**（Flash §4.1/§4.3；Godot 版只在单关开启属漂移）
6. **AI 随机数**：自写 xorshift32 而**非** `System.Random`——后者的算法在 .NET 8（验证台）与 Unity Mono 之间
   不同，同 seed 会分叉，会导致确定性测试在两边结果不一致。
7. **AI 的一处显式增强**：Flash §6 打分不含武器伤害，无法满足"能命中时优先高伤害"，故补
   `DamageScoreWeight = 0.5`（设 0 可恢复纯原版，两种模式都有测试）。
8. **弹体程序化构建**：`WeaponProjectile` 默认用图元 + 颜色在运行时造，`projectilePrefab` 为可选覆盖。
   好处是既有场景无需重新装配就能工作（新增必填序列化字段会让场景变成"未接线"）。

---

## 7. 已知降级与 TODO（**不要当成已完成**）

- **描边与选中反馈已接线并出图核验**（2026-09-13 完成，不再是缺口）：
  `OutlineStateRules`（纯逻辑，5 条测试）+ `UnitOutlineBinder`（每帧把 `_OutlineState` 写进
  `MaterialPropertyBlock`，并按队伍写 `_BaseColor`）+ 单位材质 `PirateOutlineUnit.mat`
  + 悬停（`AimThrowController.UpdateHover`）/ 选中（`BattleTeam.Select/StartTurn/FinishTurn`）驱动状态位。
  7 张图在 [`docs/images/outline-debug/`](../images/outline-debug/)（含程序化判据与复现步骤）。
  - 期间修掉**两个只在真实 GPU 会话才暴露**的问题：① shader 只 include `GlobalIllumination.hlsl` 导致
    `unrecognized identifier 'BRDFData'`；② 描边 Pass 与本体 Pass 撞 `LightMode = "UniversalForward"`,
    被 URP **静默丢弃**（本体正常、就是没描边、Console 无报错）。复盘见
    [描边Shader调试.md](../技术/渲染/描边Shader调试.md) §八。
  - `OutlineRendererFeature`（全屏后处理描边）**代码保留但当前不参与渲染**：`maskLayer` 已从
    调试期的 `Everything` 收窄为 `0`（否则地面边缘也会被描成青虚线）。选中反馈由上面的 inverted hull 承担。
- **6 种特殊武器机制未实现**：`anchor` / `seagull` / `tidalWave` / `voodooDoll` / `cannon` / `SweepingFlame`。
  入口判定见 `ProjectileProfile.SupportsGenericProjectile`，`AimThrowController.SpawnWeapon` 会告警不静默。
- `rumBottle` → 2 个 `SweepingFlame` 的扩展点已留（`WeaponProjectile.OnPostDetonate`），未实现。
- **瓦片地形缺失**：`AiTerrain` 目前是"平坦地面 + 水平边界"近似，无瓦片、无侧墙反弹（原版可借墙弹）。
  场景提供"瓦片 → 地面高度"查询接口后替换 `AiController` 的 TODO 点即可。
- 近似实现（已在注释标 TODO）：Flash 的"每帧 `|vx| -= friction`"语义（现为 PhysicsMaterial 系数近似）；
  火药桶连锁的"AABB 最近点距离"（现为球形 `OverlapSphere` 近似）；
  `dynamite` 落水变 unlit 的精确状态；`boulder` 的"推到 x±32 并继承 vx"。
- `cannon` / `boulder` 的 AI 打分未做（§5.2 未给 cannon 的 dragRange 且它不在 AI 特判清单；
  boulder 碾压伤害与执行速度相关）——**留 TODO 不臆造**。
- HUD 小地图未做（§3.2 的小地图活动点；左下名册已做）。
- **描边的两处观感问题**（已出图，属美术取向，留验收决定）：
  ① 小单位上虚线偏碎——`_DashFrequency = 50`（Godot 默认）的虚线周期（1440p 下约 45px）
  与单位屏幕尺寸（约 27×36px）同量级，一圈只落得下 1~2 段；② 单位材质换成 `PirateOutline` 后
  **没有 ShadowCaster Pass**（单位不投影），本体光照也从 URP/Lit 变成 shader 内简单 Lambert。
  两条的改法与代价见 [描边Shader调试.md](../技术/渲染/描边Shader调试.md) §三末 / §七-2。

---

## 8. 需要人参与的事（无头会话做不到）

1. ~~描边逐层截图~~ → **2026-09-13 已完成**（见 §7 首条与 `docs/images/outline-debug/README.md`）。
   仍需人做的只是**主观判断**：那些图"好不好看"、虚线是否要更长、相机是否要拉近。
2. **手感与观感验收**（**当前最值得做的一件事**）：投摆力度、拖拽跟手程度、地雷引信节奏与 beep 帧序、
   火药桶连锁的范围与顺序、Cinemachine 跟随与 panToCharacter 是否跟手、HUD 按钮是否可点可用。
   建议录短视频而不是只截静帧（`Read` 支持 MP4）。
   - 已知可在验收里一并看的点：ROSTER 面板列了 9 行（`RosterRows = 12`），血条为 100/100；
     选中角色后底部武器面板才出现（§3.4，正确）；武器按钮 17 个 3×6 排布。
   - 相机当前正交尺寸 10 → 整关可见、单位偏小；这直接影响"看得清不清"的观感，值得一并判断。
3. 场景/预制体在 Inspector 里的实际外观（序列化是否正确、层级与材质是否如预期）。

> 这些做完之前，M2 只能说"机制接通"，**不能说"验收通过"**。

---

## 9. 勘误记录

- **「模型无法消费图像」是错的**（2026-09-13 实测推翻）。此前据此搭了 WinRT OCR + 色块 + 差分三件套做
  Flash 逆向的"盲操作"。那些工具的**结论仍然有效**（关键结论都用静态 `PlaceObject` 表交叉验证过，
  如 `1p_button` 实际在舞台 (163,263)），但"因为看不见只好盲操作"这个**前提是错的**，
  `docs/技术/参考逆向/参考游戏逆向-海盗军团抢宝藏-动态.md` 已就地加勘误说明。
- 后续判断图像相关能力时，**先实测再下结论**，不要沿用旧会话的能力假设。

---

## 10. 关键提交索引

| 提交 | 内容 |
| --- | --- |
| `72225ef` | M2 描边接线打通选中/悬停反馈（`OutlineStateRules` + `UnitOutlineBinder` + 材质/预制体 + 两处 shader 真实事故修复） |
| `0c7e6cb` | 描边文档实测化（逐档判据 / §八 事故复盘 / 出图路线）+ 7 张图入库 + AGENTS 规则补强 |
| `bcc6241` `3e9b5e8` `c098046` | M2 数值层 + 资产生成器 + 48 个 `.asset` 落地 |
| `b0a0cef` | M2 战斗纯逻辑（弹道/爆炸/回合/计分/武器触发，86 测试） |
| `0d72678` | M2 选中描边（inverted hull + 全屏后处理 + `debug_mode` 分层） |
| `4e3a814` `22a565a` | M2 参照库调研（557 行）+ Godot 基准摘要（387 行） |
| `6cd9e93` | M2 战斗组装层（纯逻辑接 Unity 世界，57 测试） |
| `cb27ebc` `5713523` | Battle 场景装配脚本 + UGUI HUD + 描边 Feature 挂载 + 场景/预制体落地 |
| `8596f01` | 修 HUD 集成 bug（`AimThrowController` 缺 UI 命中拦截，导致 HUD 按钮点不动） |
| `f34915a` | M2 敌方 AI 评估器（按逆向 §6 自建） |
| `2cb27b2` | M2 武器弹体运行时——打通伤害闭环（133 测试，EditMode 305/305） |
| `011962f` | README 校正 + EventBus 事件契约登记表 + AGENTS 借鉴 stick-world |
| `7ae6ce4` `bd839b4` `49ae7da` `389921d` | 各阶段文档归档与环境经验落档 |

---

## 11. 下一步（M3 入口）

M3 = 船员管理循环：`Roster` / `Progression`（+NUnit）→ `CampaignManager` + 关卡选择 UI →
招募 → 编成 → 出战 → 结算打通 → 进度/名册接 `SaveManager`（M1 已就绪）。

动手前先读 `../game-3/pirate-crew-3d/modules/{crew_management,campaign}/`（各 3-4 个 `.gd`，是骨架）
与 `../game-3/docs/gdd.md`，并按 AGENTS 的「参照库强制」先找开源 Unity 参照。

若要先收尾 M2：优先做 §8 的两件（需要图形界面会话），或按 §7 清单逐项补。

### 11.1 描边接线 + 出图（**2026-09-13 已完成**，路线留档供同类任务复用）

产物：`OutlineStateRules.cs` / `UnitOutlineBinder.cs` / `OutlineStateRulesTests.cs`（5 测试）、
`M2BattleSceneSetup.EnsureOutlineMaterial()`（生成 `PirateOutlineUnit.mat`）、
`AimThrowController.UpdateHover()`、`BattleTeam.Select/StartTurn/FinishTurn` 同步 `SetSelected`、
`docs/images/outline-debug/` 7 张图 + README。

**可复用的"编辑器开着时出图"路线**（整条用 MCP 走完，不另开 Unity 进程、不抢 `Library/` 锁）：

1. 改代码/资产 → `refresh_unity`（`scope=scripts` 或 `assets`、`compile=request`、`wait_for_ready=true`）。
   注意：domain reload 会掐断 bridge，等 ~10s 后 `netstat -ano | grep ":6400"` 会看到重新 LISTENING，再继续。
2. `read_console`（`types=["error","warning"]`）确认 0 编译错误；**shader 改动必须等进 play mode 后**再看一次（见 §4.4）。
3. `manage_scene` 的 `load` 打开 Battle 场景（或直接 `manage_editor play`，Battle 是当前场景时不必 load）。
4. `manage_editor`（`play`）进 play mode；等 6~8 秒让 `BattleController.Awake` 生成单位。
5. 需要驱动游戏逻辑时用 `execute_code`（可直接调 `BattleController.SelectCharacter` 等公开 API——
   比模拟鼠标可靠）；取图用 `manage_camera` 的 `screenshot`（`include_image=true` 直接返回内联 PNG，能当场判断）。
6. 逐档采集跑菜单 `execute_menu_item("PirateCrew/Rendering/采集描边调试截图")`。
7. 收尾 `manage_editor`（`stop`）——**用完就把编辑器还给用户**。
8. 判图用程序化判据（按色相扫像素），不要只靠"看着像"；`external/tools/` 下的 `imgdiff.py`/`color_blocks.py` 可复用。

**这条路线上的三个已踩过的坑**（写进代码注释与 `docs/技术/渲染/描边Shader调试.md` §八）：
① 编辑器**暂停**时画面不重绘，"等帧"逻辑会挂死 → 采集脚本现在检测暂停并明确报错；
② `ScreenCapture.CaptureScreenshot` 是**异步**的，请求后不能立刻复位改档状态（曾把最后一档拍成档 0）；
③ `manage_camera` 的默认 `output_folder` 会往 `Assets/Screenshots/` 落图（不是 `export/`），需要显式指定
   `output_folder`，否则会把临时截图混进 Unity 资产里。

---

## 12. 美术写实化迭代交接（2026-09-13 深夜，上下文压缩前）

### 当前状态一句话
M2/M3 功能全部完成且门禁全绿（EditMode 908/908、PlayMode 7/7、ArtGate 11/11）；**美术方向已由"风格化低模+描边"推翻为"写实 PBR"**（用户拍板），平台化关卡/写实渲染基座已入库，**视觉迭代闭环已打通**（播放器自截图→subagent 看图→工单→修复→重拍）。

### 视觉闭环怎么跑（无人值守）
```bash
# 1) 烘资产与场景（编辑器必须关闭）
Unity.exe -batchmode -nographics -quit -projectPath .../pirate-crew -executeMethod PirateCrew.EditorTools.ArtGate.BuildAll -logFile -
# 2) 构建播放器
... -buildWindows64Player "F:/VSCode/pirate-crew-3d-unity/external/build/PirateCrew3D.exe" ...
# 3) 自动出图（7 机位 PNG → export/art-review/<轮次>/，自动退出）
external/build/PirateCrew3D.exe -artReviewOut "F:/VSCode/pirate-crew-3d-unity/export/art-review/r<N>"
# 4) 派 general-purpose subagent 逐张 Read PNG 看图诊断（本会话主模型看不了图，subagent 能看）
```
教训：PlayerArtCapture 必须等 Bootstrapper 进 MainMenu 后再切 Battle（r1 竞态全废片）；采集失败要查 `%LOCALAPPDATA%/../LocalLow/BoranFan/Pirate Crew 3D/Player.log`。

### 进行中（压缩时未交回）
- r2 出图诊断 agent（export/art-review/r2/ 共 7 张，2.2MB 级真实内容）→ 回来后按其工单修复再出 r3
- 阳光感打光调研 agent（写 docs/技术/渲染/阳光感打光调研.md）→ 与诊断工单合并落地

### 已定关键决策（不要重新论证）
- 写实 PBR：Linear 色空间、三灯 Gradient 环境、Neutral+Bloom0.85/0.55、场景描边退役（单位选中描边保留）、主光 1.35@Euler(48,140)
- 关卡=悬空平台簇（4 簇：西梯田岛/中央大帆船/东空岛/北小艇岛，水距 2-4 格，落水即死，爆炸逐格摧毁=差异化卖点）；kit 构件拼装（SceneKitCatalog）
- 关卡节奏按原版（武器前 5 关发完+地形变奏），不按 GDD"每 3 关新机制"
- 相机自由微操默认开（右键环绕+滚轮缩放，零输入守 45°/18 断言）
- 中英口碑结论：修 AI 必中感/难度曲线（L12 墙）/AI 出手节奏；瞄准线是口碑资产
- 流体库不接入（FLIP/HPWater 均只借思路）；沙堡借"湿沙带/泡沫/沙纹"不借地形物理

### 待办队列（按序）
1. 收 r2 诊断+阳光调研 → 修复工单（预计：天空灰暗/主光提亮/按钮对比度/地平线硬切/陈设密度）
2. 材质库逐材质 PBR 配方+做旧（参照 game-2 building-pipeline-v2 的 64 材质思路，见 docs 美术风格指南+品控规程）
3. 真实草贴片+风、水透明观感调参、相机俯角上限（用户：不见太阳也要阳光感——靠光质不靠太阳入画）
4. M3 收尾与 HUD 验收、settings 真接线、用户拍板项：仓库 LICENSE、关卡数 33vs18 核对
5. 每轮改动后：ArtGate→播放器→出图→看图，不合格不出库

### 用户最新原话基准
"不用看到太阳和地平线也能阳光明媚""你上网查查不就得了"——阳光感=光质问题，调研结论落地时以实拍图验收。

## 13. 美术迭代 r2→r5 交接（2026-09-14 凌晨，上下文压缩前）

### 视觉闭环跑法（同 §12，补充r4后版本）
```bash
# 1) ArtGate（13 步，含 ⓪ shader 入构建 + ⓪.5 噪声贴图）
"F:/Unity/2022.3.62f1c1/Editor/Unity.exe" -batchmode -nographics -quit -projectPath "F:/VSCode/pirate-crew-3d-unity/pirate-crew" -executeMethod PirateCrew.EditorTools.ArtGate.BuildAll -logFile -
# 2) 播放器（真实 shader 编译门禁：构建日志 grep "shader error" 必须 0）
... -buildWindows64Player "F:/VSCode/pirate-crew-3d-unity/external/build/PirateCrew3D.exe" ...
# 3) 出图 8 张 → 4) 派 subagent 七视角+像素判据评审（洋红/描边/std/对比度/WCAG 全可程序化）
external/build/PirateCrew3D.exe -artReviewOut "F:/VSCode/pirate-crew-3d-unity/export/art-review/r<N>"
# 5) 压缩 jpg+诊断报告.md 归档入库（.gitignore 已排除原始 png）
```

### 两波洋红根因（都已修复，教训入档）
- 第一波（r2）：运行时 Shader.Find 的自定义 shader 无材质资产引用 → 播放器剥离。修法：ArtGate ⓪ 步写 GraphicsSettings 的 **m_AlwaysIncludedShaders**（注意 m_PreloadedShaders 只收 ShaderVariantCollection 收不了 Shader）。
- 第二波（r3）：**创建图元/网格后不显式绑材质** → renderer 落到播放器构建里缺席的 URP 默认材质 → 粉色。两处：FxSpriteFx.Create（材质建了没绑回 renderer）；BattleTerrainView.ApplyBuffers（isShell:false 把 underside 材质误写 lowZone）。修法：材质按目标 renderer 显式路由+空材质宁可隐藏。
- **共性铁律：任何 CreatePrimitive/new MeshRenderer 后不赋材质的路径，在播放器构建里都是粉色炸弹。**

### r4 结论与 r5 在途
r4 兑现：洋红 8/8 零像素、接触阴影 45% 压暗、爆炸有火光、HUD 顶栏/名册/提示条对比全过。
r4 未过（r5 五 agent 并行修复中）：
- W-A 水：水体 L50 48→90-110、高光青白→暖金(40-60°)、空心椭圆斑→15-30px 实心碎斑、低角光路
- W-B 岸线：簇级泡沫环开缺+宽度扰动+湿沙暗带（r4 实测灰白带 r3→r4 纹丝未动）
- W-C 沙：噪声采样尺度放大 8-12 倍（0.35→0.04-0.06）、对比 ±12-18%、砖缝去饱和蓝灰
- W-D 描边：r3 862→r4 311px 回归根因（疑部件材质无 _OutlineState 属性/显式数组不全）+脚下刀片碎片+三角帽
- W-E HUD/FX：移动 2.83/操作 1.82 对比度+层级反转、小地图岛矩形→侵蚀不规则、爆炸亮度砍 30-40%、名册队伍色
**r5 收口顺序：五 agent 回收→harness 验证→域提交→ArtGate→播放器→r5 出图→评审。**

### 已定裁决（追加）
- 名册字号 24（Q-16 实现侧落地值；20 实测字形带 18px 不达判据）
- battle-45 保持忠实出厂机位（玩家视角证据）；全员入画需求若再提，另加宽景机位不折中
- 小地图静态烘焙（地形炸毁不更新/跨关不刷新）为已知取舍，彻底修需动 MinimapRules/BattleMinimap
- F2 噪声三族 smoothness 0.25/0.40/0.55；贴图均值保持乘性图（材质平均色零漂移）
- 阳光感=光质四件套：暖色高光+水面镜面波光+暗部透气+地平线不冲白（调研 §1）

### 登记待办（r5 之后）
1. Scene_*.mat 族（SceneArt 装饰网格用 PirateOutline 单色 _BaseColor）过不了 P-9/P-10——需给 PirateOutline 加噪声采样或换 Surface 材质
2. 三角帽完整建模、远距阵营肩章/领口对比
3. 木栈桥入水立柱、悬空白帆补船体
4. 相机 18→15 为提案待用户验收；描边 _DashFrequency 点划带假阴性（1080p 34px ON/OFF 条带）
5. 用户拍板项不变：仓库 LICENSE、关卡数 33vs18

## 14. r6 结论与 R7 在途（2026-09-14，上下文再压缩前）

### r6 评审裁决：未达收口门槛
兑现四项：泡沫纸板消失（竞技场级三环带删除，SceneArtBuilder）、蓝缝治好（竖直面暖偏置，B-R +12→暖中性）、描边部件全覆盖（宽度 0.006→0.010 是真根因：Bandana 与躯干同材质 guid，非材质/收集问题）、三角帽观感改善+肉瘤删除。
未动两项（R7 在修）：
1. **水"回蓝"没发生在渲染上**：基础色已蓝但被镜射瓣暖金盖住（_SunSpecBroadStrength 1.6/_SunSheenStrength 0.20 没动——r6 改错了杠杆）；掠射亮部 hue 与 r5 逐像素相同、水窗中性灰 20-56%。
2. **爆炸过曝反升**（火球框 V>0.99 68%）：白的主体是**水面金色冲刷**（爆炸机位朝太阳方位）非粒子本身；粒子亮度连砍两轮无效印证此判断。注意：协调者预验曾用"全通道>252"口径得出 0.9% 是**错误口径**（正确口径 V>0.99=max 通道>252，全图 5.9%）。
另修中：三角帽结构（帽宽/头高 1.8→≤1.35、檐上翻 42-48° 抬过冠顶、檐尖收短）、名册队色名对比（红 3.76/蓝 2.80→≥4.5）。

### R7 在途（最终窄修轮）
文件域：PirateWater.shader + WaterAssetBuilder（镜射权重 Broad 1.6→0.8/Wave 0.65→0.4/Sheen 0.20→0.10/窄瓣 8→5——暖金只留紧贴镜面角碎点）、FxMaterials（core ×0.7/fire ×0.8 第三轮累计）、CrewVisualPrefabBuilder（檐半径 ×1.05-1.10 头半径/翻角 42-48°/冠 ×0.34/檐尖收短）、BattleHud（队色名 #FF8A7A/#7FB0FF 系写死+WCAG 注释）。
**收口序列：R7 回收→harness→提交→ArtGate→播放器（grep shader error 必须 0，r6 曾抓到 breakup 变量重定义）→r7 出图→终审（判据：水窗蓝>60%/中性灰<15%/暖亮 2-6%；全图 V>0.99≤2%；橙壳≥2000px；帽宽/头高≤1.35）→归档+交用户验收。**

### 教训追加（渲染调参）
- 调色先找**主导通道**：掠射水面的观感由镜射/sheen 权重决定，基础色改了白改；
- 爆炸过曝先查**环境冲刷**（水面金色镜射）再砍粒子；
- 像素判据口径要钉死：V>0.99 = max 通道>252（不是全通道）。

## 15. r7 终审通过——美术迭代收口（2026-09-14）

**终审裁决：可交用户验收。** 四红线全清（洋红 8/8=0、全中文、HUD 对比全 ≥4.5、单位 ≥25px）；水窗蓝 73-92% 暖金只留镜射碎点；描边全部件覆盖。爆炸 blown 5.3% 不阻断（63%=水面镜射道即目标效果、真死白口径 1.74%<2%、战斗实际机位 0.02%）。
**用户最可能打回点（已交底）**：explosion-moment 无橙火球壳（D-1）——需 bloom 分层/粒子色相设计（新轮次），不是第四次压粒子。
r8+ 待办（详见 docs/images/art-review/r7/诊断报告.md D-1~D-8）：爆炸橙壳、水中性灰口径重定、岸线泡沫抖动、帽宽收量、battle-45 镜像 ShowHud 一行修、Q-20 虚线、红衣体积、近景波形。
本波全部提交：32ca045→6332fc5（含 r3~r7 五轮视觉迭代 + 两波洋红根因修复 + 交接 §12-§15）。

## 16. 额度中断抢救与交接（2026-09-14，新会话从这里继续）

### 事件与现状
周额度耗尽（重置 2026-09-19），S1/J1/J2 三 agent 阵亡但产物已抢救：编译 0 错误、ArtGate 13 步全绿、播放器 0 shader error、r10 出图 0 异常（export/art-review/r10 与 r10-l9，未归档）。**工作树含未提交的三大块**（本节随同提交）：
1. S1 格子×2 扫荡（完成；欠账=83 条钉旧常量测试断言，如 WorldGravity 19.53/ZoomBounds 3-25/WoodenCrate 间距——逐条按 ×2 新值更新即可）；
2. J1 音效（game-2 wav 已入 Resources：SfxThrowWhoosh/SfxCrewDown/SfxFleshHit/SfxExplosion/SfxTurnStart/SfxUiClick/SfxUiPanelOpen/SfxWoodCrack/MusicVictory/DefeatJingle/AmbientSeagullCry×3 + AudioService/SfxCatalog/SynthRenderer 改动；欠账=事件接线完整性未验证+变奏系统未做）；
3. J2 超美空岛（Showcase/ 4 文件，含台基/断拱/倒塌柱等遗迹结构；死在收尾，协调者补了 DaisTop 修到编译过；**未进场景未出图**）。

### 用户新报两个视觉 bug（r10 实测，修复规格已定）
**A. 岛顶正方形+面错乱**：地形壳按格子渲方块顶+侧壁+底锥（T1 停用了侵蚀），kit 再叠加=穿插。修法：岛簇顶面轮廓圆润化收边（恢复 V4 侵蚀、出生格保护不变）；**船簇格子的地形壳只渲平甲板**（侧壁/底锥剥离，交给 ShipHullGeometry 放样船体）。
**B. 船没底+搁浅**：放样船底被地形方壳包住+船簇基准高度低于吃深。修法：同 A 剥壳；船簇基准高度抬高至龙骨离水面 ≥1 单位（改 BuildFromTileMap 的船簇高度档）。

### 新会话任务队列（用户已换 Subagent 服务商）
1. **超美空岛让新 Agent 接手**（用户原话指令）：读 Showcase/ 四文件理解 J2 的设计意图，补完（Editor 菜单摆进场景→ArtGate→出图给用户验货）；用户要求"零提示词约束，它的超级美观就是标准"。
2. 修 bug A/B（规格见上）。
3. 83 条测试断言 ×2 更新（机械）。
4. juice 批次（用户已批准全做）：hit-stop/屏震分级/受击三连→回合扫镜+开局定场→投掷三拍/落水波纹→UI 动效（详细方案在 2026-09-14 会话尾部的六层计划）。
5. 音效接线核验+pitch/vol 变奏。
6. 全收后 ArtGate→r11 出图贴对话。

### 流程铁律（用户 2026-09-14 定，新会话必须遵守）
重要内容协调者亲自写亲自把关，或先做一个优秀案例给用户过目后才能批量；截图必须直接贴对话；2D→3D 灵魂译法四锚点+想象力条款（见 §M2-3D空间模型对齐 尾部）；样板关先行。

## 17. 测试欠账清账 + HUD 动效收口 + 相机俯角 bug（2026-09-14 下午）

### 摸底结论（接手 §16 队列时先知道）
工作树干净、**v0.1.0 已发行**（`4b52022`）。§16 队列大部分已被后续会话完成：样板三关落地（`23d3d4d`，含天空之岛）、
模式系统（`38023c8`，移动/操作/观察三模式+用户裁决的相机特写档）、r11/r12 出图与实玩反馈修复、README 重做（`35a3da7`）。
§16 余下的真实欠账只有三件：**过期测试断言、UI 动效（juice 最后一块）、UI 音效接线**——本节全部清账。

### 55 条过期断言清账（`8dd88b0`，两波并行 agent 按互斥文件域修）
- 根因单一：**`LevelGeometry.TileWorldSize` 1→2**（格子 ×2 扫荡）→ `PixelsPerUnit` 32→16、`FlashSpeedScale` 1.5625、
  重力 −39.0625、水面 −0.4、AABB/初速/间距全翻倍；另有相机用户裁决新机位（特写 12/30°、全场 30/45°、缩放 6-50）、
  样板三关重写（SceneArt 格号↔世界坐标混用）、UiStrings 按键名白名单（r12 提示词有意含 AD/WS/Space/Shift）、
  BedPad 音源迁移登记+SHA256 核对。
- 区分口径：Unity EditMode 实测 **55 失败→0**（1021 总）；harness All 86→31——**余 31 条 = §4.2 登记的无头环境
  固有失败**（Save/SceneLoader 族，需 `persistentDataPath`/`SceneManager`），不是回归。
- 随手清了 8 处 ×2 注释漂移与场景文档草丛区间（`62f25b0`；level_1 草丛实发 ~222，船面禁草）。

### HUD 动效 + UI 音效（`f460b70`，juice 六层计划至此全落地）
- `UiMotionRules`（纯 C# 无头测，数值全标提案/待定）+ `UiMotion`（**unscaled 时间**驱动——hit-stop 顿帧时
  UI 反馈不停；同目标后到取消）+ `BattleHud` 接线：武器面板滑入 0.16s/淡出 0.10s（状态翻转才播，防刷新重播）、
  回合横幅 punch、受击行标签弹跳、血条指数滚动、按钮 punch。
- UI 音效落点：UiClick（按钮）/UiPanelOpen（面板出现）/UiError（选枪失败）——`Game2AudioAssets` 里
  「UI 层未接线」的欠账清账；音效接线审计确认已被现有测试全覆盖（`Mapper_EveryWeaponMapsToARegisteredSfx` 等），未重写。

### 相机俯角真 bug（`704bf28`，PlayMode 门禁的功劳）
`EnterCloseUpView` 只切距离不切俯角，`_dragPitchDegrees` 残留烘焙 45°——**特写档自上线起从未真正进入 30°**。
旧 PlayMode 断言钉 15 在第一处就失败短路，修 ×2 断言后这条才暴露。已补 `_dragPitchDegrees = CloseUpPitchDegrees`。
另：`Game2AudioPortTests.FindProjectRoot` 在 Unity 侧要**反射**取 `Application.dataPath`——CoreCLR 在 JIT 编译期
解析 icall 失败时 `try/catch` 拦不住（harness All 实测炸 3 条 SecurityException 的教训）。

### 门禁实测（2026-09-14，全部可复跑）
- **EditMode 1021/1021 全绿，skipped=0**；**PlayMode 7/7**；harness All 1021 总/990 过/31 环境固有失败。
- 新 UI 动效为运行时表现，静帧出图只能验面板/横幅状态；**手感（滑入速度/punch 幅度/血条节奏）待用户实玩**，
  全部常量集中在 `UiMotionRules` 调参即可。

### 遗留观察项
- **BedPad 音源残本**只在 game-2 的 `.temp/_restore_morning` 快照（规范树已无此文件）；残本消失时
  `Port_SourcesExistInGame2Checkout` 会报**真实**失败——届时改指向本工程 Resources 副本（已 SHA256 验证一致）。
- batchmode 跑测试会把 TMP 字体图集跑膨胀（`LXGWWenKaiLite-*.asset` 的 `_typelessdata` 变化）——属运行噪音，
  提交前 `git checkout` 还原（与 EditorSettings.asset 同一处置）。

---

## 18. v1.0.0 发布收口 + Blender 管线立项（2026-09-16）

### 本轮做了什么（全部已过门禁）

- **设置面板真接线**：`MainMenuController` 接 `AudioService.SetVolume`（四路音量滑条实时生效）+
  `VideoSettingsService.SetFullscreen/SetQuality`（画质=换 `GraphicsSettings.renderPipelineAsset` 到
  PC_Performant/PC_Balanced，全屏=`Screen.fullScreen` 仅播放器生效）；关面板统一落盘（设置槽 9）。
  **音频/视频两域同槽共存靠读改写**（`AudioSettingsStore.TrySaveFrom` 也已从整槽替换改为读改写——
  旧实现会把对方的键抹掉，有契约测试锁定：`Tests/Settings/VideoSettingsStoreTests.cs`）。
- **战斗暂停**：`PirateCrew/Battle/BattlePause.cs`（静态状态 + timeScale）。**为什么不能只靠 timeScale=0**：
  TurnManager 的 inactivity、AiController 的看门狗/延迟都是纯 Update 帧计数，timeScale 冻不住——
  所以 TurnManager/AiController/BattleCameraController 的 Update 入口都查 `BattlePause.IsPaused`；
  顿帧（hit-stop）与暂停都改 timeScale，互斥处理见 BattleCameraController.RestoreTimeScale。
  `BattleHud.OnDisable` 兜底 ForceResume（暂停中退场绝不能把 timeScale=0 带回主菜单）。
- **再来一局**：`SceneLoader.ChangeScene(…, replaceTop)` + EventBus `change_scene` 字典载荷新键
  `"replaceTop"`（契约已登记）——Battle→Battle 重载不堆栈，`go_back` 仍回选关/主菜单。
  战役局走 `CampaignApi.SelectLevel(id, replaceTopScene:true)`（保留待结算归属）。
- **结算画面**：BattleHud.OnMatchFinished 弹全屏面板（胜负大字/得分/三星图形/战役局追加
  关卡/星级/经验/新招募/首次通关行 + VictoryJingle/DefeatJingle）。战役局判定 =
  BattleStarted 时 `CampaignApi.PendingLevelId != null`（依赖 CampaignApi 先订阅的时序，已注释）。
- **返回确认**：战斗 HUD 返回按钮/暂停面板共用确认弹窗（UiStrings.BackConfirm）；主菜单退出也有确认框。
- **日志收口**：`Core/Log`（[Conditional] UNITY_EDITOR|DEVELOPMENT_BUILD），全 Scripts 44 处
  `Debug.Log/LogWarning` 机械替换为全限定 `global::PirateCrew.Core.Log.*`（注意：`PirateCrew.*`
  命名空间里非限定 `PirateCrew.Core` 会解析到子命名空间，必须 global::）；LogError 直通保留。
- **遗留清理**：`BattlePlaceholder.cs` 删除（GUID 无场景引用）；`SceneSetup.BuildBattleScene` 死方法删除；
  `M3Scenes` 常量并入 `Core/SceneNames`（5 场景登记）；`SceneSetup.RegisterBuildSettings` 修为 5 场景
  （此前重跑 M1 装配会把 CrewManagement/LevelSelect 踢出 Build Settings）。
- **发布资产**：`Editor/ReleaseGate.cs`（菜单 PirateCrew/Release/应用图标与版本，无头可跑）——程序化画
  1024 图标（骷髅+交叉骨+浪带+黄铜环）赋全部 Standalone 图标槽 + bundleVersion 1.0.0；
  仓库根新增 MIT LICENSE。**图标坐标铁律**：SetPixels32 数组行 0=贴图底部，EncodeToPNG 导出即顶行在上——
  直接按行号画，再手工翻转一次就是上下颠倒（踩过）。
- **音量出厂默认**：Ambient 0.8→0.5、Music 0.8→0.7（用户反馈"背景音乐有点大"；按类别常量，非全局一个值）。

### 门禁实测（2026-09-16，全部可复跑）

- EditMode **1027 总 / 1026 过 / 0 失败 / 1 skipped**——新增 `Tests/Settings/`（6 条）；那 1 条 skipped =
  `Port_SourcesExistInGame2Checkout`（game-2 的 `.temp` 素材目录已被清理，测试按设计 Ignore；wav 已随本工程
  提交，非回归）。PlayMode **7/7 skipped=0**。ArtGate 13 步全绿。播放器构建 0 shader error，
  r14 出图（`export/art-review/r14/`，4 jpg + 报告）无洋红、无回归，Player.log 0 异常。

### Blender 管线（用户 2026-09-16 拍板，subagent 执行中）

- **本机 Blender**：Steam 版 5.2.1 LTS，`F:/SteamLibrary/steamapps/common/Blender/blender.exe`
  （winget 显示 Unknown 版本但可用；`F:/MyMy/Blender` 是用户的 .blend 素材目录不是安装目录）。
- **game-2 管线参照**：`../game-2/tools/blender_buildings/`（无头 `-b --factory-startup -P xxx.py` 模式，
  文档在 `../game-2/docs/技术/架构/建筑管线/`）。
- **Agent B（图标）已交付并验收通过**：`tools/blender/icon/render_icon.py`（Cycles 96 采样，
  判色程序化校准三轮），产出 `Assets/Art/Textures/AppIcon.png`（1024²，.meta 未动）；小尺寸可读性
  实测 48px 清晰 / 16px 可辨。程序化图标（`ReleaseGate.DrawIcon`）保留为回退。
- **Agent C（船员建模试点）已撤销 + 范围澄清**：用户 2026-09-17 先重申**角色模型不许改**
  （低多边形就是定案风格，船员试点产物已删净、游戏内角色从未被改动），随后澄清
  **"Blender 建模"指的是场景资产**——岛台/船体/栈桥/陈设走 Blender 无头管线是用户明确要求的主线。
  分工铁律已写入 AGENTS.md「注意事项」。场景套件样板：大帆船（含白帆）+ 木栈桥（入水立柱），
  正是 r8+ 验收清单登记过的缺口；showcase 过审后才接入战斗场景。
- Agent C 期间沉淀的Unity API 实测教训（留档供同类任务）：`ModelImporter.importMaterials` 已移除
  （用 `materialImportMode=None`）；`useFileScale=true` 会把 Blender FBX 的 cm 因子（×0.01）乘进来
  导致 1.9 米变 0.019；batchmode 下 `File.Exists` 相对路径按进程工作目录解析、场景存在性要用
  `AssetDatabase.LoadAssetAtPath<SceneAsset>` 判、`SaveScene` 必须显式给路径；
  **本机 batchmode 下 `EditorBuildSettings.scenes` 的 API 增量/全量修复均不落盘**（内存复核通过、
  文件纹丝不动），只能直接改 `ProjectSettings/EditorBuildSettings.asset` 的 YAML。

---

## 19. Blender 场景套件——**新对话从这里继续**（2026-09-17）

### 现状一句话
场景资产样板（大帆船 + 木栈桥）**已建模、已入库、C# 装配脚本已写好且编译 0 错误，但 Unity 侧从未运行**——
showcase 场景还没生成、没接线、没出过 Unity 实拍图。用户明确：资产留下，正式干放到新对话。

### 交付物清单（全部已提交）
- `tools/blender/scene/build_scene_kit.py`（无头建模，幂等；参数区 48-86 行）+ `README.md`（口径表/调参行号）
- `pirate-crew/Assets/Art/Models/SceneKit/Flagship.fbx`（3701 三角面，15.85×6.0×7.74，5 槽 Kit_*，原点=船长中点水线）
  与 `Dock.fbx`（3404 面，6×2.05×1.6，3 槽，原点=桥面中心）
- `pirate-crew/Assets/Editor/SceneKitPilot/SceneKitPilotSetup.cs` —— 入口 `PirateCrew.EditorTools.SceneKitPilotSetup.BuildAll`
  （导入参数已按实测坑写死：materialImportMode=None / useFileScale=false / bakeAxisConversion=true）
- `pirate-crew/Assets/Scripts/PirateCrew/SceneKitPilot/SceneKitPilotCapture.cs` —— 播放器出图：`-sceneKitOut <绝对目录>`，
  6 张 1280×720（01-pano / 02-ship-front34 / 03-ship-side / 04-ship-back34 / 05-dock-from-shore / 06-dock-waterline）
- `docs/images/scene-kit-pilot/` —— Blender 侧 6 张预览（风格验收用）

### 新对话作业序列（照抄即可）
1. `"F:/Unity/2022.3.62f1c1/Editor/Unity.exe" -batchmode -nographics -quit -projectPath .../pirate-crew
   -executeMethod PirateCrew.EditorTools.SceneKitPilotSetup.BuildAll -logFile -` → 生成 `Assets/Scenes/SceneKitPilot.unity`
   （水面 y=-0.4 + 沙岸 + 船/桥落位 + 暖阳双相机）。
2. **手工**把该场景追加进 `ProjectSettings/EditorBuildSettings.asset` YAML（本机 batchmode 下
   `EditorBuildSettings.scenes` API 不落盘，§18 有完整记录；场景 .meta 的 GUID 以实际生成为准）。
3. `-buildWindows64Player` 重构播放器（grep "shader error" 必须 0）。
4. `external/build/PirateCrew3D.exe -sceneKitOut <目录>` 出 6 张实拍 → Read 看图验收
   （判据：不躺倒/不 0.01 倍缩放/无粉色/队色无关、船吃水与栈桥立柱插水正确）。
5. 用户过审后才做**接线**（替代 `ShipHullGeometry` 观感壳、栈桥进 SceneKitCatalog），瓦片平甲板与
   内置甲板面 z-fight 时把瓦片抬高 ≤0.05。接线属于改既有文件，按 AGENTS 建模分工铁律执行。

### 分工铁律（重复一遍，防新会话再搞反）
角色模型不许改（低模定案）；**场景/环境资产走 Blender 是用户明确要求的主线**；详见 AGENTS.md「注意事项」。

---

## 20. M4 大海域世界化第一批——**新对话从这里继续**（2026-09-17）

### 用户裁决（2026-09-17 口述，全文见 [`M4-大海域世界化.md`](../技术/世界化/M4-大海域世界化.md) §0）
地图按语义重做（**不参考原版 33 关布局与数量**）、尺度放大（此前地图相对角色太小）、
操控参考战舰世界、落地翻滚按 Flash 逆向、Blender 统一风格参数多 subagent 批量建资产、
大地形站立面平直、连通性硬约束（能来回跳）、远景装饰随便堆、海面要仿真（开源可用、不考虑性能）。

### 本批已落地（全部已提交，f5208d6..8db0734 五连提交）
1. **WorldKit 资产 47 件**（`Assets/Art/Models/WorldKit/{Archipelago,Props,Marine,Horizon}/`）：
   4 组 subagent 并行 Blender 无头建模，统一风格参数 `tools/blender/scene/style_tokens.py`；
   可站立件带 `<Asset>.standable.json` 站面 manifest（0.5u 档 + 可选 yaw），预览图在 `docs/images/worldkit-*/`。
2. **8 张语义地图**（`Battle/WorldMaps/WorldMapCatalog.cs`，关卡号 101–108）：
   wreck_hymn/atoll_ring/ghost_harbor/turtle_back/mangrove_veil/spiral_throne/storm_cape/sunken_gate；
   站面表 `WorldMapStandables.cs` 是 GENERATED 文件（`tools/blender/scene/sync_standables.py` 从 manifest 再生成）。
3. **连通性硬约束已测试化**：`WorldMapConnectivityTests` 断言 8 张图出生点站面同连通分量、
   出生净空 ≥1.2u、栅格化有效；harness All 域 **1083 通过 / 31 失败（=ECall 固有基线）/ 1 跳过**。
4. **大海面**：`Water/OceanRig`（近场细分+环带+4200u 裙边）+ `PirateOcean.shader`（双长涌+白帽+
   HPWater 背光透射吸收）；Crest 否决（URP 版付费）、FLIP（MIT）记为后续交互液体候选——
   见 [`M4-海面选型与实现.md`](../技术/世界化/M4-海面选型与实现.md)。
5. **手感**：`Battle/RollRules`（逆向精确转写：空中 48°/s×vx、落地反弹 0.2/线性摩擦 78.125u/s²/
   接触阻尼 ×0.5，刚体仍冻结、滚视觉子变换）+ Scope 瞄准（FOV 28）+ 全景档 160u + 弹体追焦。
6. **Battle 接线**：`-worldMap <id>` 命令行/`WorldMapRuntime.SetPending` 进图；站面 box 直接生成
   BoxCollider（视觉自由碰撞平直）；世界地图禁爆炸破坏、跳过 RuntimeSceneArt、自动挂 OceanRig +
   `SetWorldSpan` + 氛围档（Storm→Overcast）。

### 踩坑记录（重要）
- **manifest 镜像**：Unity (X,Z)=(Blender X, **−Blender Y**)，yaw 同值——沉船/栈桥长轴在 Blender 沿 Y，
  摆放 yaw 90 才是东西向；弧段隆起经镜像在本地 **+Z**（yaw 0=北/90=东/180=南/270=西）。
- 旋转矩形 **Contains/Distance 必须用逆变换**（曾经写成正变换，yaw≠0 全镜像）；
  **内部净空用 EdgeClearance**，`Distance()` 在 box 内恒 0 不能当余量。
- `List<struct>` 索引器不能写字段（CS1612）；style_tokens 的 `bpy.data.curves.new(type="TEXT")`
  在 Blender 5.2 已改名（已删死代码）。

### 未完成（新对话按序）——【2026-09-17 早上二轮反馈后的最新状态】

**二轮实拍暴露并已修复**（详见 [`M4-设计理念与实现档案.md`](../技术/世界化/M4-设计理念与实现档案.md) §3/§6）：
FBX 导入 ×100（useFileScale=true 修）、海洋 shader 未入包（场景引用 Ocean_Water.mat 修）、
整海染金（光路方位门 + 波峰偏置 0.25）、近场看穿海床（_Opacity 0.94）、
布局"运兵过河"（8 图紧凑化，接敌 ≤110u，compact_layouts.py）、逐块色抖回退为有限色板
+ 程序化植被散布（ScatterDecorations）、灰盒 z-fight 下沉 0.06。
harness 全绿（31 条 = ECall 基线）。**播放器已按最终参数重建、wreck_hymn/atoll_ring 已重拍。**

新对话待办（按序）：
1. **看图**：`export/worldmap-captures/{wreck_hymn,atoll_ring}/world-*.png`——确认海洋蓝、
   植被密度、无白模闪烁；再补采其余 6 图（同一命令换 -worldMap id）。
2. 观感未达标再调：海洋参数走 ApplyOceanMaterialDefaults（C# 单一事实源，改完重烘+重建），
   `-oceanDebug 1/4/11/12` 调试档可二分定位。
3. 色彩/密度继续加码的方向：Kit 材质饱和度上探（现行中间调偏灰）、散布数量 2-4 → 4-6、
   手摆叙事道具补密度。
4. 战役 UI 重绑 8 图、M4 §7 未决项（射程上调？进度映射？风暴降水？）。

1. ~~batchmode 门禁~~ **✅ 已全绿（2026-09-17 凌晨）**：EditMode **1114 通过 / 0 失败 / 1 跳过**、
   编译 0 error、**shader error 0 条**（PirateOcean.shader 真编译通过）、47 件 FBX 的 .meta 已生成入库
   （`external/test-results-m4.xml` / `external/m4-editmode.log`）。
2. **用户看图**：`docs/images/worldkit-*/` 预览 + batchmode 后出 Unity 实拍（沿 §19 播放器流程，
   `-worldMap wreck_hymn` 之类的捕图参数还没接进 PlayerArtCapture）。
3. **kit FBX 真拼装**：`WorldMapAssetSet`（Editor 生成器还没写）扫 FBX 生成资产表 → Battle 场景序列化持有 →
   灰盒换真模；`SceneKitPilotSetup` 同款导入参数修正（useFileScale=false 等）。
4. archipelago agent（地形组）本会话仍在视觉迭代中，最终 README 交了后如有几何变动需重跑 sync_standables。
5. M4 §7 未决项（满力射程是否随大图上调、战役进度映射 8 图、风暴降水系统）。

## 21. 视觉审计修复批次 A–F——**新对话从这里继续**（2026-09-17 下午）

### 现状一句话
审计（[`审计/视觉审计报告.md`](../审计/专项/视觉审计报告.md)，§二 六项发现）定位的"水模拟被接线杀死 /
相机-雾-海洋三套尺度纲打架 / 光照链贫瘠 / 站面纯色灰盒"已按 §三 批次方案全部修复并提交
（六连提交 `e1e7b65`..`88f645f`）；门禁全绿（EditMode 1146/0、PlayMode 7/7、编译 0 错、
shader error 0 条）。**所有新数值是【提案/待定】，还没实拍——下一步就是开编辑器看图调参。**

### 本批做了什么（对应审计报告 §四 回写表）
- 批次A 救活水模拟：waterPlane 只退役 Renderer/WaterTessellator，WaterSimulationDriver 常驻，
  `ConfigureWorldDomain` 把模拟域接到世界地图图心（域边长 span×0.9 clamp[128,256]）。
- 批次B 海洋侧：长涌包络 12/110（全涌起点=半径+122，最大图 ≈306u 进画面）、环宽 1.15、
  地平线融合 1000/1400——C#/shader/mat 三处同源。
- 批次C 光照资产：阴影距离 100→224；Balanced Renderer 挂 URP 内置 SSAO
  （`Assets/Editor/BalancedSsaoInstaller.cs` 幂等安装，executeMethod 可重跑）。
- 批次D 雾侧：正午 150→1200、黄昏 130/1000、阴云 110/800；目录/BattleSceneLighting/场景
  三方逐值一致（测试钉住）；AmbientDirector 登记 RenderSettings.sun。
- 批次E 站面：BandMaterial 换 PirateTerrain（分档阈值推导、细节噪声、shader 缺失回退 Lit）。
- 批次F 浮沉：WaterSurfaceSampler（shader 同式）+ FloatingPropView（±0.5u/≤2°，纯表现），
  贴水阈值 0.5u；装配接线在 WorldMapComposer（BuoyRing 走 Props 循环命中）。

### 实拍验收清单（新对话按序，对应审计报告 §四 遗留项）
1. 进 Battle（任一 `-worldMap` 图）：确认爆炸有涟漪泡沫（A）、远处有长涌白帽（B）、
   拉远阴影仍在（C）、远景不再白茫茫（D）、站面沙草岩分带+纹理（E）、浮标随浪起伏（F）。
2. 调参入口：雾距改目录+BattleSceneLighting 两处（测试联动）；海洋参数走
   ApplyOceanMaterialDefaults 单一事实源；SSAO 改 BalancedSsaoInstaller 常量后重跑安装。
3. 裁决项：chop/涌浪幅度是否上探、ShipWheelPost 开阔水面命中是否合意、
   站面分档阈值与 SSAO 强度观感、环境光是否升级天空盒驱动（遗留项 6）。
4. 已知边界：SSAO 只作用 URP/Lit 物件；站面细节贴图打包降级（AssetDatabase 仅编辑器）；
   模拟障碍图仍是旧竞技场烘焙（世界图掩码未接，审计报告 §四 遗留 2）。

### 后续统筹：五审交叉与修复排期（2026-09-17 晚）

本目录五份审计（架构/代码/UI/地图设计/视觉）已交叉去重并归并为修复轨道
（Wave 0 卫生 + Track 2 玩法 P1 + Track 3 世界地图收口 + Track 4 死件清仓 +
Track 5 资源生命周期 + Track 6 契约收口 + Track 7 性能 + Track 8 架构工程），
总排序与难度分级见 [`审计/汇总-五审交叉与修复统筹.md`](../审计/汇总-五审交叉与修复统筹.md)。
**四阀门已裁决（2026-09-17，§七）**：尺度=方案 D（图跨维持 + 原版分层军火 + 俯视海图）/
实拍=主出图用户终审 / 删 TeamTurnTracker / Horizon 接线。
三个"难+视觉"任务各有自包含交接文档（`docs/隔壁交接-1/2/3-*.md`），用户另开对话执行。

### 波 1 已收口（2026-09-17 深夜，本会话停点）

**门禁全绿**：EditMode **1196 条（1195 通过 / 0 失败 / 1 既有跳过）**、编译 0 错、
shader error 0 条、装配链三步（M2BattleSceneSetup.BuildAll → HudMinimapSceneSetup.WireMinimap →
M3SceneSetup.BuildAll）exit 0。提交五连：

| 轨道 | 内容 | 提交 |
| --- | --- | --- |
| Wave 0 | .gitattributes + 47 FBX meta 收口 | `baa4886` |
| 裁决回写 | 四阀门 + 方案 D + 三份隔壁交接文档 | `823bf1d` / `64d3522` |
| Track 4 | 死件清仓（假表接真/死标签删/假提示改真/TeamTurnTracker 删/juice/字号口径），装配链重跑清场景死节点 | `d435e15` |
| Track 5 | 资源泄漏三处 + Fx 孤儿材质 + CampaignApi.Reset 退订（幂等测试）+ 太阳扫描负缓存 + HillIsland 死代码 | `4ce09a7` |
| Track 7 | 性能六项（contacts/轨迹数组/PlaybackGate int/监听器节流/Binder 缓存/循环重建） | `9624d7b` |
| Horizon | 远景接线（8 图签名天际线，200-280u 环带，48 测试） | `8886ccd` |
| 副产品 | 8 prefab 补序列化 enableRolling 默认值 | `0449655` |

**纠偏记录**：审计称 CampaignApi.Reset 会双订阅结算翻倍——实际 EventBus 按值语义去重不会翻倍，
违规照修但测试按真实语义写（Track5 agent 已验证回退可让测试变红）。

**下一步（新会话按序；1 已完成见下方 Track 2 回写）**：
1. ~~**Track 2（主模型亲自）**~~ ✅ 已收口（见下）。
2. **波 2（subagent）**：Track 6 契约收口 + 2P 结算乐句（同碰 BattleHud）、海床板退役、
   分层军火配置（方案 D，`WorldMapCatalog`）、出包贴图降级、障碍掩码接世界图、
   俯视海图小地图（方案 D 瞄准 UI）。**协调线暂被阻塞**：ZCode 更新后子代理模型
   （glm-5.3-flash）"思考档位未选择"派发失败，待修复后重派（提示词已备好）。
   **注意 WorldMapCatalog 与出包贴图两项正在隔壁布局对话手里，重派前先核对工作树。**
3. **Track 8①②（主模型亲自，Track 3 硬前置）**：BattleController 反向依赖修正 + asmdef 三合一
   （隔壁交接-2 的阶段 2 起点就是它，见 §22）。
4. **实拍图组（主出图用户终审）**：重建播放器 → 8 图 A–F 验收图 + chop 三档 + Horizon 签名核对
   （预期清单见 Track4/Horizon agent 汇报：wreck 双鲸、mangrove 独号触手、sunken_gate 三类齐上）。
5. 隔壁三对话已在并行（见下方"多对话并行现状"），交接-3 的 A–F 前置尚未满足（先出图）。

**已知遗留（小）**：RuntimeSceneArt 直删舞台会绕过 Lowpoly 网格回收（Track5 发现，域外）；
EditorBuildSettings.asset 有 eol 假差异噪音（不在 `*.meta` 规则内，可考虑扩 .gitattributes）；
2P 蓝队胜结算乐句（全量 P1-4）未修，归波 2。

### Track 2 已收口（2026-09-17 深夜续，主模型亲自修）

全量审计 PR-1 的 P1 三件落地，提交 `3c07e9c`（GoBack 栈语义）/ `f11af61`（Flash 帧口径）：

| 项 | 修法 | 测试 |
| --- | --- | --- |
| §一.1 GoBack 栈语义 | 返回导航不压栈（栈收敛）；同场景重载自动不压栈；replaceTop 补丁退役（SelectLevel/BattleHud 联动） | `ShouldRecordReturnPoint` ×3 |
| §一.2 帧计时漂移 | WeaponProjectile 特殊机制/引信 + TurnManager inactivity 全部移 FixedUpdate（25Hz=Flash 帧）；地雷引信查询同步降至 25 次/s（§四.2 连带解决） | 结构性修复，数值口径由各 *Rules 测试钉住 |
| §一.3 看门狗掐回合 | `BattleFlowRules.DecideAiTick`：**活动优先于超时**（弹体飞行重置看门狗，长弹道不再被掐）；看门狗只对"评估卡死"兜底 | DecideAiTick ×6 |
| §一.5 sweepingFlame 击退 | 水平击退从"恒世界 X"改为投到**远离火焰的方位轴**（随机符号语义保留） | `FlameKnockbackWorldDelta` ×5 |

**门禁**：EditMode 1275/1277 + PlayMode 7/7（端到端流转验证新栈语义）。唯一失败
`WorldMapConnectivityTests.AllMaps_SpawnsLandOnStandables(wreck_hymn)` 为隔壁布局对话的
进行中状态（spawn#1 净空 0.32<0.35u），归该对话处理。

### 多对话并行现状（2026-09-17 深夜实测，重要）

用户已开隔壁对话同仓并行：**布局重摆**（WorldMapCatalog/ReefFieldRules/摆放测试，进行中未提交）、
**天空盒环境光**（`0a754ed` 三档资产已提交、未接线）、**场景接线审计**（§22，`295dabb`）。
铁律升级：①各对话只 stage 自己的文件（Track 2 已示范外科手术式提交）；②batchmode 有
Library 锁冲突风险，跑前想清楚；③自己领域的红测试归领域对话修，别代修。

## 22. 隔壁交接-2 阶段 1：场景接线审计完成（2026-09-17 晚，本会话停点）

### 事件
用户指令「交接2」→ 执行 [`docs/项目/归档/隔壁交接-2-场景Prefab化与Find清退.md`](归档/隔壁交接-2-场景Prefab化与Find清退.md)。
**前置核对结论：Track 8①② 未完成**（`Assets/Scripts/` 下仍是 `PirateCrew.Gameplay.asmdef` +
`PirateCrew/Rendering/PirateCrew.Rendering.asmdef` 两个程序集，git log 无 asmdef 三合一提交），
故按该文档规定**只做阶段 1（审计 + 方案），不动场景**。

### 交付物（三件，均已入库）
1. `pirate-crew/Assets/Editor/SceneWiringAudit.cs` —— 场景接线**转储/比对工具**：
   实测读取场景的完整层级 + 组件 + 每个序列化引用的指向，输出人读 txt / 机读 tsv / 统计 summary；
   `Diff` 模式比对两次转储。**这是阶段 3「重跑装配链后行为等价」的验收仪器**（转储 diff 必须为空，
   或每条差异都能解释），不另开 Unity 进程也能跑（batchmode `-executeMethod`）。
2. [`docs/审计/专项/场景接线审计报告.md`](../审计/专项/场景接线审计报告.md) —— 实测基准 `3c6b51d`，
   F-1…F-9 九条发现 + 四 Prefab 切分契约 + 阶段 2–5 计划。README 待补表第一行（场景接线审计）就此销号。
3. `pirate-crew/Assets/Scenes/README.md` —— **场景契约**：五个场景的来源、装配链顺序铁律、
   稳定结构/关卡参数几何/运行时生成三类切分、改前必跑的转储命令。

### 关键实测结论（后续会话可直接引用，不必重测）
- **372 对象 / 23 根 / 最大深度 7 / Prefab 实例 0 / 序列化引用 1741（空 1278、跨根 26）**；
  `BattleCanvas` 一个根占 348 个对象（HUD 169 + 小地图 168 + 确认框 9）。
- **26 条跨根引用全部是"场景内对象 → 场景内对象"**，没有一条指向别的场景或运行时单例
  → 把 23 个根整体收进**单个** Prefab，26 条引用会全部内化、场景里 0 条接线。
  这是推荐的第一刀（阶段 2a），之后（2b）再按模块拆 4 个嵌套 Prefab。
- **Find 存量分三类根因**（修法差异很大）：
  ① 字段**压根没序列化**——`BattleCameraController.cs:901` 的 `aimThrow`、`BattleHud.cs:251` 的
  `_cameraController`（各加一行 `[SerializeField]` + 装配接线即可，懒加载 Find 变死分支可删）；
  ② 字段**不存在**——`BattleController.cs:379` 找 `AmbientDirector`、`:420` 找烘死的小地图岛层
  （`BattleMinimap` 加 `islandLayer` 字段即可，`HudMinimapSceneSetup.cs:152` 已持有该对象）；
  ③ **全局服务/环境兜底**——`AudioService.cs:1012`（AudioListener 存在性）、
  `WaterSimulationDriver.cs:446`（平行光，已被 `RenderSettings.sun` 覆盖，可评估退役）、
  `FxRoot.cs:145/307`（FX 单例反查战场）。
- **关卡内容混进稳定 HUD**：`MinimapPanel/DotLayer/IslandLayer` 下 **163 个 `IslandTile_*`** 按
  level 1 烘死（`HudMinimapSceneSetup.cs:56,376`）；`BattleMinimap.arenaWidth/arenaDepth` 同为
  level 1 值且**运行期无 setter**（`BattleMinimap.cs:45-47`）→ 世界地图下小地图点位仍按旧竞技场换算。
- **新发现缺陷 F-9（未出图验证）**：`Assets/Art/Materials/Scene/` 里没有 `Scene_SandWet.mat`
  → 运行时 `RuntimeSceneArt` 的 SandWet 组（簇级水线湿沙）被 `MaterialFor` 静默跳过，烘焙版有、运行时没有。
- **"顺序敏感三步链"的真实约束只有 ①→②**（`M3SceneSetup` 不碰 Battle，只写 Build Settings），
  且这条约束**没有任何测试钉住**。

### 门禁与下一步
- 本次只新增 Editor 工具 + 文档，**未改任何运行时代码与场景**；门禁见提交信息。
- 下一步（需等主协调线 **Track 8①②** 落地，否则会与 asmdef 大搬家互相踩）：
  按审计报告 §五 推进阶段 2a（单根 Prefab）→ 2b（拆 4 个）→ 3（Setup 收缩 + 等价性 diff）→
  4（Find 清退）→ 5（收尾）。阶段 3 顺带处理 F-2（小地图岛层改运行时）与 F-9（SandWet 材质）。

## 23. 隔壁交接-1 收口：八图布局重摆与出生构图（2026-09-17 深夜）

### 一句话
M4 八张世界地图的布局质量迭代已完成并实拍验收：站面覆盖 **3.9-13.8% → 16.6-17.8%**、
内容跨度 x 51-76% → **76-94%** / z 29-75% → **69-85%**、出生质心间距 40-174u → **44-117u**、
八图**全部单连通**、零落水道具；出生机位遮挡比 **53-80% → 0-44%**。
提交 `89eeb2d` → `e28c6bf` → `9867e13` → `9ab372e` → `cb3b388` → `21f9165`（专项文档已删）。
实拍 40 张：`docs/images/worldmap-captures-r2/`（README 含判据表与修复前后对照）。

### 本轮真正值钱的三条经验

1. **装配链是四步不是三步**（本轮踩到、已修 `cb3b388`）：`M2BattleSceneSetup.BuildAll`
   会**重建 Battle 场景**，而 `worldMapAssetSet` / `worldOceanMaterial` 两个引用是由
   `WorldMapAssetSetBuilder.BuildAll` 写进场景的。波 1 按旧的三步链重跑后这两个引用双双变空
   （`{fileID: 0}`）——实拍第一版整图只剩彩色灰盒、Player.log 报
   `找不到 shader PirateCrew/Ocean`。**正确顺序**：
   `M2BattleSceneSetup.BuildAll → HudMinimapSceneSetup.WireMinimap → M3SceneSetup.BuildAll
   → WorldMapAssetSetBuilder.BuildAll`（末步收口）。
   ⚠ 该约束**没有测试钉住**（与交接-2 发现的"三步链只有 ①→② 有约束"是同一类问题）。
2. **无头布局探针是这类任务的核心杠杆**：`external/harness-layout/`（不入库）里的
   `LayoutProbe.cs` 把 8 图的站面 box / 出生点净空 / 死水占比 / 落水道具 / 礁石场 /
   连通分量 / **ASCII 俯视图**全部导出成文本，跑一次 ~1 秒——"改布局 → 看形状与数值"
   完全不必启动 Unity。没有它，本轮的 20+ 次布局迭代会全部变成"重烘播放器 + 出图"的
   5 分钟循环。（窄域 `HarnessScope=Probe` 刻意不收 `Core`，这样并行会话改 Core 时
   不会把探针的编译带崩。）
3. **判据必须先用"修复前"的旧图校准**：`tools/worldmap/judge_worldmap_captures.py`
   在旧图上复现出审计的"全景内容占比约 25%"与"出生机位遮挡 2/3"——判据确实读得出审计
   认定的事实，才敢拿它当门禁。且**只有能自证的判据当硬门禁**（洋红 = 缺材质、
   出生机位遮挡 = 被糊满）；"内容/陆地占比"在矩形海床板退役前会被污染，只报数不判死。

### 本轮修掉的两个真 bug（都不是"审美问题"）

- **散布落点被父级缩放乘了一遍**：装饰挂成站面方块（`localScale=(Size.x, 顶高+4, Size.y)`）
  的子物体再写 `localPosition` → 位置被父级缩放再乘一遍，**50-147 件装饰全部被甩到图外
  3000-12000u**。"散布 2-4 件几乎不可见"（审计 §二.6）的根因在此，不是密度不够。
- **陆地道具落水**：道具 y 吸附到"该 xz 无站面 → 水面"，紧凑化后坐标漂到海里的 33 件
  篝火/木箱/宝箱就浮在浪上。现加白名单守卫（石质件/浮件放行）+ 跳过告警 + 测试钉死。

### 新会话若要接着做

- **留给波 2 的两条**：矩形海床板退役（当前画面里最显眼的问题——它让海面读成土黄色）；
  `(255,0,255)` 品红点（审计 §二.10【待查】，本轮把线索收窄到"物体到图心距离与图跨度
  成比例、只在 pano/team1-spawn 出现 ⇒ 最可能在随跨度配置的海洋域"）。
- **待用户终审的四条阈值**（均【提案/待定】，见 `WorldMapPropPlacementTests` 常量）：
  站面覆盖 ≥15%、内容跨度 ≥55%、出生质心间距 ≤120u、礁石场密度（每图 66-223 件）。
- **改布局的固定流程**：改 `WorldMapCatalog` → `external/harness-layout` 跑
  `HarnessScope=Probe`（秒级）→ Unity EditMode 全量 → 重烘播放器 →
  `PirateCrew3D.exe -worldMap <id> -artReviewOut <dir>` → `judge_worldmap_captures.py` → 人眼。

## 24. 波 2 subagent 配额故障（2026-09-17 深夜，主协调线，待用户裁决）

子代理思考档位修复后重派波 2（W2a 军火配置 / W2b 海床板+海图 / W2c 贴图+掩码 /
W2d 契约+2P乐句），**四路全部因周用量配额耗尽失败**（重置 2026-09-21 00:00 +0800，
或升级配额）。四个 agent 均死在探索阶段（各 ~5 分钟），**文件域零残留**（已核实工作树；
当前未提交改动属隔壁天空盒对话的接线阶段）。

**选项**：①升级配额后重派（提示词都在本会话记录与 §21 波 2 条目）；②等 9-21 重置；
③主模型亲自逐项做（顺序：军火配置 → 海床板+海图 → 契约+2P乐句 → 贴图+掩码；
若主模型与子代理共享配额池，亲自做同样有中途断气风险——**每完成一项立即提交**，
减小损失面；本节之前的 Track 2 与波 1 均已提交落袋为安）。

**与 §23 的交叉情报**：品红点（审计 §二.10）线索已收窄到"随跨度配置的海洋域"——
大概率归 W2c（水模拟域重配）顺手排查；海床板退役（W2b）被 §23 点名为"当前画面里
最显眼的问题"，优先级最高。r2 实拍 40 张（`docs/images/worldmap-captures-r2/`）已天然覆盖
A–F 验收与 Horizon 签名的出图需求，用户终审可直接看那批图。

## 25. 波 2 主线收口（2026-09-17 深夜续，主模型亲自逐项做完）

子代理配额耗尽（§24），用户裁决主模型亲自做。四项全部落地提交，每项即时提交落袋：

| 项 | 内容 | 提交 |
| --- | --- | --- |
| 海床板+俯视海图 | `SetupWorldMapEnvironment` 按 `Seabed_` 前缀退役 M2 矩形海床（§23 点名的最显眼问题）；`BattleMinimap.ConfigureWorldChartFromRuntime` 世界图双模式（跨度重映射/旧 level_1 瓦层隐藏/站面 box 岛层），`MinimapRules.WorldBoxToChartRect` 纯函数 | `542d71c` |
| 方案 D 分层军火 | `WorldMapDefinition` 新增 `CrewWeapons`/`CaptainWeapons`；8 图船员近程基线（樱桃∞+主题变化件）、船长全图级旗舰（101 锚/102 浪/103 巫毒/104 炮/105 海鸥/106 浪/107 炮/108 巫毒+锚双旗舰）；补齐 101/107 空投池中程档；Runtime 消费目录初配（null 回落旧惯例）。配比【提案/待定】 | `1125e3b` |
| 契约收口+2P 乐句 | `Core/SceneEvents`/`SaveEvents` 常量类，收编 UI×4+CampaignApi 五处本地 const；契约表 §2 转正+补登 `battle_shot_released`/`camera_focus_requested`+零订阅标注；**2P 热座蓝队胜不再误播失败乐句**（`BattleHud.SettlementJingleFor` 委托 mapper，全组合一致性测试）；M2 空间模型文档口径勘误（水面 -0.4/瓦片 2u） | `26a0f5d` |
| 贴图+障碍掩码 | 六张细节噪声贴图挪入 `Assets/Resources/MaterialNoise/`，WorldMapComposer 改双平台 `Resources.Load`（出包不再降级）；`ConfigureWorldDomain` 带地形重载——障碍掩码由 `ObstacleMapRules.Bake` 按世界图站面实时烘焙（涟漪岛缘反射，不再穿岛/不再采旧竞技场 PNG） | `11b3920` |

**验证状态**：窄域无头 harness（`external/harness-minimap/`，Scope=MinimapChart）10/10 全绿
（海图纯函数 6 + 军火数据 4）。**全量 EditMode 未跑**——asmdef 对话（Track 8①②）与天空盒
对话尚在工作树施工（`BattleController` 等文件半成品），全量门禁待它们落地后统一收口，
届时一并跑：海图装配测试（MinimapWorldChartTests）、乐句一致性（SettlementJingleTests）、
海床退役与前两项的世界图冒烟。

**事故记录（多对话并行的真实代价）**：`11b3920` 首次提交时误把 asmdef 对话预存 index 的
asmdef rename 一起提交（无路径限定 commit 提交了整个 index）——已 `reset --soft` 拆出重提，
对方工作树无损；**教训：多对话并行期 commit 必须路径限定或提交前查 `git diff --cached` 全量**。

**审计纠偏累计三条**：①CampaignApi.Reset"双订阅翻倍"不会发生（EventBus 值语义去重）；
②存档键已是 public const（代码审计 §三.1 描述过时）；③契约表"重复登记"实为总表与
§3.3 订阅方分表各司其职，非重复。

**遗留**：品红点（§23 线索：随跨度配置的海洋域物件）——r2 图判据未触发，下轮实拍复查；
军火配比/海图岛色等【提案/待定】数值待试玩与实拍转正。

## 26. Track 8①② 落地 + 乐句契约补齐（2026-09-17 深夜续，asmdef 专项对话）

本对话即 §25 所说「全量门禁待 asmdef 对话落地」的那个对话。三笔提交：

| 内容 | 提交 |
| --- | --- |
| Track 8① 反向依赖修正：`Core/BattleLaunchContext` 中立注入（关卡序号 + 编成导出符号快照），`BattleController` 删 Campaign/CrewManagement 引用，Campaign 四处对称写清 | `fcda283` |
| `26a0f5d` 乐句契约漏改补齐：mapper 改 `bool + out SfxId`（2P 热座蓝队胜不放乐句唯一真值），同步 AudioService/音乐映射测试/漏 using——HEAD 此前编译不过 | `5ef73e2` |
| Track 8② asmdef 五程序集：Core（零引用）/ Runtime（挪入 PirateCrew/，GUID 不变）/ CrewManagement / Campaign / UI，测试 asmdef 补按名引用；分层违规从此是编译错误 | `a3973ab` |

**门禁（全绿，可复跑）**：EditMode **1308 总 / 1307 过 / 0 失败 / 1 既有跳过**；PlayMode **7/7 skipped=0**；
harness All 1271 过 / 36 失败全为 ECall 固有。架构审计 P0-1 已销号（该报告 §7）。

**§25 事故的呼应**：主协调线 `reset --soft` 拆出误提交后，本对话的 git mv 退回未暂存态（工作树无损），
本次按「路径限定 add」重提；乐句漏改是 `26a0f5d` 只收了 BattleHud/测试、没同步 mapper 所致——
**提交前全量编译（哪怕窄域跑过）这一课再次应验**。

### 下一步（新对话/本对话续做均按此序）
1. **交接-2 阶段 2a（单根 BattleRig.prefab）已解锁**——但天空盒对话仍有
   `M2BattleSceneSetup.cs`/`BattleSceneLighting.cs`/Ambient 三件在途未提交，恰是 Prefab 化要碰的文件，
   **等它落地、树安静后再开工**（改前 dump、改后 dump、Diff 等价的流程见场景 README）。
2. Track 8② 余项：P1-4 目录归位 + P1-5 命名空间折叠（`PirateCrew.*`→`PirateCrew.*`，163 文件），
   同样须安静窗口；做法=git mv 连 meta 一起挪（保 GUID）+ 命名空间/using 批量替换 + 全量门禁。
3. 天空盒对话落地后：§25 预告的统一收口门禁已由本次提前跑绿（1308/7 全过，含其在途 Ambient 文件）。

## 27. 主协调线：海图 UI 入口 + 双通道互斥 + 海面回归二分 + 一代退场裁决（2026-09-18）

### 本会话落地的两笔代码（门禁全绿）
| 内容 | 提交 |
| --- | --- |
| 选关页「大海域」页签——8 图首次有 UI 入口（UI 审计 P0-1；P2-7 溢出随之消解） | `4ebc986` |
| 进战斗双通道互斥（出海清战役注入/选关清海图待战）+ `WorldMapRuntime` 进播放重置 + 4 测 | `a23b5f5` |

门禁：EditMode **1311/0/1**、PlayMode 全过（external/test-results-r4*.xml）。
热区排查结论：波1/波2/并行对话代码复核无新实锤 bug；海图/乐句/水域新 API 调用方齐全。

### 海面不可见回归——二分进行中（跨域阻塞项，接手必读）
- **实锤为真回归**：`cb3b388` 播放器海面在（world-pano 下半幅蓝像素 **31.92%**），
  HEAD 缺席（**0.54%**）；`[OceanRig] 调试档` 日志证明 `OceanRig.Create` 已执行、
  `-oceanDebug 4` 全泡沫档无变化 → **网格未被光栅化**（非颜色链问题）。
- 场景引用（worldOceanMaterial/worldMapAssetSet）完好、shader/mat 自 r2 零改动、
  BattleController 净 diff 与渲染无关——凶手在 `cb3b388..HEAD` 的某笔提交
  （波2 / asmdef / 天空盒三段）。**下一步：重测 `26a0f5d`**（上轮构建命令 cd bug 结果作废，
  判海跑了旧播放器）。
- 判海工具已入库：`tools/worldmap/judge_sea_visible.py`（对 pano 下半幅扫蓝像素，
  ≥5% = 有海，≈0.5% = 无海）。构建-判海一条龙命令模板见该文件头。

### 用户裁决：一代（M2 瓦片竞技场）整体退场（本会话最大转向）
裁决原文与逐文件施工图见 [`docs/项目/归档/关卡体系瘦身-一代退场计划.md`](归档/关卡体系瘦身-一代退场计划.md)
——**新会话从这里接手**：先修海面（§上），再按计划 Phase 1→5 执行删除。
战役元循环改挂海图 id；Battle 变世界图单路径；隔壁交接-2（Prefab 化）待 Phase 3 后重排；
README 宣传口径（"33 关重制入远期"）随一代退场失效，Phase 5 重写。

### 残留移交
- `export/worldmap-captures/`（36MB）与 `export/art-review/gallery/`（31MB）为已完成对话的
  **未压缩中间产物**（成品已分别入库 `worldmap-captures-r2/`、`ambient-skybox-debug/` 等），
  待用户处置：压缩入库或删除。
- URP 资产 SSAO prefilter 序列化改动（Unity 自动重写）已随本节提交收口。

---

## 28. 一代退场执行完成（2026-09-18 续，主协调线）——重构门禁全绿，剩海面二分与场景重建

### 执行结果
用户指令「开始瘦身重构」，按 [`docs/项目/归档/关卡体系瘦身-一代退场计划.md`](归档/关卡体系瘦身-一代退场计划.md) 施工图执行。
**代码面一笔提交完成**（原计划 Phase 1→5 分五个提交，实际因编译闭合——UI/Campaign/Battle 相互引用
被删类型——合并为一笔）：`c4458eb`（93 文件，**-17462 / +1701 行**）。

- **数据层**：LevelCatalog(+Levels2/3)/LevelTileMaps/LevelDefinition/CampaignCatalog/BattleLaunchContext/
  TerrainCatalog/PlatformClusterLayout 布局器/SceneArtBuilder 全删；幸存数据载体抽为
  `Data/LevelData.cs` 与 `Terrain/PlatformMap.cs`；块高单一来源 = `LevelGeometry.BlockWorldHeight`。
- **战斗单通道**：BuildPlan = 世界图（唯一玩法路径）> 样板三关（`-artReviewLevel`，兼无待战图兜底）；
  BuildTerrain/RebuildSceneArt 两支；瓦片整格破坏随一代退役。
- **战役重指向**：结算键 = 海图 id（battle_started 从 `WorldMapRuntime` 收养待战图）；
  `campaign_map_completed` 替代 level_selected/level_completed（契约表已同步）；
  招募门槛 = **累计星数**（UnlockStars，数值 0/3/5/7/10/13 沿用）；存档格式不变值域变化，**旧档弃**。
- **UI**：LevelSelect 重写为大海域单列表（海图行带星级）；MainMenu「进入战斗」= 第一张海图直出；
  再战 = SetPending(同图) 重载。
- **Editor**：M2BattleSceneSetup 删海床/烘焙段（场地按样板 20×15）；HudMinimap 删岛层烘焙；
  WaterAssetBuilder 障碍图改样板第 1 关高度场；M2DataAssetGenerator 删关卡资产生成；
  Assets/Data/Levels/ 三个 SO 资产删除。
- **文档/版本**：README（测试数 1157、生涯循环措辞、路线图去"33 关重制"）；bundleVersion 1.0.0→**1.1.0**。

### 执行者的四个口径决策（超出施工图字面，已落地并标注）
1. **招募门槛**：一代按关卡序号，海图无序号语义 → 改按累计星数（数值沿用，量级恰好匹配 8 图×3 星）。
2. **编成注入退役**：`BattleLaunchContext` 还携带 ActiveBattleSymbols（出征过滤），按施工图整删后
   海图战用地图自带布阵，名册只影响经验/招募 → 「船员编成重新接入世界图战斗」列为后续任务（同级化一并考虑）。
3. **样板三关入口**：常规 UI 不可达（出海页只列海图）；保留 `-artReviewLevel 1–3` 出图入口 +
   直接 Play Battle 兜底样板 1（与旧 level_1 默认一致）。
4. **SceneKitCatalog.BuildFor 保留**：纯 PlatformMap→kit 组合器、无一代数据依赖（BuildLevel1 已删）；
   现无场景调用方，是否随同级化退役待后续裁决。

### 门禁
EditMode **1150 过 / 0 失败 / 1 跳过**（净 -160 用例：删一代专属 8 个测试文件，重写 5 个）；
PlayMode **7/7**；编译 error 0 条。**播放器重建与 8 图捕图未做**（见下）。

### 新会话接手顺序（阻塞项优先）
1. **海面回归二分收尾**（§27 的实锤与工具不变）：重测 `26a0f5d`（上轮作废），两三轮构建锁凶手提交并修复。
   删除 surgery 不影响二分——被测提交在 `cb3b388..HEAD` 历史里。
2. **场景重建一笔**：跑四步装配链（M2BattleSceneSetup.BuildAll → WireMinimap → M3SceneSetup →
   WorldMapAssetSetBuilder）重烘 Battle.unity（去 Seabed_*/烘焙瓦层/旧水体检）→
   **删 BattleController 三个退役器**（RetireLegacyWaterPlane/RetireLegacySeabedShelves/HideBakedMinimapTiles，
   前者退役的是 waterPlane 渲染件、WaterSimulationDriver 仍靠它常驻——删渲染禁用逻辑但保留驱动常驻语义）→
   播放器重建（grep "shader error"=0）+ 8 图捕图 + 人眼/判海工具验收。
3. **同级化任务书**（WorldKit/Showcase/Blender 拉公共代码，另立会话）；
   隔壁交接-2（Prefab 化）在场景重建后重排；`export/` 67MB 残留待用户处置（§27 不变）。

### §28.1 更正：海面"回归"实为"从未渲染过"——已修复（64d25d1）

§28 所列"海面回归二分"已在本会话收口，但结论与 §27 预期**完全不同**：
- **海面从未在播放器里渲染过**（r2 实拍就没有海面；早期被沙色海床板遮住，`542d71c` 撤板后缺失才显形）。
- §27 的"cb3b388 31.92% 海面在"与"重测 26a0f5d"**全是判海工具假阳性**（蓝像素占比把相机清屏色/
  天空盒背景当成了海）。
- 真根因：`BuildDiscMesh` 绕序从上方看全为背面，被 Pass 的 `Cull Back` 整体剔除。
  修复 = PirateOcean 改 `Cull Off`（一行）。完整实验链与教训见
  [`docs/项目/归档/海面回归二分-实拍记录.md`](归档/海面回归二分-实拍记录.md)。
- 验收：wreck_hymn 三机位海面完整（`docs/images/worldmap-captures-r3-fix/`）；shader error 0；
  EditMode 1150/0。**判海工具不可再用于海面判定**（待改为纹理方差类判据）。

遗留事项更新：原"海面二分"销号；§28 的"场景重建 + 删退役器 + 8 图捕图"仍然有效
（现在可以在海面可见的正确基线上做视觉验收了）。

## 29. 管线合并（糖豆人式资产架构）五阶段执行完毕——**新对话从这里接手**（2026-09-19）

### 一句话
[管线合并任务书](../技术/架构/管线合并-糖豆人式资产架构任务书.md) 前置+五阶段全部落地并提交：
几何生成彻底退出运行时——C# 程序化生成器改岗**编辑器烘焙器**（配方+种子 → 按材质组合并网格 →
prefab 落盘），运行时只做实例化组装；WorldKit 手作 FBX 与烘焙件同目录同 manifest 登记。

### 提交索引（每阶段一笔）
| 阶段 | 内容 | 提交 |
| --- | --- | --- |
| 前置 | 场景重建一笔：OceanRig 海面全模式统一（样板关不再用旧 PirateWater 平面）、旧水面 Renderer/Tessellator 烘焙期禁用、删三个退役器、ArtGate 补 ⑦.5 空岛摆入 + ⑩.5 WorldKit 资产表链尾收口 | `d98e087` |
| A | SceneArtBaker：Ship_Galleon/Ship_Longboat/ShowcaseDangerBorder 烘焙落盘，样板第 2/3 关改摆位表实例化，风摆深度查找 | `0b4f980` |
| B | CloudField.prefab（含双 BoxCollider），第 1 关改实例化，RuntimeSceneArt 旧装配路径全删 | `bd54148` |
| C | SceneAssetManifestBuilder：Assets/Art/Models 全树 81 件上游登记（程序化=配方+种子/手作=Blender 脚本），manifest 格式【提案/待定】 | `c9bc7e2` |
| D | 新编辑器程序集 PirateCrew.SceneArtBake（几何发射器家族迁入，GUID 全保）；一代布局链死码清除；BattleTerrainView 收缩为纯碰撞层 | `7c91a51` |
| E | SceneArtBakeDeterminismTests（6 用例，同配方两次合成 bit 级一致） | `4e9fe23` |

### 门禁实测（2026-09-19，可复跑）
EditMode **1142/0/1**（-14 = 一代布局器死码测试随迁，+6 = 确定性用例）、PlayMode **7/7 skipped=0**、
编译 0 错。**活体确定性验证**：重烘后 Baked/ 网格资产逐字节零漂移。实拍对照入库
[`docs/images/scene-art-bake/`](../images/scene-art-bake/README.md)（l1 差异 ≤0.5%、l3 ≤0.2%；
l2 特写机位差异放大属「南船逐件种子序号平移」的预期抖动差，几何级判据以两条程序化门禁为准）。

### 架构现状（新会话改场景美术前必读）
- **改船/云的形状** → 改配方/规格 → 重跑 `PirateCrew/烘焙/样板场景件`（ArtGate ⑦.6）→ 重烘场景接线。
  运行时零几何代码，改运行时代码不会改变外观。
- **场景美术材质组命名契约**：prefab 子网格名 = `"SceneArt_" + 组名`（AmbientWindBinder 按名深度查找绑风摆）。
- **摆位**：样板关摆位表在 `ShowcaseLevels.BakedPlacements`（纯数据）；世界图摆位在 `WorldMapCatalog`。
- **运行时程序集**（PirateCrew.Gameplay）已无几何发射代码；`MeshBuffers` 留守只为 Ambient 海鸥复用。

### 遗留
- 交接 §28 的「8 图 A–F 视觉验收/实拍」仍待用户终审（本轮前置已重烘场景并修好 skyIslandRoot 接线）。
- `WaterTessellator` 已无消费者（烘焙期禁用 + 无运行时路径），可在后续清理轮删除。
- `PirateWater` shader 旧水面材质只剩禁用组件引用；样板关海面已统一 OceanRig。
- `export/showcase-{baseline,baked}/` 中间产物（各 24 张 PNG）留在 export/ 未入库，按 export 策略本地留存。

---

## §30 关卡制作管线立项 + 样板三关重构（2026-09-19，用户裁决轮）

**裁决**：两艘程序化烘焙船（Ship_Galleon/Ship_Longboat）观感「完全不能看」整体删除；
样板三关按真关卡策划流程彻底重做——每关一份设计文档（体验/难度曲线/为什么/怎么做/资产），
六阶段管线（概念→纸面→灰盒→走查→美术→封版），逐阶段留文本与资产。

**产物体系**（全部在 [docs/设计/关卡/](../设计/关卡/README.md)，设计值均标 AI 提案/待终审）：
- 管线章程（阶段门禁表）+ 旧样板三关归档（含旧设计理念回忆与逐关处置结论）
- L01 云端漫步（教学 4v3，蓝 luck1）/ L02 碎岛雨（进阶 4v4，luck2，替换双雄并舷）/
  L03 天空之岛（考核 4v5 以少打多，luck5）——布局 ASCII 块为唯一真源，
  `tools/level-design/render_{layouts,curve}.py` 从文档渲染 PNG
- 数值锚点全部引用逆向文档行号（满力射程 12.5 格、R1-R18 建关约束、水距↔武器池配平律）

**代码/资产落地**：船管线四文件（SceneKitCatalog/Composer/Geometry/ShipHullGeometry）+
双 prefab + 四网格资产删除；三关编成/武器池/luck 按设计文档重写；L2 新烘
`Islets_L02.prefab`（IslandShellGeometry.BuildSolidShell 从 L2 逻辑高度场直出岛壳，
顶面与碰撞层**按构造对齐**，材质复用 L3 空岛 RockMid）；manifest 上游同步换血。

| 提交 | 内容 |
| --- | --- |
| `8590ba7` | 上轮残留补交（BattleTerrainView 纯碰撞化场景重存 + 两个文件夹 .meta） |
| `82a0ed5` | 关卡管线文档体系 + 布局图/难度曲线渲染工具 |
| `9b25c6d` | 双船退役 + 三关数据重构 + 碎岛烘焙件 + 测试（34 文件 -2697 行） |

**门禁实测**：EditMode **1164 用例 1163 绿 / 0 失败 / 1 跳过**（跳过=既有音频移植用例）；
烘焙链 ⑦→⑦.5→⑦.6→⑦.7→⑧ 全绿；碎岛/云场网格重烘逐字节零漂移；场景无船 GUID、
isletsPrefab 已接线；scene-assets.json 船条目清零、碎岛登记上游。**三关实拍走查通过**
（AI 自查，待用户终审）：L1 云台+四环云齐/7 人贴云面、L2 七岛对角/8 人贴岛面、
L3 大岛+空岛齐/9 人贴草皮面，全部无船无洋红海面正常，走查记录与实拍图已回写各设计文档 §8
（入库 `docs/images/level-design/`）。

**新会话改关卡前必读**：
- 改布局 → 先改设计文档 ASCII 块 → 再改 `ShowcaseLevels`（高度场/编成/摆位）→
  L2 还须重跑 ⑦.6（岛壳与逻辑场按构造对齐）→ 跑设计契约测试（`ShowcaseLevelDesignTests`：
  站位实心/簇数/水距/luck 阶梯，改口径先改测试注释里的设计出处）。
- 难度旋钮只有三个：编成规模、蓝方 luck（1/2/5/10 档）、武器池——角色零属性差（原版基准）。
- 布局图/曲线图重渲染：`python tools/level-design/render_layouts.py` / `render_curve.py`。

**与 UI 线（隔壁会话）的协作台账**：
- `BattleHud.cs:771` 的 `HidePausePanel()` 调用是 UI 线中间态（方法未实现）；本线为开编译窗口
  **临时注释了该行**（已被 UI 线提交 `3deb7cb` 带入历史）——UI 线落地暂停面板时取消注释即可。
- 本线曾顺手修复 UI 线 `OverheadHealthBar.cs:57` 的 `Vector.one → Vector3.one` 单 token 笔误。
- **Battle.unity 未提交**：当前工作树版本同时含本线接线（isletsPrefab/船清退）与 UI 线 HUD
  场景改动，等 UI 线收口后随其提交（或本线补一笔场景卫生提交）；运行时已按新接线工作。
- **并行会话场景竞态教训**：本线 14:29 建的播放器烤到了 UI 线场景重建的中间版本（⑦ 洗掉
  空岛、⑦.5 未跑的窗口态），首轮 L3 实拍空岛缺失；确认盘上 Battle.unity 收口态后重烤即恢复。
  **并行期间出图前必须先核对场景文件为装配链完整跑完的版本。**

**遗留**：设计文档用户终审（走查已过，见上）；L2 观感升级候选
（FloatingIslandComposer 多岛小尺寸版，提案/待定）；8 张世界海域图套用本管线做纸面设计
（尺度/武器配平矛盾见地图设计审计报告）登记为后续工作。

---

## §31 UI 跨引擎像素级复刻（stick-world→本仓，2026-09-20）——**本任务新对话从 game-2 侧交接档接手**

**任务**：把姊妹项目 stick-world（`F:\VSCode\game-2`，Godot）的 UI 设计语言像素级复刻进本仓，含主菜单在内全部界面换装 + 世界空间血条族。**双仓任务**，完整交接档/方案/决策在 game-2 侧：`F:\VSCode\game-2\.temp\ui-pixel-replica\docs\项目\交接\UI跨引擎像素级复刻-进度与交接.md`（worktree 分支与本仓同名 `agent/ui-pixel-replica`）。

**创始人已拍板**：① 配色令牌完整复刻，game-2 为唯一真相源——**§27 之前的"夜海蓝改配色"线（`6a9cd00`）废弃**；② 文本容差对齐（1~3px 不追究，红线=布局不乱）。

**已落地（P0，本仓分支 4 提交 `10c7aca`→`9877922`）**：`Assets/Resources/UI/StickWorld/`（81 沸腾贴图/4 字体/93 图标/令牌 JSON+SHA256 清单，同步脚本 `game-2 tools/ui_sync/sync_to_unity.py` 产出，含 `StickTokens.generated.cs` 代码生成——**禁手改，改令牌走 game-2 导出器重跑同步**）；`Assets/Scripts/UI/Stick/`（`PirateCrew.UI.Stick`）：Tokens 令牌层 + Paint 手绘自绘核（SketchWobbleGraphic/SketchBarGraphic，逐公式移植）；`Assets/Editor/TextSampleBuilder.cs` 文本样张出图器；SketchSkin 贴图沸腾节拍对齐 7.5fps。

**文本容差实测（8 档字号样张，Godot vs TMP）**：整图超容差像素 3.07%（tol=8）；**布局零漂移**——八行墨迹起止右端差 ≤2px、纵向 ≤1px，差异全在笔画边缘 AA 质感；对比样张 `F:\VSCode\game-2\.temp\ui-pixel-replica\temp\text_sample\diff\side_by_side.png`。

**待做（P1/P2，顺序见 game-2 交接档 §五）**：令牌接管 UiSkin（注意 `Tests/UI/UiSkinTests.cs` 夜海蓝断言会红，需同步改写）；SketchSkin 贴图路径切 `UI/StickWorld/`（槽位名差异做双路径映射）；四场景 CanvasScaler 改 **Expand**（对齐 Godot expand，废止 BattleUiTheme"不改 CanvasScaler"契约）；MainMenu→LevelSelect/CrewManagement→Battle 逐界面换装；世界空间血条族（SketchBarGraphic+数据镜像）；出图铁律不变（GUI 模式 `-executeMethod`，batchmode 只做编译门）。

**⚠ 并行会话教训（本仓再现）**：2026-09-20 14:37–14:45 有会话在共享工作副本上切 main 提交 `c700ad6`，
把本任务 agent 写盘的 Stick 文件卷入——本仓与 game-2 一样存在多会话共写一个工作副本的竞态，
**开工前先 `git branch --show-current` + `git status` 核对现场**；本任务 Unity 侧一律在 `agent/ui-pixel-replica` 分支，不碰 main。

---

## §32 美术风格翻新步骤 1 收口（2026-09-21，等距像素卡通开工序列）——**新对话从这里接手步骤 2**

### 一句话
创始人裁决美术方向转向**等距像素卡通**（文档四件套已入库 `d78d0c5`：美术风格指南 Art Bible /
渲染管线实现口径 / 像素纹理资产管线 / GDD），本会话完成渲染篇 §8 开工序列的**步骤 1**：
正交相机切换 + 全屏像素化 RendererFeature，播放器实拍验收通过（块化判据 0.00、UI 不块化、
洋红 0、shader error 0、EditMode 1161/1162 全绿）。

### 本步落地（全部已提交，见下方提交索引）
| 件 | 内容 |
| --- | --- |
| 像素化 Feature | `PirateCrew.Rendering.PixelationRendererFeature`（`AfterRenderingPostProcessing` 抓相机色 → nearest blit 到高 360 Point RT → nearest blit 回屏幕）+ `PixelationInstaller`（两档渲染器幂等安装，菜单/executeMethod 均可） |
| 相机正交化 | `BattleCameraController`：距离档位退役 → **OrthoSize 档位**（特写 5/全场 17/全景 clamp(span×0.3,17,60)/滚轮 [3,60] 整数档）；俯角统一 45°、Transposer 距离恒 30、**旋转锁死**（环绕输入退役，观察模式/中键自由锚保留）；Scope/推近/旁观 FOV 特效按当量比率映射 size（`size = 手动档 × fov/60`）；蓄力拉远改 `ChargeZoomOrthoSize` |
| 场景烘焙 | `M2BattleSceneSetup`：vcam 与回退 Camera 均正交（`OrthographicSize: 17` + FOV 60 当量保留）；「透视=真 3D」旧口径注释改写 |
| URP 全局 | 两档 asset：HDR 关 / MSAA 1x / 主光阴影关 / 附加光 0（渲染篇 §6 表；SSAO 暂留 Balanced，去留随步骤 2 裁决） |
| 测试 | `BattleSceneWiringTests`（透视断言→正交断言 `AssertOrthographicTiltedCamera`）/`CameraFeelRulesTests`（距离档位→size 档位契约，PitchForDistance 测试删除）/`HudProjectionRulesTests`（25px 可读性判据改正交屏幕口径） |

### 门禁与验收（可复跑）
- EditMode **1161/1162 全绿 0 失败 1 既有跳过**；编译 0 错；播放器构建 **shader error 0**
  （仅 3 条存量 warning：PirateOutlinePost 循环梯度 / PirateOcean pow 负数，d3d11 提示非 error）。
- 实拍 11 张 `export/pixel-review/r1/`，6 张入库 [docs/images/pixel-review/r1/](../images/pixel-review/r1/README.md)。
  程序化判据：**3×3 块内方差**（旧图校准 2.5-2.8 → 新 3D 图 0.00 = 完美块化；含 HUD 图 0.59-1.2 =
  UI 区不块化即红线 2 证据）；洋红全零。
- 出图命令：`external/build/PirateCrew3D.exe -artReviewOut <dir>`（PlayerArtCapture 既有链路）。

### 教训（§4.4 血泪教训 +1）
- **首版自写 blit shader `Hidden/PirateCrew/PixelBlit` 在播放器真实 GPU 会话报
  `not supported on this GPU (none of subshaders/fallbacks are suitable)`**——编辑器打开无报错、
  播放器构建 grep shader error 也是 0（首次构建曾现 4 条匹配但被缓存后的重跑掩盖，**构建日志必须当场存文件**）。
  修法：blit 材质改用 **URP 自带 `Hidden/Universal/CoreBlit` 的 pass 0 "Nearest"**（`FragNearest` 内部
  就是 `sampler_PointClamp` 点采样；被 URP 运行时引用故构建必然保活，无 r2 剥离风险）。自写 shader 已删。
- 像素化验收判据「3×3 块内方差」先用未像素化旧图校准再判定（阈值才可信），与 §23 判据纪律一致。

### 新对话接手（步骤 2：赛璐璐 + 反向壳描边试点）
1. 读渲染篇 §4（赛璐璐：N·L 2~3 档阶梯 + 暗部色相偏移 + Bayer dithering + 无高光/无实时阴影）与
   §5（反向壳描边：`SRPDefaultUnlit` 独立 LightMode / 平滑法线烘顶点色 / RT 1px / INK 墨色）。
2. 拿 Battle 场景一个岛台试点（`SceneArtBake` 程序集里的烘焙器产出件 + `PirateOutline` shader 族）。
3. 验收：编辑器开着的会话用 MCP `read_console` 零 shader error + 出图判据；阴影全关后的落地感
   （blob shadow / `ContactShadowDecal` 全量接线）一并处理。
4. 待定项回美术指南 §8 销项（RT 分辨率/俯角/档位数值本轮全是 AI 提案起步值，实玩校准后回写）。

### 本轮提交
- `feat(render): 等距像素卡通步骤 1——正交相机 + 全屏像素化...`（功能：Feature/安装器/相机/场景/URP/测试）
- `docs(pixel-review): 步骤 1 实拍验收图入库 + 三文档回写`（图库/渲染篇状态/待办/本节）

---

## §33 等距像素卡通步骤 2 收口（2026-09-21，主智能体线）——**新对话从这里接手**

### 一句话
赛璐璐 shader + 常驻反壳描边**已落地为 ToonPilot 独立风格测试场景**（创始人指令：单建测试场景、
逐层复杂化，像从头开发新游戏）；赛璐璐全链已验证工作，**描边覆盖面/岛台几何/船员渲染三处未决**。
同时落地两条相机裁决（真等距俯角 35.264° + 方位可旋转）与 SSAO 退役、放置类弹体 blob shadow。

### 本轮提交（六笔）
| 提交 | 内容 |
| --- | --- |
| `e5f1218` | `feat(render)`：PirateToon.shader（赛璐璐本体 + 常驻 INK 反壳 + DepthOnly）+ ToonLightDriver + SmoothNormalsBaker（+5 测试）+ PixelationFeature RT 下发 + ToonPilotSetup/ToonPilot.unity + PlayerArtCapture `-toonPilotOut` |
| `0a8d1e3` | `feat(battle)`：放置类弹体接触阴影（`ProjectileContactShadow`，按 `IsPersistent` 判定） |
| `488cafc` | `feat(camera)`：恢复方位角环绕（创始人修正："固定俯视角但可旋转的平行投影"） |
| `c003a25` | `chore(render)`：SSAO 退役（Balanced 反装）+ 判据脚本 `tools/pixel-review/judge_toon_pilot.py` |
| `docs(索引)` | 上轮未提交文档波（技术文档四域分目录 + 立项任务书与四调研入库） |
| 本笔 | 真等距俯角 35.264° 落地（代码常量/基准方向/装配 offset/两处测试断言/试点场景）+ 本节 |

### 两条相机裁决与落地（**别把口径搞回 45°/方位锁定**）
- **俯角 = 真等距 35.264°**（渲染篇 §2.1 创始人裁决；原 45° 提案已废）。已落地：
  `BattleCameraController.OrthoPitchDegrees`、`OffsetDirectionForPitch`（**真等距基准方向**：
  水平分量 X/Z 等分 + 竖直 sinθ——yaw 0 = 等距基准方位 45°，不再是"面向 +Z"）、
  `M2BattleSceneSetup.CameraPitchDegrees` + `BattleCameraOffset`（30×(1,1,1)/√3 ≈ 17.32 三分量等分）、
  Battle 场景已重烘（装配链五步全绿）、两处断言改写（`CameraFeelRulesTests` 钉值 +
  `BattleSceneWiringTests` 场景自洽）。
- **方位可旋转**（本会话创始人修正）：右键拖拽只改 yaw（俯角无输入路径）；左键仍是瞄准。
  自由旋转下像素网格非轴向对齐的观感（是否 45° snap）留样张裁决。
- ⚠ **屏幕拖拽→世界 XZ 映射**（`LevelGeometry.ScreenDragToArenaDirection`）以相机朝向为基准，
  方位从 0° 变 45° 后**建议实玩复核**（本轮未动该函数）。

### ToonPilot 风格测试场景（用户指令的正式载体）
- 装配入口 `PirateCrew.EditorTools.ToonPilotSetup.BuildAll`（幂等；重跑覆盖材质参数 = 调色唯一入口）。
- 场景 `Assets/Scenes/ToonPilot.unity`（已进 Build Settings）；出图
  `PirateCrew3D.exe -toonPilotOut <目录>` → 5 张 1920 PNG（wide/mid/close/bands/ink 调试档）。
- 判据 `python tools/pixel-review/judge_toon_pilot.py <目录>`（块化比硬门禁/洋红/唯一色数/墨色覆盖）。
- **演进方向（用户指令）**：逐层加元素（船员 → 道具 → 纹理 → 光效 → UI），每层过审后固化，
  不污染 Battle 场景。
- 调色/线宽常量在 `ToonPilotSetup` 顶部材质色板 + `EnsureToonMaterial`。

### 三处未决（下轮首修，按序）
1. **岛台顶面缺失**：`IslandShellGeometry.BuildSolidShell` 在**列式模式**（`new TileTerrainGrid(w,h,blocks,0)`）
   下生成不出完整顶面——mid 图里规律的"深蓝方块"就是看穿到海面。修法＝改用平台模式构造（参照
   `ShowcaseLevels.BuildLogicGrid`）或读该函数的顶面分支。**船员悬空/落位也要一起复查**
   （`BlockWorldHeight = 0.5u` 而非 1u：3 块顶 = 1.5u，装配已按 `SurfaceWorldY` 修正）。
2. **船员实例不进画面**：资产（网格/材质）与场景引用齐备、`SpawnToonCrew` 无报错，但渲染不可见——
   最快是**编辑器里看层级/Inspector**（无头播放器已到盲区）。
3. **描边覆盖面**：现在**只在部分边缘出现**（close 图有、wide 无）。六轮实测的完整因果链：
   ① 海面 quad 朝向（`Euler(90,0,0)` 才让 Quad 正面朝上，写 -90 会本体被剔除、壳铺满屏）；
   ② 线宽量纲必须除 `|P.m11|`（clip 法线受投影缩放，直接乘 2/RT高 会随视场缩水 ~7 倍）；
   ③ `abs` 而非 `max`（D3D 下 m11 为负，钳位曾放大 5000 倍）；
   ④ 队列分离（海面材质 `renderQueue = 2000`、描边物 `Geometry+50`）——否则海面 fill 全屏盖掉壳环；
   ⑤ 壳先画（SRPDefaultUnlit 在 tag 列表首位）+ 本体后画盖回重叠区，故壳用 **ZTest Always**；
   ⑥ 45° 俯视下前缘轮廓外的地面比壳更近 → 壳沿视线拉近 5×线宽。
   **剩余缺口**：宽机位下壳体覆盖仍不稳定（wide 0%、mid/close 有）——**建议下一步开编辑器
   + Frame Debugger 会诊**（无头盲调收益已尽）；线宽实测 **1 RT px 经降采样不可见、2 px 起可见**
   （裁决 #3 的实测输入）。
4. （登记）**色阶对比**：抖动已默认关（`_DitherStrength=0`，参照图无有序抖动）；暗部/环境光/海面
   基调已压暗一档（沙暗 #4A4266、ambient #1A1E28）——**观感变化待复拍**。
5. （登记）**材质细节是相似度权重最大项**（参照比对估 40-45%）：顶点色 R 通道的逐格明暗偏移
   是零 shader 风险的先手，随后按资产篇上 32px/m 锁纹——见下节比对结论。

### 参照比对结论（创始人参照图 vs 试点图，2026-09-21）
Top3 行动：① 灭抖 + 重定暗部与环境光（已做一版，待复拍）；② **让船员真出现在画面并据此裁描边路线**
（反壳只能给外轮廓，参照图的线大半是**内部断口线**——要么裁决"内线交给纹理/顶点线"，要么立项
depth+normal 后处理描边）；③ 材质细节前移（顶点色逐格明暗 + 格缝 → 纹理）。
量化判据建议纳入 `judge_toon_pilot`：明度跨度 ≥40-250、明度<40 占比 ≥10%、饱和>0.5 占比 ≥10%、
4-邻域异色占比 <8%。

### ⚠ 并行会话竞态（本会话实测，复现 §31 教训）
本会话期间**有另一个会话在同一工作副本写美术翻新文档**（渲染篇/美术指南/资产管线/空间模型/
立项任务书 5 个文件在途）。本会话开工时 `git status` 干净，但工作期间渲染篇出现了新的
§2.1/真等距裁决段——**说明竞态发生在会话中期**。处置：本轮只写代码域 + `docs/项目/`，
**渲染篇等 5 个在途文档一律不碰**（回写留给其持有者）。**新会话务必先核对这 5 个文件的归属**。

### 门禁（本会话实测）
EditMode 1167 用例（1165 过 / 1 失败 / 1 跳过 → 失败为断言口径，已改写复跑）；PlayMode 7
（6 过 / 1 失败同源，已改写）；播放器 shader error 0（多轮）；装配链五步全绿。

---

## §34 场景 Prefab 化收口 + UI 像素化参照备齐（2026-09-22）——**新会话从这里接手**

### 现状一句话

A 桶（场景与装配）里"唯一剩下的结构性债务"——场景 Prefab 化——**做完了**：四个装配产物场景
折叠成"一个 Prefab 实例"（场景文件 38,034 → 784 行），Battle 的八步装配收成一条命令且顺序即代码，
折叠态与链顺序由 EditMode 契约测试冻结。UI 像素化（M4）从"只有方案"推进到"参照与几何口径齐备"，
**生成器尚未开写**。

### 本轮提交索引

| 提交 | 内容 |
| --- | --- |
| `d311519` | Battle 折叠为 `BattleRig.prefab` 实例（18,772 → 186 行） |
| `1b11eec` | 四个场景全部折叠 + `BattleScenePipeline` 装配链收口 + 两个契约测试 + 修 3 个真实缺陷 |
| `d089a69` | UI 像素化标准参照入库（两张 Terraria 截图片段）+ 逐像素量出的斜面几何 |
| `2824696` | 泰拉瑞亚 UI 原始件提取（XNB→PNG，41 件）+ XNB 实测布局与解码器选型 |
| `77eb008` | 补首选路线：wiki 直下成就图标 137/137 |

### 已定决策（不要重新论证）

1. **Prefab 是装配链的生成物，不是手改对象。** 要持久化手工调整就改生成器。
   这条与折叠前"手改场景会被 `NewScene` 洗掉"是同一件事，只是代价从一个 199 对象的场景
   缩到一个职责明确的 Prefab。
2. **装配步骤函数绝不终止进程。** 终止进程是顶层 CLI 入口的职责
   （`BattleLookupWiring.Wire` 曾因此在 batchmode 下把整条 ArtGate 掐断，症状是"日志戛然而止"）。
3. **"算不算 Prefab 覆盖"问引擎，不要自己在 YAML 里列白名单。**
   `HasPrefabInstanceAnyOverrides(root, includeDefaultOverrides: false)` 是唯一权威；
   文本白名单既会把等值默认项误报成覆盖，又看不出 TMP 那类"运行期解析后写回实例"的真覆盖。
4. **自绘件一律顶级类 + 独立文件**（Unity 不能序列化嵌套 MonoBehaviour：存不进 Prefab、
   场景重载后组件消失且不报错）。存量 11 处登记在 `ScriptAssetSerializabilityTests` 的棘轮里。
5. **MSAA 固定 0**（像素化裁决的硬项，不是性能取舍）。`BattleSceneLighting.ConfigureUrpAsset`
   过去写 2，等于**每跑一次资产管线就把裁决改回去**；已改成 0 并实测"重跑后资产无 diff"。
6. **UI 参照三件套**：创始人指定的两张截图片段（入库 `docs/images/ui-pixel-ref/`，
   并已量成可实现的几何）+ 137 张成就图标 + 41 张 UI 原始件（后两者在 `external/terraria-ref/`，
   版权原因不入库）。**UI 换装直接照 `ui-pixel-ref/README.md` 的几何表写生成器，不要重新猜。**

### 遗留 TODO（**不要当成已完成**）

| # | 事项 | 落点 |
| --- | --- | --- |
| 1 | **M4-4a Beveled Pixel 九宫格生成器**（令牌从调色板派生、按压 1px 位移 + 明暗对调） | 这是 UI 换装的阻塞项，参照与几何已齐备 |
| 2 | M4-4b 一屏试点（提议 Battle HUD）→ 出图过审 → 全界面扫荡 | 需人眼 |
| 3 | **UI 字体裁决**：中文像素字体（Fusion Pixel / Zpix）vs 保留 StickHand 混搭 | 需创始人裁决 |
| 4 | **`UI_*` 调色板槽定稿**：5 个里 3 个（`UI_PANEL`/`UI_BEVEL_HI`/`UI_BEVEL_LO`）仍是【提案】 | 需定稿后才能写生成器常数 |
| 5 | UI 脚本可序列化存量 11 处（两个同名 `ToastFader` 谁留谁删属设计决定） | `KnownOffenders` 棘轮逐条清 |
| 6 | `FontAssetBuilder` 重跑会换字体资产 GUID 并重排字形集 | 先查"删了重建"还是"就地重生成" |
| 7 | `SceneAssetManifestBuilder` 清单会随目录漂移（跑链才更新，平时静默落后） | 纳入门禁或接受它是快照 |
| 8 | 场景 Prefab 按模块拆成嵌套 Prefab（审计阶段 2b，有意未做） | 组织性收益，风险见审计报告 |
| 9 | `Achievements.xnb` 总表仍解不出（首帧失败） | **已无必要**——wiki 直下拿到同一批 137 张 |

### ⚠ 本机有别的会话/项目在跑

本轮实测：`Get-Process Unity` 见到一个 Unity 进程，命令行指向 **`transparent-pet-unity`**
（另一个项目，不是本仓）。所以看到"有 Unity 进程"**不等于**本工程有僵尸进程——
判断依据是命令行里的 `-projectPath`，不要凭进程数量就动手清理。

### 本轮踩坑记录（省下轮的时间）

1. **Agent/工具的产出要真跑才算数。** 我一度把 `ArtGate` 的链内缺口（第⑥步从未进链）判成
   "已修"——是整链重跑才把它暴露出来的。**"我以为"与"实测"之间的差距，就是这类重构的成本所在。**
2. **Unity YAML 里的 `m_Modifications` 不等于覆盖。** 判定问引擎（见上决策 3）。
3. **XNB 的 7 位变长整数是小端**（.NET `Read7BitEncodedInt` 口径）。按大端解会让后面所有字段
   整体错位，而开头仍然像文本，极难发现——详见
   [泰拉瑞亚参照资产提取.md](../技术/资产管线/泰拉瑞亚参照资产提取.md)。
4. **wiki 优先于解包**：能直下的素材不要自己解容器格式。成就图标 137/137 一把抓完，
   而我在 XNB 上绕了很久。

### 新会话恢复顺序

1. 读本文 §34 → `docs/项目/待办事项.md` → `AGENTS.md` 注意事项；
2. **改场景/装配链前**读 `pirate-crew/Assets/Scenes/README.md`（折叠态契约 + 八步链顺序）
   与 [架构总览](../技术/架构/架构总览.md) §7；
3. **做 UI 换装前**读 `docs/images/ui-pixel-ref/README.md`（几何表）+
   [美术风格指南](../设计/美术风格指南.md) §5 + 渲染管线-等距像素卡通 §1 四条红线；
4. 长任务（M4 是 2-3 会话量）上下文将满时按惯例更新本文。
