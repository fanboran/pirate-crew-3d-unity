# 待办事项

> 按 AGENTS 约定记录改进待办；按里程碑组织，做完的挪到底部归档区。
> 口径：M2 前不碰海战/大世界等远期系统；战斗模块以"补最小可玩闭环"为口径。
>
> 最近更新：2026-09-13（M2 Wave 1 完成，Wave 2 进行中）

## M2 战斗（补最小可玩闭环）——进行中

> 设计/数值依据：`docs/参考游戏逆向-海盗军团抢宝藏-静态.md`（回合机/投掷伤害公式/17 武器/AI 伪代码/33 关数据），
> 而非 Godot 版 `config/*.json`——后者是离线规格（`config_manager.gd:19` 读 `*.cfg` 而目录只有 `*.json`，从未被运行时加载）。
>
> **执行方式**：Wave 1 五个文件域互不重叠的 subagent 并行，纯逻辑用无头验证台（`external/m2-harness/`，
> 不启动 Unity 跑 NUnit）自验；Wave 2 上 MonoBehaviour 层与场景组装，Unity batchmode 收口（Library 锁独占，一次一个进程）。

### Wave 1：数值与纯逻辑地基（已完成 2026-09-13）

- [x] **W1-A Unity 参照库调研**（`4e3a814`）→ `docs/M2-Unity参照库调研.md`（557 行）+ `external/m2-combat-reference/` 4 个参照库
  - angry-birds-slingshot(679★) / urp-outlines(717★) / rts-camera-cinemachine(162★, 与本工程同为 2022.3 + Cinemachine 2.9.7) / trajectory-dots(28★)，均 MIT
  - API 风险 R1-R9 逐条本地核实：`Rigidbody.velocity` 可用而 `linearVelocity` 在 2022.3 命中 0 次；
    Cinemachine 2.9.7 只有 `CinemachineVirtualCamera`；`LineRenderer.SetVertexCount` 是遗留 API
- [x] **W1-B Godot 基准摘要**（`22a565a`）→ `docs/M2-Godot基准摘要.md`（387 行）
  - 计数勘误：是 26 个 `.gd` + 3 个描边 `.gdshader`（不是 29 个 `.gd`）
  - 47 处空 TODO/未接线；三项关键论断已 grep 证实：`ai_controller.gd:17` AI 全空、
    `turn_manager.gd:40` 的 `end_turn()` 零调用方（回合永不推进）、config 从未加载
  - 数值差异：火药桶三方不一致（config 35/2.0 ≠ 代码 60/20/4.0 ≠ Flash 150/30）；
    投掷/重力四套数并存导致预览≠实弹 → M2 统一取 Flash 基准、单一真值来源
- [x] **W1-C 数值层**（`bcc6241`/`3e9b5e8`/`c098046`）→ `Scripts/PirateCrew/Data/` + `Assets/Editor/M2DataAssetGenerator.cs` + `Tests/Data/`
  - 17 武器全集（Godot 版 6 种只是子集）、船员共享属性（全体 100 HP / weight 1 / 无差异）、16 个平衡常数、3 个代表关
  - 三层：纯 C# `WeaponCatalog`/`CrewCatalog`/`LevelCatalog`（真值来源）→ SO 定义类 → Editor 生成器
  - 已验证：42 条测试独立复跑绿；17 武器表逐行与逆向文档核对无一处抄错；
    batchmode 生成 48 个 `.asset`（17 武器 / 27 船员 / 3 关卡 / 1 平衡）
- [x] **W1-D 战斗纯逻辑**（`b0a0cef`）→ `Scripts/PirateCrew/Combat/`（Ballistics / ExplosionResolver / TurnRules / ScoreRules / WeaponTriggerRules）+ `Tests/Combat/`
  - 弹弓 `-0.25` 反号 + 等比限速、满力拖距 80/120px、15 段预测、爆炸 `maxDamage×(1-d/半径)` + 击退 `5k/-6k`
  - `d==0` 显式处置（falloff=1、方向取零向量，避免原版 `dx/d` 得 NaN 污染物理）
  - 已验证：86 条测试独立复跑绿；**变异测试 3/3 抓到**（改 ExtraLift 6→5 挂 4 条、改撞墙系数 -0.4→-2 挂 2 条、严格大于改大于等于挂 3 条）
- [x] **W1-F 选中描边**（`0d72678`）→ `Assets/Art/Shaders/PirateOutline{,Post}.shader` + `Scripts/PirateCrew/Rendering/` + `Assets/Editor/OutlineDebugCapture.cs` + `docs/描边Shader调试.md`
  - 参照 Godot 版**真实接线**的实现：hover 走 inverted hull（淡白/0.0025/实线）、selected 走全屏后处理（青/0.006/流动虚线）
  - `_DebugMode` 0-4 五档，每档在分支处写明预期画面；后处理走 mask Sobel（因本工程 URP 资产 `m_RequireDepthTexture: 0`，深度法线路线会输出死色）
  - 已验证：全部 `#include` 落地核对存在；两个坑已规避（core 14.0.12 无 `ShaderLibrary/Blit.hlsl`，正确路径 `Runtime/Utilities/Blit.hlsl`；core 无 `ComputeScreenPos`）
  - 新增 `PirateCrew.Rendering.asmdef`（`PirateCrew.Runtime.asmdef` references 为空，引用不到 URP 类型；隔离比污染游戏程序集干净），引用名已在 `Library/ScriptAssemblies` 核实存在

### Wave 2：MonoBehaviour 层与场景（进行中）

- [ ] **W2-E 战斗组装层**：`Scripts/PirateCrew/Battle/` —— 船员实体 / 回合驱动 / 投掷瞄准 / 轨迹预览 / Cinemachine 相机 / BattleController 结算
  - 纯逻辑继续下沉（`LevelGeometry`/`BattleFlowRules`/`WeaponInventory` 可无头测），MonoBehaviour 只做薄壳
  - 四个 3D 化决策须显式交代：px→单位换算、y 轴翻转（注意 `ExplosionResolver.deltaVy` 是 Flash 约定算出的）、重力与力度换算、落水即死取全局规则
- [ ] **W2-G Battle 场景组装 + ugui HUD + AI**：替换 M1 占位场景（走 Editor 脚本，不手写 .unity YAML）；
  HUD 回合提示/武器面板/小地图；AI 按逆向 §6 自建（时间片评估 + 50 次自抛模拟 + luck 加权 + evilness，取 success 最大）
  —— Godot 侧 `ai_controller.gd` 是空的，这块必须自己写
- [ ] **W2-H 收口**：batchmode 编译 + EditMode/PlayMode 全绿 + 新脚本 meta + 归档

### M2 遗留项（本轮不做，记此不丢）

- [ ] **RendererFeature 脚本化挂载**：`OutlineRendererFeature` 需手工加到 URP Renderer 资产才能生效；
  当前只有代码，未挂载（应写个 Editor 脚本自动挂，避免依赖手工步骤）
- [ ] **描边调试截图补跑**：见下方环境限制，需有图形界面的编辑器会话
- [ ] **`export/` 入库策略待定**：Godot 版把 `export/battle-debug` 入库了，Unity 侧 `export/` 尚未创建且不在 `.gitignore`；
  补跑截图前先定"截图是否入库"（若入库注意体积，若不入库须加 `.gitignore`）

> ⚠ **图形学调试截图规范的执行限制**：本环境 Unity 无 GPU 可用（不带 `-nographics` 会卡在 GfxDevice 创建），
> `Camera.Render` / `ScreenCapture.CaptureScreenshot` 拿不到真实像素。因此 M2 的描边 shader 只能做到
> **代码级交付**（shader + `debug_mode` 分层 + 采集脚本 + "每张图应看到什么"的文档），
> 实际逐层截图待有 GPU 的编辑器会话补跑 `export/<功能>-debug/`。**不要把"没截图"当作已完成。**


## M3 船员管理循环

- [ ] **Roster / Progression**（附带 NUnit 测试）
- [ ] **CampaignManager + 关卡选择**：翻译 `campaign_manager.gd`（3 章 × 5 关）+ 关卡选择 UI
- [ ] **闭环**：招募 → 编成 → 出战 → 结算
- [ ] **存档接通**：`SaveManager`（M1 已就绪）对接管理循环进度/名册

## 参考线（并行）

- [x] **2D 原版 Flash 游戏逆向**（静态 + 动态）→ `docs/参考游戏逆向-海盗军团抢宝藏-静态.md`（868 行全稿）+ 动态篇（`f06a38e`）
- [ ] **动态试玩收尾**（可选）：1P/2P 已打通但 game 帧角色交互受 Ruffle `tt` 覆盖层阻塞；若后续需要动态验证，试 Ruffle 新版本或桌面 Flash 播放器

## 工程基建（随时可做）

- [x] **无头验证台**（`external/m2-harness/`，不入库）：用 `dotnet` 直接引用 Unity 已编译程序集 + NuGet NUnit，
  **不启动 Unity** 就能编译工程源码并跑纯 C# 测试。解决"M2 多 agent 并行时抢 Library 独占锁"的瓶颈。
  - 能力边界：`GameObject`/`MonoBehaviour`/`ScriptableObject` 实例化走原生 `ECall`，脱离 Unity 必崩
    （实测 44 条 EditMode 中 13 条纯 C# 通过、31 条因 `new GameObject()` 失败——**这是固有边界，不是回归**）。
  - 用法：`cp external/m2-harness/M2Harness.csproj external/harness-<域>/ && dotnet test M2Harness.csproj -p:HarnessScope=<All|Data|Combat>`
  - 最终门禁仍是 Unity batchmode（编译 + EditMode/PlayMode），验证台只是快速内循环。
- [ ] **TextMeshPro 接入**：manifest 加 `com.unity.textmeshpro`，替换 M1 的 legacy `UnityEngine.UI.Text`（`SceneSetup.cs` / `MainMenuController.cs` 已标 TODO）
  - 注：TMP 在 Unity 2022.3 的 ugui 1.0.0 里**没有**（2023.2+/Unity 6 才并入 ugui），需单独装包
- [ ] **CI**：GitHub Actions + batchmode EditMode/PlayMode 测试（参考姊妹项目 stick-world 的 CI-DI 文档）

---

## 归档区

### M1 主菜单 + 场景流转 + 存档骨架（已完成 2026-09-12）

- [x] **EventBus** 静态事件中心——翻译 `event_bus.gd`（`db7abbf`，12 条测试）
- [x] **SceneLoader** 异步场景流转（淡入淡出 + 返回栈 + 重入保护）——翻译 `scene_manager.gd`（`96406a1`，7 条测试）
- [x] **SaveManager** 多槽位存档 + 合并 `data_manager.gd` KV（`.bak` 回滚健壮性）（`a77bd1d`，20 条测试）
- [x] **Bootstrapper** 组装根 + `SceneNames` 常量（`9a5c8a2`）
- [x] **MainMenu / Battle 占位场景** + `SceneSetup.BuildAll()` 一键重建（`9a5c8a2` 脚本、`a9586c6` 场景落地）
- [x] **M1 全量验证**：编译 0 错误、EditMode **44/44 绿**、PlayMode 端到端流转测试通过（Bootstrapper→MainMenu→Battle→go_back，`2b65e67`）
- [x] 工程副产物：meta、BattleGround.mat、SceneTemplateSettings（`6283057`、`668f2c9`）

**M1 沉淀的环境经验**（详见 AGENTS.md 注意事项）：
- batchmode 命令必须显式带 `-projectPath`（缺失会打开 EditorPrefs 最近工程，本次曾导致僵尸进程锁工程）+ 当前环境统一加 `-nographics`
- EditMode 测试 asmdef 必须 `includePlatforms:["Editor"]`；PlayMode 测试 asmdef 留空（全平台）；程序集引用**只写 GUID 或只写 name**，双写会 duplicate
- `SceneLoader` 契约：过渡（含淡入淡出）期间的新切场景请求会被忽略——调用方/测试需等 `IsLoading == false`

### M0 骨架（已完成）

- [x] **URP 落地**：装包 + 管线资产 + Graphics/Quality 挂载（`b807689`）
- [x] **asmdef + UTF 接入**：`PirateCrew.Runtime` / `PirateCrew.Tests` 程序集 + 冒烟测试跑通（`8616e29`）
- [x] **ProjectSettings 定名**：productName `Pirate Crew 3D`、companyName `BoranFan`（`e23f7e9`）
- [x] 顶层目录结构：Unity 工程本体入 `pirate-crew/` 子目录（`62571f4` 起）
