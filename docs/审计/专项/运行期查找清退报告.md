# 运行期查找清退报告

> **这是什么**：`Assets/Scripts/**` 下 `GameObject.Find` / `FindObjectOfType` / `FindObjectsOfType` / `FindWithTag` 存量的**逐条登记表**与防回归闸门说明。
> 由 `Assets/Editor/SceneLookupAudit.cs` 扫描生成（唯一真源是脚本里的白名单表），每次改动后重跑即刷新。
> 相关：场景装配债诊断见 [场景接线审计报告](场景接线审计报告.md)（F-3/F-4/F-5）。

## 怎么复跑

```bash
U="F:/Unity/2022.3.62f1c1/Editor/Unity.exe"
P="F:/VSCode/pirate-crew-3d-unity/pirate-crew"

# 扫描 + 写本报告；有未登记命中则退出码 1（CI 用）
"$U" -batchmode -nographics -quit -projectPath "$P" \
  -executeMethod PirateCrew.EditorTools.SceneLookupAudit.AuditFromCommandLine -logFile -
```

编辑器菜单同义入口：`PirateCrew/审计/运行期查找清退报告`。

## 一、汇总

| 指标 | 数值 |
| --- | --- |
| 清退前基线（2026-09-21 实测调用点） | 11 |
| 当前调用点 | 7 |
| 其中已登记（运行时/工具） | 7 |
| **未登记（闸门失败）** | **0** |
| 注释与字符串提及（不是调用点） | 23 |
| 白名单僵尸项 | 0 |

## 二、前后对比（逐条）

| 清退前（file:line · 调用） | 处置 | 现在 |
| --- | --- | --- |
| `PirateCrew/ArtReview/PlayerArtCapture.cs:303 · FindObjectsByType` | 保留并登记：出图工具路径（命令行模式），运行时零调用 | 已清退（无命中） |
| `PirateCrew/ArtReview/PlayerArtCapture.cs:574 · FindObjectsOfType` | 保留并登记：出图工具路径（命令行模式），运行时零调用 | 已清退（无命中） |
| `PirateCrew/Audio/AudioService.cs:1023 · FindObjectOfType` | 保留并登记：能力探测（非依赖注入），限频 + 负缓存，审计 F-5 建议保留 | 已清退（无命中） |
| `PirateCrew/Battle/BattleCameraController.cs:404 · FindObjectOfType` | 清退：改为 `[SerializeField] aimThrow`（装配注入）+ Awake 一次性兜底（不在热路径） | 已清退（无命中） |
| `PirateCrew/Battle/BattleCameraController.cs:419 · FindObjectOfType` | 清退：改为 `[SerializeField] aimThrow`（装配注入）+ Awake 一次性兜底（不在热路径） | 已清退（无命中） |
| `PirateCrew/Battle/BattleCameraController.cs:1117 · FindObjectOfType` | 清退：改为 `[SerializeField] aimThrow`（装配注入）+ Awake 一次性兜底（不在热路径） | 已清退（无命中） |
| `PirateCrew/Battle/BattleController.cs:360 · FindObjectOfType` | **不属本轨道**（BattleController.cs 归数据外化轨道）：待其接线 AmbientDirector | 已清退（无命中） |
| `PirateCrew/Fx/FxRoot.cs:143 · FindObjectOfType` | 收敛并登记：每局一次（非每帧）的类型获取，白名单有理由 | 已清退（无命中） |
| `PirateCrew/Fx/FxRoot.cs:293 · FindObjectsOfType` | 删除：死代码（battle_started 的发布者必然激活，回落扫描拿不到任何对象） | 已清退（无命中） |
| `PirateCrew/Water/WaterSimulationDriver.cs:457 · FindObjectsOfType` | 清退：改为 `[SerializeField] sunLight`（装配注入，来源 AmbientDirector.sunLight） | 已清退（无命中） |
| `UI/BattleHud.cs:403 · FindObjectOfType` | 清退：改为 `[SerializeField] cameraController`（装配注入）+ Awake 一次性兜底 | 已清退（无命中） |

## 三、当前调用点逐条

| # | 位置 | 分类 | 调用 | 允许 | 理由 / 待办 |
| --- | --- | --- | --- | --- | --- |
| 1 | `pirate-crew/Assets/Scripts/UI/BattleHud.cs:363` | 运行时路径 | `FindObjectOfType<BattleCameraController>` | 是（白名单） | **一次性装配兜底**（Awake 里跑一次；旧写法在每次 SetHudMode 都可能扫一次，已删）：理由同 BattleCameraController——注入优先、兜底只为不静默，装配完整性由 CameraControllerWiredByAssembly（PlayMode 测试断言）钉住。 |
| 2 | `pirate-crew/Assets/Scripts/PirateCrew/ArtReview/PlayerArtCapture.cs:303` | 工具路径 | `FindObjectsByType<MeshRenderer>` | 是（白名单） | 出图工具路径（-artReviewOut 命令行模式）：按包围盒挑要出镜的网格，被挑对象没有持有者可以注入。运行时零调用。 |
| 3 | `pirate-crew/Assets/Scripts/PirateCrew/ArtReview/PlayerArtCapture.cs:574` | 工具路径 | `FindObjectsOfType<MeshRenderer>` | 是（白名单） | 出图工具路径（-artReviewOut 命令行模式）：要在任意手工场景里量天际线，被测量对象没有持有者可以注入；改成显式引用会让命令行工具模式复杂化。运行时零调用。 |
| 4 | `pirate-crew/Assets/Scripts/PirateCrew/Audio/AudioService.cs:1023` | 运行时路径 | `FindObjectOfType<AudioListener>` | 是（白名单） | 能力探测而非依赖注入：问的是"本场景有没有监听器"，没有持有者可以注入。已限频（ListenerProbeRetrySeconds=1s）+ 负缓存，命中一次后永久短路；换成登记制需要 5 个场景（含手摆的菜单场景）都加登记组件，属装配轨道范围（审计 F-5 亦建议保留）。 |
| 5 | `pirate-crew/Assets/Scripts/PirateCrew/Battle/BattleCameraController.cs:386` | 运行时路径 | `FindObjectOfType<AimThrowController>` | 是（白名单） | **一次性装配兜底**（Awake 里跑一次，HotPath 的 Update/LateUpdate 轮询已彻底删除）：场景是装配脚本的产物，未跑 BattleLookupWiring.Wire 的旧场景该字段为空。兜底把"静默失效"降级成"可用但吵闹"，而真正的装配缺陷由 AimThrowWiredByAssembly（PlayMode 测试断言）失败暴露——注入是正路，兜底只是防静默。 |
| 6 | `pirate-crew/Assets/Scripts/PirateCrew/Battle/BattleController.cs:361` | 运行时路径 | `FindObjectOfType<Ambient.AmbientDirector>` | 是（白名单） | **不属本轨道**：BattleController.cs 归数据外化轨道（同一分支并行在改）。修法已由审计 F-4 定案（加 [SerializeField] AmbientDirector 并在装配期写入），登记在此以保证闸门可用，清账由该轨道完成。 |
| 7 | `pirate-crew/Assets/Scripts/PirateCrew/Fx/FxRoot.cs:167` | 运行时路径 | `FindObjectOfType<BattleController>` | 是（白名单） | FxRoot 由组合根在 BeforeSceneLoad 创建并 DontDestroyOnLoad（进程级常驻，无场景可接线），而它要的是场景作用域的 BattleController 实例；battle_started 载荷不带该实例。已收敛为每局一次（只在该事件回调里），失败会打告警。彻底消除需给载荷加字段（破坏性事件契约变更，归事件/数据轨道）。 |

## 四、白名单登记表（脚本内 `SceneLookupAudit.Whitelist`，键 = 路径 + 调用片段）

| 文件 | 登记时行号 | 调用 | 分类 | 理由 |
| --- | --- | --- | --- | --- |
| `PirateCrew/Audio/AudioService.cs` | 1023 | `FindObjectOfType<AudioListener>` | 运行时路径 | 能力探测而非依赖注入：问的是"本场景有没有监听器"，没有持有者可以注入。已限频（ListenerProbeRetrySeconds=1s）+ 负缓存，命中一次后永久短路；换成登记制需要 5 个场景（含手摆的菜单场景）都加登记组件，属装配轨道范围（审计 F-5 亦建议保留）。 |
| `PirateCrew/Battle/BattleCameraController.cs` | 386 | `FindObjectOfType<AimThrowController>` | 运行时路径 | **一次性装配兜底**（Awake 里跑一次，HotPath 的 Update/LateUpdate 轮询已彻底删除）：场景是装配脚本的产物，未跑 BattleLookupWiring.Wire 的旧场景该字段为空。兜底把"静默失效"降级成"可用但吵闹"，而真正的装配缺陷由 AimThrowWiredByAssembly（PlayMode 测试断言）失败暴露——注入是正路，兜底只是防静默。 |
| `PirateCrew/Battle/BattleController.cs` | 360 | `FindObjectOfType<Ambient.AmbientDirector>` | 运行时路径 | **不属本轨道**：BattleController.cs 归数据外化轨道（同一分支并行在改）。修法已由审计 F-4 定案（加 [SerializeField] AmbientDirector 并在装配期写入），登记在此以保证闸门可用，清账由该轨道完成。 |
| `PirateCrew/Fx/FxRoot.cs` | 167 | `FindObjectOfType<BattleController>` | 运行时路径 | FxRoot 由组合根在 BeforeSceneLoad 创建并 DontDestroyOnLoad（进程级常驻，无场景可接线），而它要的是场景作用域的 BattleController 实例；battle_started 载荷不带该实例。已收敛为每局一次（只在该事件回调里），失败会打告警。彻底消除需给载荷加字段（破坏性事件契约变更，归事件/数据轨道）。 |
| `UI/BattleHud.cs` | 363 | `FindObjectOfType<BattleCameraController>` | 运行时路径 | **一次性装配兜底**（Awake 里跑一次；旧写法在每次 SetHudMode 都可能扫一次，已删）：理由同 BattleCameraController——注入优先、兜底只为不静默，装配完整性由 CameraControllerWiredByAssembly（PlayMode 测试断言）钉住。 |
| `PirateCrew/ArtReview/PlayerArtCapture.cs` | 303 | `FindObjectsByType<MeshRenderer>` | 工具路径 | 出图工具路径（-artReviewOut 命令行模式）：按包围盒挑要出镜的网格，被挑对象没有持有者可以注入。运行时零调用。 |
| `PirateCrew/ArtReview/PlayerArtCapture.cs` | 574 | `FindObjectsOfType<MeshRenderer>` | 工具路径 | 出图工具路径（-artReviewOut 命令行模式）：要在任意手工场景里量天际线，被测量对象没有持有者可以注入；改成显式引用会让命令行工具模式复杂化。运行时零调用。 |

## 五、注释与字符串里的提及（不是调用点，列出以正视听）

这些行只是**文字提到**了查找 API（多为「本类不做 GameObject.Find」这类声明式注释、或 Tooltip 文案）。扫描器跳过它们，计数不计入调用点——避免「文档写了禁令反而被判违规」。

| # | 位置 | 文本性质 |
| --- | --- | --- |
| 1 | `pirate-crew/Assets/Scripts/UI/BattleHud.cs:32` | 注释文本（不是调用点） |
| 2 | `pirate-crew/Assets/Scripts/UI/BattleHud.cs:351` | 注释文本（不是调用点） |
| 3 | `pirate-crew/Assets/Scripts/UI/BattleMinimap.cs:19` | 注释文本（不是调用点） |
| 4 | `pirate-crew/Assets/Scripts/UI/BattleMinimap.cs:39` | 字符串/属性文本（文档提及） |
| 5 | `pirate-crew/Assets/Scripts/PirateCrew/Ambient/AmbientDirector.cs:24` | 注释文本（不是调用点） |
| 6 | `pirate-crew/Assets/Scripts/PirateCrew/Ambient/AmbientDirector.cs:24` | 注释文本（不是调用点） |
| 7 | `pirate-crew/Assets/Scripts/PirateCrew/Ambient/AmbientWindBinder.cs:30` | 注释文本（不是调用点） |
| 8 | `pirate-crew/Assets/Scripts/PirateCrew/Ambient/CrabAgent.cs:16` | 注释文本（不是调用点） |
| 9 | `pirate-crew/Assets/Scripts/PirateCrew/Ambient/CrabAgent.cs:16` | 注释文本（不是调用点） |
| 10 | `pirate-crew/Assets/Scripts/PirateCrew/ArtReview/PlayerArtCapture.cs:18` | 注释文本（不是调用点） |
| 11 | `pirate-crew/Assets/Scripts/PirateCrew/Audio/AudioService.cs:150` | 注释文本（不是调用点） |
| 12 | `pirate-crew/Assets/Scripts/PirateCrew/Audio/AudioService.cs:1010` | 注释文本（不是调用点） |
| 13 | `pirate-crew/Assets/Scripts/PirateCrew/Battle/BattleCameraController.cs:372` | 注释文本（不是调用点） |
| 14 | `pirate-crew/Assets/Scripts/PirateCrew/Battle/BattleCameraController.cs:442` | 注释文本（不是调用点） |
| 15 | `pirate-crew/Assets/Scripts/PirateCrew/Battle/BattleCameraController.cs:1396` | 注释文本（不是调用点） |
| 16 | `pirate-crew/Assets/Scripts/PirateCrew/Battle/BattleController.cs:309` | 注释文本（不是调用点） |
| 17 | `pirate-crew/Assets/Scripts/PirateCrew/Fx/FxBootstrap.cs:21` | 注释文本（不是调用点） |
| 18 | `pirate-crew/Assets/Scripts/PirateCrew/Fx/FxRoot.cs:27` | 注释文本（不是调用点） |
| 19 | `pirate-crew/Assets/Scripts/PirateCrew/Fx/FxRoot.cs:305` | 注释文本（不是调用点） |
| 20 | `pirate-crew/Assets/Scripts/PirateCrew/Fx/FxRoot.cs:307` | 注释文本（不是调用点） |
| 21 | `pirate-crew/Assets/Scripts/PirateCrew/SceneKitPilot/SceneKitPilotCapture.cs:18` | 注释文本（不是调用点） |
| 22 | `pirate-crew/Assets/Scripts/PirateCrew/SceneKitPilot/SceneKitPilotCapture.cs:123` | 注释文本（不是调用点） |
| 23 | `pirate-crew/Assets/Scripts/PirateCrew/Water/WaterSimulationDriver.cs:428` | 注释文本（不是调用点） |

## 六、闸门语义

- **存量可见、新增禁止**：只有 `Whitelist` 里带理由的命中被允许；其余一律非 0 退出。
- **键是 file + 调用片段，不是行号**：行号会随无关改动漂移，用它做键会让闸门变成噪音源；登记时的行号只作文档（本报告第三节打印扫描时的实时行号）。
- **逃逸写法也在扫描范围**：`FindAnyObjectByType` / `FindObjectsByType`（新 API）同样命中——否则「把旧 API 换成新 API」就能绕过闸门。
- **扫描域是 `Assets/Scripts/`**：`Assets/Editor/` 下的装配/审计脚本（含本文件白名单表）不扫，它们按设计要按名字/按类型在编辑器里找对象。

