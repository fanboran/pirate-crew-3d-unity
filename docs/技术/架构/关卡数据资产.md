# 关卡数据资产

> **本文是什么**：关卡（海图 + 非海图战斗场）数据外化之后的数据模型、目录约定、改数据流程，
> 以及迁移器 / 校验器 / golden JSON 三件工具的用法与边界。
> 想了解为什么做这次外化，读 [工业级重构总纲](../../项目/工业级重构总纲.md) §3.4 与 §5；
> 想了解关卡怎么设计，读 [关卡制作管线](../../设计/关卡/关卡制作管线.md) 与各关设计文档。
>
> **一句话**：关卡数据是 `Assets/Data/**` 下的 ScriptableObject 资产，代码里没有第二份；
> `_golden/*.json` 是同一份数据的文本渲染，逐字节对拍，既是回归锚点也是 diff 面。

---

## 1. 数据模型

### 1.1 三层结构

```
关卡载荷（纯 C# 可序列化）          资产宿主（ScriptableObject）        运行时视图
LevelAssetPayload            ←→  LevelDefinition             →    LevelData + TileTerrainGrid
WorldMapAssetPayload         ←→  WorldMapDefinitionAsset     →    WorldMapDefinition（Battle.WorldMaps）
LevelCatalog（清单）             ←→  —                           →    LevelAssetLibrary（查表口）
```

- **载荷与宿主分家**（`Scripts/PirateCrew/Data/Levels/LevelAssetTypes.cs`）：
  字段真源放在纯 C# 载荷上，SO 只有一个 `data` 字段（`LevelDefinition.cs:23`）。
  理由是判据"资产内容 == golden JSON"必须在**无头环境**能算，而 SO 实例化在无头验证台走原生
  ECall 必崩（见 `external/m2-harness/README.md`「能力边界」）。载荷可序列化 ⇒ JSON / Unity YAML /
  运行时三处渲染同一份字段，不存在"两套模型"。
- **运行时视图是既有类型**：`LevelData`（`Scripts/PirateCrew/Data/LevelData.cs:16`）与
  `WorldMapDefinition`（`Scripts/PirateCrew/Battle/WorldMaps/WorldMapKit.cs:90`）语义未变——
  纯 C# 规则层（连通性 / 栅格化 / 远景环 / 海图装填）继续只认后者，于是"数据换载体"与"规则不变"彻底解耦。
  载荷 → 运行时定义的转换只有 `WorldMapFromAsset.ToRuntime`（`…/WorldMaps/WorldMapFromAsset.cs:24`）
  与 `LevelAssetPayload.ToLevelData`（`…/Levels/LevelAssetTypes.cs`）两处，逐字段直搬、不做"顺手修一下"。

### 1.2 `LevelAssetPayload`（非海图战斗场）

| 字段 | 语义 | 单位 / 取值 |
| --- | --- | --- |
| `levelNumber` | 关卡序号（选关 / 结算 / 得分口径） | 样板关 1–3 |
| `assetName` | 资产 ASCII 名（`.asset` 文件名 / `m_Name` / JSON 文件名） | 如 `cloud_walk` |
| `displayName` | 展示名（选关页与结算） | 中文，如「云端漫步」 |
| `widthTiles` / `depthTiles` | 场地格子尺寸 | 逻辑格；样板关 20×15 |
| `originalXmlPlayers` | 原版 XML `players` 属性（仅存档备查） | 1 / 2 |
| `waterTileY` | 逻辑水面行 | `LevelData.WaterY = waterTileY × 32`（§5.5 口径） |
| `maxChests` / `sourceXmlMaxChests` | 宝箱占位口径 / 原版原值（仅存档备查） | 恒 3 |
| `airdropPool` | 空投武器池 | `WeaponStack[]`，`count = 10` 表示无限 |
| `units` | 双方出战单位（顺序 = 实例化顺序） | `LevelUnit[]`：符号 / 队伍 / 格 / luck / 初始武器 |
| `terrain` | **逻辑高度场**（见 §1.4） | `TerrainRaster` |
| `bakedPieces` | 烘焙陈设摆位（件 id + 世界摆位） | `ShowcasePieceId` 整数值 + instanceName |

### 1.3 `WorldMapAssetPayload`（大海域海图）

| 字段 | 语义 |
| --- | --- |
| `id` | 地图 id（选关 / 战役结算键，如 `wreck_hymn`） |
| `displayName` / `levelNumber` | 中文名 / 关卡号（海图占 101–108 段） |
| `spanX` / `spanZ` | 图幅（米）；水模拟域与海面外扩按它算 |
| `ambientTier` | 氛围档 `Noon` / `Dusk` / `Storm`（对接 AmbientTimeOfDayCatalog） |
| `crewWeapons` / `captainWeapons` | 分层军火：船员近程基线 / 船长含 ≥1 件全图级旗舰（方案 D） |
| `terrain` / `horizon` / `props` | 摆位表（kit + 资产 + x/y/z + yaw）；站面 box 由 kit 资产自己带 |
| `spawns` | 出生点（世界坐标 + 队伍 + 符号 + luck） |
| `airdropPool` | 空投池 |
| `horizonSeed` / `horizonFeatures` | 远景环随机种子 / 本图专属远景特征件 |

摆位表用**扁平字段**（`x` / `y` / `z` / `yawDeg`）而不是嵌套 `Vector3`：YAML 每行一个数，
diff 里改一个坐标就是改一行（见 §5.3 的理由）。

### 1.4 逻辑高度场：单一栅格语义

`TerrainRaster`（`LevelAssetTypes.cs`）= `widthTiles` × `depthTiles` × `blockWorldHeight` × `blocks[]`
（**行主序** `blocks[x + y × widthTiles]`，`0` = 该格无块）。

- 这是**唯一**的栅格语义。降落点是 `TileTerrainGrid` 的**列式**分支：
  `LevelRasterFromAsset.Build`（`Scripts/PirateCrew/Battle/Levels/LevelRasterFromAsset.cs:20`）。
- `TileTerrainGrid` 还留着平台簇模式（逐格带簇归属、水格无地面）——那是一代"悬空平台群"的形态，
  **现役内容已无生产者**；资产里不表达它（见 `Battle/Terrain/TileTerrainGrid.cs` 类头第三节的两种构造形态）。
- 海图那条线的栅格是**派生**的：站面 box（kit 资产的 standable 表）→ `WorldMapRules.TryRasterize`
  → 同一形态的列式栅格（`Battle/WorldMaps/WorldMapRuntime.cs`）。
  它不落资产：海图栅格 75×75 到 140×140 格，落进 YAML 是每图上万行整数，
  反而把 diff 面毁掉；而它的唯一真源本就是 Blender 站面表（改站面必须重烘 kit 资产）。
  **为什么样板关的栅格落资产**：手摆高度场没有可派生的上游，不落资产就得回到"数据即代码"。
- **块高**（`blockWorldHeight`）存进资产是为了让资产自解释：尺度常量改了，
  校验器会指出哪份资产还没跟上（`LevelAssetRules.cs` 比对 `LevelGeometry.BlockWorldHeight`）。

---

## 2. 目录与文件约定

```
Assets/Data/
├── Levels/
│   ├── cloud_walk.asset            ← 关卡资产（唯一真源）
│   ├── islet_rain.asset
│   ├── sky_island.asset
│   ├── _golden/
│   │   ├── cloud_walk.json         ← 文本锚点（评审面 + 迁移器输入 + 对拍基准）
│   │   ├── islet_rain.json
│   │   └── sky_island.json
│   └── Resources/
│       └── LevelCatalog.asset      ← 唯一加载清单（Resources 路径 `LevelCatalog`）
└── WorldMaps/
    ├── wreck_hymn.asset … sunken_gate.asset        （8 张）
    └── _golden/
        └── wreck_hymn.json … sunken_gate.json      （8 份）
```

- **资产命名 = `assetName`（关卡）/ `id`（海图）**：资产文件名、`m_Name`、golden JSON 文件名三者同源，
  迁移器不靠外部约定猜文件名。
- **清单必须落在 `Resources/` 下**：播放器构建里 `Assets/` 不能按路径加载（非 Resources / Addressables）。
  清单持有全部资产引用，Unity 因此把它们一并打进包；`LevelAssetLibrary` 用
  `Resources.Load("LevelCatalog")` 取它（`LevelAssetSchema.CatalogResourcePath`，`LevelAssetTypes.cs:40`）。
  新增关卡漏登记 = 运行时加载不到，由内容门禁报红。
- **`.asset` 的 `m_Script` guid 由资产路径确定性派生**（`LevelAssetYaml.GuidFor`）：
  生成器 / 迁移器 / 对拍测试三处共用同一个派生式，靠"手工抄 guid"必然漂移。
  连带约束：`.cs.meta` 必须与生成时一致，**不要手改这两个脚本文件的 guid**
  （`LevelDefinition.cs` / `WorldMapDefinitionAsset.cs` / `LevelCatalog.cs`）。

---

## 3. golden JSON：作用与更新流程

**它是同一份数据的文本渲染，不是第二真源。** 三个用途：

1. **回归锚点**：`Assets/Tests/Battle/LevelAssetTests.cs` 断言
   `Assets_MatchGoldenJson_ByteForByte`——提交进仓库的 `.asset` 正文 == golden JSON 经
   `LevelAssetYaml` 渲染的正文（逐字节，仅行尾归一化）。即使有人手改了资产、或 Unity 版本换了序列化写法，
   这条判据都会立刻红。
2. **评审面**：资产 YAML 的整数栅格是 Unity 的"每行一个整数"写法（300 行），人读很痛苦；
   golden JSON 把栅格写成**每行一行格子**（`LevelAssetJson.WriteIntGrid`），改一个格子就是 diff 里改一行里的一个数。
3. **无头输入**：无头验证台没有 AssetDatabase，靠它读到与资产同源的关卡数据
   （`LevelAssetLibrary.LoadFromGoldenJson`；`Assets/Tests/Battle/LevelAssetTests.cs`
   的 `Library_LoadedFromGoldenJson_…` 钉住"无头读的就是它"）。

### 改一张关卡的流程

| 步骤 | 怎么做 |
| --- | --- |
| ① 改数据 | 改 `_golden/<name>.json`（推荐，diff 可读）**或**在 Inspector 里改资产 |
| ② 落盘 | 菜单 `PirateCrew/关卡/迁移关卡资产（golden JSON → 资产）` 或 `回写 golden JSON（资产 → 文本锚点）`，跑对应方向 |
| ③ 自查 | 菜单 `PirateCrew/关卡/校验关卡资产`：连通性 / 出生点 / 高度范围 / 武器 id / 清单登记 / 文本等价 |
| ④ 门禁 | EditMode 全部用例（含 `LevelAssetGateTests` 的四条） |
| ⑤ 数值 | 若改了既有数值，`LevelAssetTests` 的**冻结期望值表**会红——那是设计裁决，确认过再改表 |

> 「改动 == 改代码 + 重编译」这件事到此为止：改 JSON 或改资产都不需要碰 C#。

---

## 4. 运行时怎么读：来源解析器

关卡来源的优先级**只写在 `LevelSourceResolver` 一处**（`Scripts/PirateCrew/Battle/Levels/LevelSourceResolver.cs`）：

```
① -artReviewLevel N（N 有关卡资产）        → 该关卡资产
② 无出图覆盖 且 有 -worldMap / SetPending  → 该海图
③ 否则                                     → 兜底关卡 1（并留下 Notice）
```

- 解析结果是 `LevelSource`：出战计划 / 逻辑高度场 / 水位 / 图幅 / 氛围档全部已算好，
  `BattleController.BuildLevelSource`（`BattleController.cs:244`）只取数、不再判"这局是不是海图"；
  表现层唯一的一处分派是 `RebuildSceneArt` 的 `switch (_source.Kind)`（`BattleController.cs:283`）。
- `Resolve(int, WorldMapDefinition)`（`LevelSourceResolver.cs:122`）是**可注入的纯函数重载**：
  不读命令行、不读静态状态，故"优先级没变"是可测断言而非常识（见 `LevelAssetTests.LevelSourceResolver_*`）。
- 命令行开关名与语义**未变**：`-worldMap <id>` / `-artReviewLevel N`（argv 解析仍在
  `Core/CommandLineOptions`，本轨道只换数据来源）。
- `WorldMapCatalog` 与 `SceneArt.ShowcaseLevels` 现在是**读口**：前者把载荷转成
  `WorldMapDefinition` 并维持 `All` / `Count` / `TryGet` / `TryGetByLevelNumber` 查表接口
  （UI / 战役 / 待战入口只认它），后者供 `BuildLevelData` / `BuildLogicGrid` / `BakedPlacements` 三个查询。

---

## 5. 工具与判据

### 5.1 迁移器（`Assets/Editor/Levels/LevelDataMigrator.cs`）

双向，两个入口（菜单 `PirateCrew/关卡/…`，无头 `-executeMethod`）：

- `MigrateAll()`：`_golden/*.json` → `.asset` + 重写清单。写出内容只由载荷决定
  （字段顺序 / 缩进 / 数字格式全固定），**重复跑逐字节一致**；清单里的引用 guid 由路径派生。
- `WriteGoldenFromAssets()`：资产 → `_golden/*.json`（在 Inspector 里改完数据后同步文本锚点）。

**旧 C# 表怎么处置**：`WorldMapCatalog` 的 8 图手写表与 `ShowcaseLevels` 的三关高度场
**已从源码删除**（判据"运行时不再有硬编码关卡数据"），实现只留在 git 历史里。
一次性抽取（C# 旧表 → golden JSON）由工作台脚本 `external/levelgen/`（不入库）用
`Compile Remove` 换回迁移前的两个文件、逐字段写载荷完成——它是**一次性工具**，
交付侧保留的是"golden JSON ⇄ 资产"这段必须能重复跑的链路，不依赖任何被删掉的代码。

### 5.2 校验器

判据体只有一份，两个入口共用：

| 入口 | 位置 | 覆盖 |
| --- | --- | --- |
| 规则层（纯 C#，无头可跑） | `Scripts/PirateCrew/Battle/Levels/LevelAssetRules.cs` | 连通性、出生点落位/净空、高度范围、武器 id 在 `WeaponCatalog`、单位站位在实心格、编成双方非空、队伍归属与导出符号一致、栅格自洽、摆件 id 已登记；**尺度告警**（出生质心间距 > 120u，提案/待定） |
| 编辑器校验器 | `Assets/Editor/Levels/LevelAssetValidator.cs` | 规则层全部 + 文件层（golden JSON 存在 + 资产正文 == 其确定性渲染 + 清单登记/空引用/重复 id） |
| 内容门禁测试 | `Assets/Tests/Battle/LevelAssetTests.cs`（无头）、`LevelAssetGateTests.cs`（EditMode，`#if UNITY_EDITOR`） | 前者跑规则层 + 文本等价 + 冻结期望值；后者跑 `AssetDatabase`/`Resources` 真实加载（手写 YAML 能否被 Unity 反序列化、清单是否把 11 张资产带进来） |

`LevelAssetGateTests` 加 `#if UNITY_EDITOR` 是必须的：无头域的 `Assets/Tests/Battle/*.cs`
不引用 UnityEditor，不加条件会把主控的 Battle 域门禁打成编译错误
（同 `Assets/Art/Tests/PixelArtTextureImportTests.cs:15-21` 的处置）。

### 5.3 语义零漂移的证据

- `LevelAssetTests.Assets_MatchGoldenJson_ByteForByte`：资产文本 == golden JSON 渲染（逐字节）。
- `LevelAssetTests.WorldMaps_MatchFrozenLegacySignature` / `Levels_MatchFrozenLegacySignature`：
  两张**冻结期望值表**手抄自迁移前的 C# 目录表（`WorldMapCatalog` / `ShowcaseLevels`，git 历史），
  独立于资产与 golden JSON。即使有人用漂移过的数据重跑迁移器，这里也会红。
- 原有海图/关卡测试全绿：连通性（`WorldMapConnectivityTests`）、装填（`Tests/WorldMaps/WorldMapLoadoutTests`）、
  远景（`WorldMapHorizonFeatureTests`）、置物（`WorldMapPropPlacementTests`）、
  样板关设计契约（`Tests/SceneArt/ShowcaseLevelDesignTests`）——它们现在读的都是资产数据。

---

## 6. 新增一张关卡

以新增非海图关卡（第 4 关）为例：

1. 写设计文档 `docs/设计/关卡/L04-*.md`（管线阶段 2，见 `关卡制作管线.md`）。
2. 在 `Assets/Data/Levels/_golden/` 放 `l04_<slug>.json`（照抄现有 JSON 的字段顺序即可；
   栅格 `blocks` 就写格子行）。
3. 跑菜单 `PirateCrew/关卡/迁移关卡资产`：生成 `.asset` 并把它登记进 `LevelCatalog`。
4. 跑菜单 `PirateCrew/关卡/校验关卡资产` 自查；再跑 EditMode 门禁。
5. 在 `LevelAssetTests` 的 `FrozenLevelSignatures` 补一条冻结期望值，
   并确认 `ShowcaseLevels.FirstLevel/LastLevel` 与相关范围常量仍覆盖新关卡号
   （当前 `IsShowcase` 是"是否有对应资产"的查询，不再靠写死的 1–3）。

新增海图同理，区别是：id 段（101–108）、`Assets/Data/WorldMaps/`，
以及**连通性硬约束**——承载出生点的站面 box 必须同连通分量（`WorldMapRules.MaxJumpGap = 13u`、
`MaxUpStep = 3.5u`，`Battle/WorldMaps/WorldMapRules.cs:17-19`），校验器会直接报红。

---

## 7. 已知边界（不在本轨道范围内，登记而非顺手改）

1. `docs/设计/关卡/关卡制作管线.md` 阶段 3「灰盒落地」仍写"译成代码数据：`ShowcaseLevels` 的高度场"——
   现已是资产 + golden JSON，该文档需按本文档改写（该文件不属于本轨道文件域）。
2. 仓库 `.gitattributes` 只对 `*.meta` / `*.asset` 等钉了 `eol=lf`，**`*.json` 未钉**：
   在 `core.autocrlf=true` 的机器上重新签出时 golden JSON 会变 CRLF。
   对拍判据只比对 `.asset` 正文（已钉 LF）且 JSON 解析器容忍 CRLF，故门禁不受影响；
   但建议后续把 `*.json text eol=lf` 补进 `.gitattributes`。
3. `Assets/Editor/M2DataAssetGenerator.cs` 的类头注释仍写"世界海图走 `WorldMapCatalog` 纯 C# 目录，
   无 SO 资产"——已过时（该文件不属于本轨道文件域）。
4. 一代遗留的悬空注释仍在别处：如 `Scripts/PirateCrew/Ambient/AmbientRules.cs:83` 提到已删除的
   `Data/LevelCatalog`、`AmbientCreatureRules.cs:702` 提到已删除的 `Assets/Editor/SceneArtBuilder.cs`。
   本轨道只清了 `Battle/Terrain/TileTerrainGrid.cs` 里那类引用已删除类型的尸体注释。
